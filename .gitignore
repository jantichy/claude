# Historie a session data
history.jsonl
sessions/
session-env/
shell-snapshots/

# Cache
cache/
image-cache/
paste-cache/
file-history/
downloads/
telemetry/
mcp-needs-auth-cache.json

# Per-project data (paths, contexts, settings per project)
projects/

# Plány (dočasné, session-specific)
plans/

# Tasks
tasks/

# Autoprompt log – nikdy neverzovat, obsahoval by soukromé prompty
PROMPTS.md

# Pluginy
plugins/

# Zálohy
backups/

# Runtime (daemon, background jobs, stavové soubory)
daemon/
daemon.lock
daemon.status.json
daemon-auth-cooldown
daemon-auth-status.json
jobs/
.last-cleanup
.last-update-result.json

# macOS
.DS_Store
