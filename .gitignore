# Historie a session data
history.jsonl
sessions/
session-env/
shell-snapshots/

# Cache
cache/
image-cache/
paste-cache/
file-history/
downloads/
telemetry/
mcp-needs-auth-cache.json

# Per-project data (paths, contexts, settings per project)
projects/

# Plány (dočasné, session-specific)
plans/

# Tasks
tasks/


# Pluginy
plugins/

# Skilly synchronizované z claude.ai (cache harnessu, ne obsah repozitáře)
skills/synced/

# Drafty zpětné vazby (harness; nesou cesty a request ID z jiných session)
feedback/

# Zálohy
backups/

# Runtime (daemon, background jobs, stavové soubory)
daemon/
daemon.lock
daemon.log
daemon.status.json
daemon-auth-cooldown
daemon-auth-status.json
jobs/
.last-cleanup
.last-update-result.json

# Python – meta-testy v tests/ vyrábějí bytecode cache
__pycache__/
*.pyc

# macOS
.DS_Store

# Běhový stav skillů (fronta nálezů z /review, /oponent – může obsahovat citlivý obsah)
.claude/run/

# Tajemství – návnady i skutečné soubory; deny pravidla je blokují modelu,
# .gitignore je drží mimo veřejný repozitář
.env
.env.*
