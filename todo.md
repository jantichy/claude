# Co zbývá

Úkoly na konfigurační vrstvě Claude Code: v pravidlech, skillech, hoocích, testech a průběžné kontrole.

**Repozitář je veřejný.** Nic, co sem přibude, nesmí prozradit **obsah** soukromého `~/Dev/context/` – jméno klienta či organizace, sazbu, obchodní nebo osobní údaj, detail přístupu ke klientskému systému, jméno klientského projektu ani know-how, které se prodává. Struktura toho adresáře veřejná je a `README.md` ji sám píše; konkrétní obsah ne. Podrobněji `.claude/CLAUDE.md`, *Výjimky z obecných pravidel*.

**Nezávazné nápady sem nepatří** – co nikdo neschválil ani nezamítl, leží v `backlog.md`; sem jde jen to, u čeho je rozhodnuto, že se udělá, i kdyby to bylo až za rok. Hranici drží `STRUCTURE.md`, *`backlog.md`*.

**Hotové věci se sem nevracejí** – jakmile je úkol hotový, přesune se do `done.md`.

- [ ] **Rozhodnout, jestli mez 12 znaků pro `header` v `AskUserQuestion` platí, nebo ne.** `~/.claude/RULES.md`, *Ptej se postupně, ne všechno najednou*, ji uvádí jako tvrdou, ale pět skillů ji překračuje právě tím tvarem, který je nejužitečnější – číslem položky v hlavičce: `/cleanup` má `Téma N/celkem` (14 znaků) a `Položka N/celkem` (16), `/review`, `/attack` a `/oponent` shodně `Nález N/celkem`. Našel to čtenář bez kontextu 20. 9. 2026 na rozporu se `skills/cleanup/out-of-scope.md`, kde se mez nově cituje jako důvod, proč číslo v hlavičce **není**.

  **Jsou tři možnosti a každá něco stojí.** Buď mez platí a všech pět hlaviček se zkrátí – pak uživatel ztratí orientaci v tom, kolikátá otázka z kolika přichází, a získá ji jen z výpisu nad otázkou. Nebo mez neplatí a opraví se pravidlo – pak je potřeba zjistit, co se s delší hlavičkou v rozhraní opravdu stane, protože dnes to nikdo neměřil. Nebo se mez zjemní na „do 12 znaků, číslo položky se nepočítá“, což je výjimka a musí mít napsaný důvod.

  **Nejdřív to změř, pak rozhoduj:** mez se vzala z dokumentace nástroje, ne z pozorování. Zjisti, jestli se delší `header` ořízne, zalomí, nebo projde.

- [ ] **Rozhodnout, čím stahovat cizí články do projektu** (zadáno 18. 9. 2026). Při zakládání projektu pro klientský web se ukázalo, že se jeho vydané články stahovaly a převáděly do Markdownu ručně – a přitom **mechanika už existuje v `/compose collect`**, který tímhle způsobem postavil celý `~/Dev/context/archive/`: stahování po zdrojích, ověření úplnosti proti datům (`X-WP-Total`, sitemapa), převod do jednotné podoby, konvence pojmenování `YYYYMMDD - Titulek.md`. Postupy a pasti jednotlivých zdrojů drží `~/.claude/skills/compose/sources.md`.

  **Proč se to nedá použít rovnou:** `/compose collect` plní **Honzův** archiv, ze kterého `/compose profile` destiluje jeho hlas. Proto v něm jsou věci, které u cizího webu nedávají smysl – značení AI textů, aby se učení nezacyklilo, vyřazování cizích zadání, časové vážení ročníků. Cizí články se naopak nestahují kvůli hlasu, ale kvůli znalosti o klientovi a kvůli tomu, aby bylo vidět, jak se na tom webu píše.

  **Tři varianty, žádná zatím nevyhrála:** (1) **zobecnit `collect`** o cílový adresář a přepínač „cizí korpus“ – nejmíň práce, ale skill je o psaní Honzovým hlasem a tohle do něj tematicky nepatří; (2) **vytáhnout stahovací část do sdíleného postupu**, který si zavolá `/compose collect` i nový skill – čistší, ale je to zásah do fungujícího skillu; (3) **samostatný skill** na stažení cizího webu do projektu – nezávislý, ale zduplikuje se v něm mechanika, kterou `sources.md` už popisuje. **Rozhodne se, až bude potřeba stáhnout druhý cizí web** – na jednom případu se nepozná, co je z toho společné.

- [ ] **Vyladit skill `/diagram` po prvních ostrých bězích.** Vznikl 17. 9. 2026 vytěžením ze session nad rezervačním systémem a Honza ho založil s tím, že ho bude ladit později – je to zápis jedné session, ne vyzkoušený postup. **Co se při vzniku neověřilo:** plný běh až do publikace artefaktu (srovnávací i tlakový běh končily před publikací), vyvolání doslovným `/diagram` (měřilo se jen popisem situace, 24 z 24) a test na nosnou část skillu, který zatím není. **Kandidáti na ladění:** čtení schématu z migrací nebo ORM (dnes vědomě jen z dokumentace, viz `decisions.md`, *Skill `/diagram`*) a vytažení parseru nebo šablony stránky do vedlejších souborů skillu, až se ukáže, co je napříč projekty společné. **Co ostré běhy ukážou, patří do sekce *Časté chyby* skillu**; úkol je hotový, až skill projde plným během ve dvou projektech s rozdílně psanou dokumentací modelu. *Nosnou částí* se tu myslí pravidlo „nekreslit hranu, kterou dokumentace nejmenuje“ – to je kandidát na test.

- [ ] **Naučit `/depot` pracovat s proudem a pamatovat si, kde skončil** (rozhodnuto 22. 9. 2026, návrh ověřen ostrým během téhož dne). Dnes stojí skill na pravidle *rozsah je přesně to, co stojí v argumentu, ani o soubor víc* a zdrojem je jmenovaná dávka souborů. Honza chce navíc tvary `/depot downloads` a `/depot email`, případně s intervalem: zdrojem je **proud**, do kterého se skill opakovaně vrací, takže musí vědět, co už viděl.

  **Mechanismus je celý navržený, ověřený a zapsaný jinde – neopisuj ho sem zpátky.** Dvě vrstvy paměti, dvě osy tabulek, tři úrovně duplicity i tvar `_state/` drží `~/Dev/context/depot/depot.md` (*Zdroje* a *Workflow dokumentu*); proč to tak je a co se zamítlo, `~/Dev/context/decisions.md`, sekce `depot`.

  **Co z toho už funguje:** doména to popisuje celé a `_state/` existuje a je naplněný – aktuální hodnoty drží on, ne tenhle zápis. **Jeden celý běh proběhl 22. 9. 2026 ostře** – `/depot` nad Downloads: deset souborů rozpoznaných obsahem, duplikát odhalený hashem, dvě faktury zpracované až do Fakturoidu, sedm položek vědomě přeskočených a čára posunutá za ně. Kolik dokladů prošlo celkem toho dne, drží `~/Dev/context/done.md`, sekce `depot`.

  **Co zbývá udělat ve skillu:** jel jsem to celé ručně podle domény, `SKILL.md` o proudu neví nic. Musí se přepsat:

  - **Sekce *Rozsah*** – dnes zakazuje jít do adresáře sám a je opřená o srovnávací běh, kde si agent rozsah rozšířil na pět tisíc položek. **Ta ochrana nepadá, ale nově ji drží interval a čára místo výčtu cest** – napiš to jako změnu s důvodem, ne jako tiché zmírnění, jinak to příští revize vrátí zpátky.
  - **Rozpoznání zdroje v argumentu** – `/depot downloads` a `/depot email` nejsou ani cesta, ani dnešní režim. **Jména zdrojů jsou anglická** (`~/.claude/RULES.md`, *Jazyk*); česky je Honza zadává v řeči (`/depot stažené`) a skill je má rozpoznat jako totéž, ne jako vlastní režim.
  - **Fáze 2 až 4** – práce s proudem, čtení a zápis `_state/`, průnik intervalu s čárou a řádek v plánu, kolik položek čára odfiltrovala.
  - **Zúžení zákazu mazání.** Dnešní *Hranice* říkají „skill nemá jediný důvod volat `rm`“. Platí dál **mimo potvrzený duplikát s ověřeným originálem**, který jde do koše – nikdy přes `rm`. Napiš to jako zúžení s důvodem, ne jako povolení mazat.
  - **Vědomé přeskočení jako plnohodnotný výsledek** – bez něj se čára nepohne nikdy.

- [ ] **Projít startovní tabulku workflow v `~/Dev/context/depot/depot.md`.** Startovní tabulku napsal 15. 9. 2026 Claude jako návrh – nejsou to Honzova pravidla. Čtyři řádky (nahrávka, materiál z akce, cizí dokument, vlastní poznámky) jsou odvozené z toho, co v `~/Depot` opravdu leží, a dají se brát za doložené; **řádek přijaté nákladové faktury přibyl 22. 9. 2026 a ostrým během ověřený je**. **Nedořešené zůstávají dva:**

  - **Řádek *Uložit ke klientovi*** – dnes v tabulce stojí, že se mezi `~/Dev/<project>/` a `~/Documents/Projekty/<client>/` rozhoduje podle toho, jestli klient má repozitář. Je to odhad. Ve hře byly ještě dvě varianty: rozhodovat podle **povahy podkladu** (do `~/Dev/` jen to, z čeho se bude pracovat v kódu – exporty, zadání, data; zápisy, nahrávky a obchodní věci vždycky do `~/Documents/Projekty/`, i když repozitář existuje), nebo mít **jediný cíl** `~/Documents/Projekty/<client>/` bez výjimky.
  - **Řádek *Odložit osobní věc*** – cíl `~/Documents/` je hrubý, konkrétní podadresář zapsaný není.

  Honza to 15. 9. 2026 odložil s tím, že se k tomu vrátí. **Do té doby skill nad klientským podkladem jede podle odhadu** – a přesun je vidět až zpětně. Vědomé mezery domény se od té doby změnily a **výčet sem neopisuj** – drží ho `~/Dev/context/depot/depot.md`, *Co zatím zapsané není*.

- [ ] **Pravidlo *Commituj jmenované cesty, ne `-A`* se porušuje i poté, co vzniklo.** Zapsalo se 3. 9. 2026 do `~/.claude/RULES.md` právě proto, že souběžná session dvakrát smetla cizí rozpracovanou práci. **6. 9. 2026 se to stalo potřetí** – commit `3a22221` v `~/.claude` zatáhl šest rozpracovaných skriptů `skills/compose/scripts/*.py` z druhé session, která je commitovala až o deset minut později. Session, která to udělala, si toho navíc nevšimla a v shrnutí tvrdila opak. **7. 9. 2026 počtvrté a v obou repozitářích naráz** – commity `1222311` (`~/.claude`) a `a651d4e` (`~/Dev/context`) sebraly rozpracovanou práci session, která zaváděla `backlog.md`: sedm souborů včetně `tests/test_skills.py` a `structure/structure.md`. Obsah se neztratil a průběžná kontrola nad ním prošla, ale sedí pod zprávou o typografii. **Poškozená session si toho tentokrát všimla** při vlastním commitu (`nothing to commit, working tree clean`) – to je zatím jediná detekce, kterou máme. **Popáté téhož dne, tentokrát bez škody:** commit `50e1401` v `~/.claude` použil `-A`, ale zabalil jen vlastních dvanáct souborů, protože v repozitáři zrovna nic cizího rozpracovaného neleželo. Session, která to udělala, si toho všimla až zpětně při úklidu. **Je to nejpoučnější z pěti výskytů:** ukazuje, že absence škody není doklad správného postupu – rozhodovala shoda okolností, ne to, co se zadalo. Čtyři předchozí výskyty se od tohohle liší jen tím, že v repozitáři někdo souběžně pracoval.

  **10. 9. 2026 přibyl šestý výskyt, a ten je jiný než předchozích pět: pravidlo se neporušilo, jen nestačilo.** Commit `7681ec6` v `~/Dev/context` (session přesouvající životní cyklus do `LIFECYCLE.md`) zatáhl rozepsané změny session, která uklízela po rozdělení `PTYDEPE.md` – jenže obě psaly do **téhož souboru** `decisions.md`, každá do své sekce. Jmenování cest tady nepomůže z principu: `git add decisions.md` sebere celý soubor včetně cizích řádků. Obsah se neztratil, ale odůvodnění zamítnutého testu a oprava vycucaného počtu hesel sedí pod commit message o životním cyklu.

  **11. 9. 2026 přibyl sedmý výskyt a je zase jiný: `git add <directory>`.** Session běžící `/consistency full` nad `~/Dev/context` použila `git add business`, čímž do commitu `7ebc203` zatáhla rozpracovanou práci souběžné session na `/invoicing` – katalog zdrojů pro `recover`, měření hranice session logů a nové pravidlo o vyplňování polí. **Pravidlo tenhle tvar nejmenuje**: zakazuje `-A`, `.` a `-a`, kdežto adresář vypadá jako jmenovaná cesta, a přitom nese totéž riziko.

  **15. 9. 2026 přibyl osmý výskyt a přinesl druhý způsob detekce.** Commit `82fb4ce` v `~/.claude` („Třetí stupeň závažnosti je NÍZKÉ a platí i pro /audit“) zatáhl odstavec o `/depot`, který do kořenového `README.md` psala souběžná session zakládající ten skill. Obsah se neztratil, rozešlo se jen zdůvodnění: `git blame` nad tím odstavcem ukáže commit o závažnosti nálezů. **Poškozená session si toho všimla až při vlastním commitu**, kdy `README.md` chyběl ve výpisu `git status` – to je po detekci z 7. 9. (`nothing to commit, working tree clean`) druhý doložený způsob, jak se to vůbec pozná, a oba fungují až zpětně a náhodou.

  **Škoda byla tentokrát jiného druhu než u předchozích šesti.** Tam šlo o to, že cizí práce sedí pod cizím commit message. Tady k tomu přibylo, že **commitující session ten text nepřečetla, a pak proti němu rozhodovala**: o dva nálezy později prohlásila, že šablona *Stop práce* je výčet možných zdrojů, ne povinných – v době, kdy v ní už deset minut stálo pravidlo „Pole se vyplňují všechna, i když odpověď je `nemá`“, které sama commitla. Chyba se našla až ve druhém průchodu auditu. **Je to první výskyt, kde nešlo jen o atribuci, ale o věcně špatné rozhodnutí postavené na nepřečteném vlastním commitu.**

  **21. 9. 2026 přibyl devátý výskyt a je poprvé z druhé strany.** Commit `9455ff2` (session rozhodující `/evaluate`) zatáhl rozpracovanou práci session, která nad týmž repozitářem běžela `/cleanup`: opravu odstavce o ověřovací vrstvě `/discovery` v `todo.md` a celý nový záznam *Předjímané `Časté chyby` se ve skillu nedrží* v `decisions.md`. Obojí teď sedí pod zprávou „Rozhodni, jak se do cyklu zapojí zpětná vazba z provozu“. **Předchozích osm výskytů popisovalo situaci, kdy práci sebrala tahle session cizí; tenhle je opačný** – a ukazuje, že detekce, kterou máme, funguje jen u toho, kdo commituje, ne u toho, komu se to stane: úklidová session to zjistila **náhodou**, při `git rev-parse` kvůli záznamu do `done.md`, protože hash nesouhlasil s jejím posledním commitem. Bez toho kroku by o tom nevěděla vůbec.

  **Mění to zadání téhle položky.** Dosud se hledalo, jak vynutit jmenování cest; tenhle výskyt ukazuje, že to řeší jen půlku problému. Druhá půlka je souběžný zápis do jednoho souboru a proti té by musel jít jiný mechanismus – commit po hunku (`git add -p` s vlastním filtrem), nebo detekce, že se soubor mezi čtením a zápisem změnil cizí rukou.

  **Je to doklad, že zákaz psaný souvislým textem na tuhle třídu chyb nestačí** (`RULES.md`, *Ověřitelná kontrola místo dojmu*: co má chytat stroj, nemá být jen v textu). Řešení ale zatím není a hledá se jinde než v hooku.

  **Zamítnuto – `PreToolUse` hook, který odmítne `git add -A`, `git add .` a `git commit -a`.** Nabízelo se to jako nejpřímější kontrola; Honza to 7. 9. 2026 odmítl jako **zbytečné** s tím, že se s tím poperem jinak. Kontrola by navíc hlásila falešné poplachy (v čerstvém repozitáři je `git add -A` správně), hooky běží mimo permission systém, a hlavně by léčila symptom místo příčiny. Nenavrhovat znovu.

  **Kde hledat dál:** příčinou není příkaz, ale **sdílený pracovní strom** – dvě session nad jedním adresářem si vidí do rozpracovaného. Souvisí to s vedlejší položkou o souběhu dvou session (tam jde o průběžnou kontrolu, tady o commit, ale příčina je táž) a nabízí se řešit obojí naráz, například worktree layoutem i pro `~/.claude`.

- [ ] **Dorovnat zbylé projekty na kontrakt příkazů a zapnout v nich průběžnou kontrolu.** (Dvě dřívější položky sloučené v jednu – byla to tatáž práce se stejným seznamem osmi projektů.) **Samotný kontrakt kontrolu nezapne:** hook spouští příkazy jen tam, kde člověk vydal souhlas `~/.claude/verify.sh --allow <project>`, a ten musí spustit uživatel ručně, ne Claude – je to souhlas se spouštěním kódu z repozitáře. Průchod `~/Dev` (2. 9. 2026) našel 12 projektů s kódem. Čtyři už kontrakt mají, u jednoho z nich zbývá commitnout – visí to v rozpracovaném stromu. Zbylých osm ho nemá kam napsat, protože **nemají `CLAUDE.md` vůbec**. U každého je potřeba nejdřív rozhodnout, jestli je to živý projekt (pak `/project`), nebo archiv (pak nechat být). Bez kontraktu v nich neběží průběžná kontrola, takže Claude v nich může uzavřít práci nad červenými testy.

  **Od 14. 9. 2026 k tomu přibyly kontrolní vrstvy.** `/project`, Krok 12 se přejmenoval na *Kontrakt příkazů a kontrolní vrstvy* a nově zakládá i CI podle `skills/project/checks.md`; katalog toho, co do které vrstvy patří, drží `~/Dev/context/coding/quality.md`, *Vrstvy kontroly a co do které patří*. **CI má zatím jediný projekt**, a to od 15. 9. 2026 (do té doby žádný, ověřeno průchodem `~/Dev` 14. 9.). Ve zbytku běží deterministické kontroly jen tehdy, když někdo ručně pustí `/review`.

  **Nepouštět `/project update` hromadně.** Je interaktivní, u každého projektu se rozhoduje jinak a u části z nich se už možná nepracuje. Rozumnější je pustit ho u projektu ve chvíli, kdy se k němu příště dostaneš – v pořadí podle poslední aktivity; prvních pět pokrývá práci posledního týdne (stav 14. 9. 2026).

- [ ] **Zámek na souběžné běhy průběžné kontroly.** Review (2. 9. 2026) našlo, že dvě session Claude Code nad týmž projektem spustí `typecheck`/`test` současně ve stejném adresáři. Stav ani souhlas si nepřepíšou (jsou oddělené podle session), ale sdílené artefakty ano – `tsconfig.tsbuildinfo`, `.next`, vitest cache, testovací databáze, obsazený port. Jedna ze session pak dostane selhání, které nezpůsobila, a zablokuje si odpověď. Řešení je atomický zámek přes `mkdir` v adresáři stavu, s uvolněním v `trap` a ignorováním zámků starších než pár minut. Nedělá se hned, protože dvě session nad jedním projektem jsou u mě spíš výjimka – ale až to nastane, projeví se to jako náhodně padající krok bez zjevné příčiny.

  **Zámek sám je od té doby hotový** – `verify.sh` ho drží přes `mkdir "$LOCK"` v adresáři stavu a `tests/test_verify.py` ho testuje; ověřeno 14. 9. 2026. **Otevřená zůstává druhá půlka**, kterou popisuje odstavec níž: cizí rozpracovaná změna ve sdíleném stromu. Ta nastala znovu 14. 9. 2026, kdy nad `~/.claude` běžely dvě session a v pracovním stromu ležela cizí rozdělaná práce po celou dobu běhu.

  **Nastalo to 6. 9. 2026 v `~/.claude` a projevilo se jinak, než tahle položka čekala** – ne kolizí artefaktů, ale sdíleným pracovním stromem. Dvakrát po sobě spadla průběžná kontrola na cizí rozdělané práci (`/discovery` jmenovaný v `RULES.md` bez existujícího skillu) a blokovala uzavření vlastní, nesouvisející změny; k tomu cizí autocommit sebral rozpracované soubory pod svou zprávu. **Zámek na příkazy tohle neřeší** – průběžná kontrola měří celý strom, takže cizí rozpracovaná změna ji shodí bez ohledu na zamykání. Až se to bude řešit, je otázka širší: co má dělat `Stop` hook, když je strom rozdělaný někým jiným.

- [ ] **Oddělit v `/review` deterministické jádro od poradního hala.** Dnes se nálezy z nástrojů a od panelu slévají do jednoho seznamu. Filipovo pravidlo: co je souzené modelem nebo závislé na spuštění, se **nikdy nesmí smíchat** s tím, co je reprodukovatelné – jinak přestane být použitelné jako kontrola. Až budeme chtít v CI gateovat na výstup `/review`, bez té hranice to nepůjde.

- [ ] **Úrovně s gate formulí místo seznamu nálezů v `/consistency`.** Formule: skupina projde, když `deficit ≤ max(0.5, 0.2 × počet hodnocených)`; skupina bez hodnocených dimenzí **blokuje**, nikdy nesplňuje. Kumulativní žebřík vrací jednu větu typu „L2, posun na L3 blokuje deficit 1.0 > 0.5 ve skupině Proč“. Odolné vůči tomu, že se fatální díra v jedné oblasti vyžehlí leštěním jiné – což prostý průměr neumí.

- [ ] **`--fix` jako řízený dialog.** Po auditu projít doporučení shora dolů, u každého ukázat mezeru **a skill, který ji umí opravit**, zeptat se, a na konci nabídnout přeskórování. `/consistency` už interaktivní průchod má – chybí mapování nálezu na konkrétní krok životního cyklu.

- [ ] **Anti-patterns a strop jistoty u specialistů `/review`.** Ke každému specialistovi seznam toho, co **není** nález (parafráze dokumentace, verdikt podle délky textu, „přidejte validaci“ bez určení čeho), a pravidlo, že nález s nízkou jistotou **nikdy nedostane nejvyšší závažnost**. Konkrétnější než pozitivní pravidla a levné na zápis.

- [ ] **Rate-limitované neblokující připomínky.** Stavový soubor pojmenovaný datem (rotace ničením, žádná čistící logika), klíč podle toho, co se připomíná, a zápis klíče **i když se nic nenašlo**. Až budeme chtít připomínat věci jako „projekt nemá CI“ nebo „`plan.md` je 40 dní starý“.

- [ ] **Dorovnat konfigurační vrstvu na přestavěný životní cyklus.** Rozhodnutí i s důvody drží `decisions.md`, zápisy z 20. 9. 2026 (*Životní cyklus se dělí na osu a kontroly*, *`/specify` se dělí na `/specify` a `/architect`*, *Návrh řešení je sada dokumentů*, *`/review` stojí za každým krokem osy*). **Hotové je jádro** – `LIFECYCLE.md`, rámeček v `RULES.md`, `STRUCTURE.md` a norma tvaru v `skills/SKILLS.md`; **„hotové“ tu znamená přestavěné, ne bezvadné** – čtenář bez kontextu v nich při úklidu 20. 9. 2026 našel třináct pozůstatků a ty jsou opravené. **Nehotové je všechno ostatní**; průzkum našel **44 míst, která po změně lžou, 19 neúplných a 21 kosmetických**. Seznam níž je taxativní, ať se nemusí hledat znovu.

  **Dělej to v tomhle pořadí, protože se to podmiňuje – s jednou výjimkou: `/next` (bod 8) šel první.** Rozhodl uživatel 20. 9. 2026: je to jediné místo, kde se cyklus fakticky spouští, a `collect.py` tehdy četl rámeček jako plochou řadu, takže z dvoublokového rámečku vyráběl nesmyslná data, se kterými pak počítala fronta práce. **Bod 8 je od téhož dne hotový**; věta zůstává jako doklad, proč se přednost dala jemu. Čísla bodů se kvůli tomu nepřehazovala – pořadí drželo tohle, ne jejich posloupnost.

  **Stav k 22. 9. 2026: kontrakt příkazů je zelený** – `python3 -m unittest discover -s tests` projde. **Počet testů se tu schválně neuvádí**, rozejde se s prvním doplněným scénářem; kolik jich je, řekne ten příkaz. Hotové jsou body 1, 2, 3, 7, 8 a 9 a k tomu celé rozdělení `/specify` (viz `done.md`), takže zbývají body 4, 5, 6, 10 a z bodu 11 dvě ze tří položek. **Seznam se 20. 9. 2026 přečísloval** – dřívější bod 5a se stal pětkou a zbytek se posunul, protože písmenný bod stál nad pětkou a v Markdownu tím končila číslovaná řada. Body 1 až 3 se dorovnávaly v tomhle pořadí a testy se u toho chovaly přesně tak, jak se čekalo: po opravě měřicích funkcí se z 28 selhání stal jmenovitý seznam práce (*„specify: mluví o svém pořadí, ale vzor na tvar té věty nesedne“*), který zhasínal, jak se texty přepisovaly.

  1. ~~**Testy první, jinak nic nechytí.**~~ **Hotovo 20. 9. 2026.** `cycle_with_order()` čte rámeček jako dvě vrstvy: vrstvu zakládá jen řádek, který začíná jejím jménem, takže odsazené pokračování osy už nevyrábí vrstvu `/breakdown`. **Pořadí a sousedy má jen osa** – kontrolní kroky mají u všech tří polí `None` a `_one_step_sentence()` u nich měří opak: že o svém místě v cyklu mluví a že to nedělá větou o pořadí. Počet vět se odvozuje z počtu existujících kroků osy, ne z konstanty. **Krok bez skillu se nezakazuje, ale musí být přiznaný:** nová `cycle_missing_skills()` ho čte z `LIFECYCLE.md` a `test_cycle_was_read` ho porovná se skutečností v obou směrech, takže odstavec nepřežije svůj důvod ani nebude chybět. Kotvou kontroly „mluví o pořadí, ale vzor nesedne“ je nově odkaz na cyklus, ne znění věty za ním – vzor psaný podle dnešního znění mlčel přesně nad tím, co se přepisuje.
  2. ~~**Rámečky v README**~~ **Hotovo 20. 9. 2026** – nejdřív deset tehdy existujících, po vzniku `/architect` všech dvanáct. **Bez odkazu v nich dnes stojí jen `/consolidate`** a hromadná instalace ho nejmenuje; hlídá to test proti `cycle_missing_skills()` v obou směrech, takže se sám připomene, až skill vznikne.
  3. ~~**Věty „je to N-tý krok, navazuje na X a předává na Y“**~~ **Hotovo 20. 9. 2026** – všech deset. Kroky osy nesou pořadí i oba sousedy, kontrolní kroky místo toho větu „je to kontrolní krok, ne bod na ose“ a výčet mezer, ve kterých stojí. Věty o chybějícím `/architect` ve `/specify` a `/breakdown`, které tu stály do vzniku toho skillu, zmizely spolu s ním.
  4. **Závěrečné verdikty „Další krok“** – `implement:141` a `implement/README:54`, `review:352`, `consistency:289`, `cleanup:392`, `attack:322`, `project:460`, `release:219` (ten ukazuje na **„krok 9“**, což je po zrušení číslování nesmysl). **Čísla řádků ber jako vodítko, ne adresu** – soubory se od sepsání seznamu měnily. `/specify` z výčtu vypadlo: rozdělením se přepsalo celé a jeho verdikt dnes míří na `/architect`; **`/architect` v něm nikdy nebyl a nepatří tam** – vznikl už s novým cyklem a jeho verdikt míří na `/breakdown`.
  5. **Doplnit do `/review` a `/oponent` vymezení podle měřítka.** Rozhodl uživatel 20. 9. 2026 při úklidu: pravidlo *„`/review` má měřítko v souboru, `/oponent` žádné nemá“* má být kromě `LIFECYCLE.md` (kde už je) i v kapitolách *Co skill nedělá* obou skillů, protože se tam vymezují navzájem. `/review` dnes říká jen „neposuzuje, jestli je záměr dobrý“, což nad dokumentací nestačí; chybí věta, že nad projektem bez relevantního standardu je skoro prázdný a má se přeskočit. Rozbor v `decisions.md`.
  6. **Vymezení „o krok dřív / až po tomhle“** v kapitolách *Co skill nedělá* – sedmnáct míst: `breakdown:19`, `review:29,30`, `consistency:21,23,30,38,75,95`, `cleanup:24,26,44`, `implement:17`, `attack:38,44`, `release:19,121`. **Čísla řádků ber jako vodítko, ne adresu** – u `/cleanup` neplatí vůbec, protože se 25. 9. 2026 přepsal celý. **Nejhorší je `consistency:75`** („typecheck ani linter tady nespouštěj, pustil je `/review` o krok dřív“) – je to operativní pokyn, který po změně může vynechat kontrolu.
  7. ~~**Kořenové `README.md`**~~ **Hotovo 20. 9. 2026** – sekce jdou ve dvou blocích podle vrstev cyklu a `/oponent` je popsaný třemi mezerami, ne jako „krok mezi zadáním a plánem“. **Bloky nese pořadí a úvodní odstavec, ne nadpisy** – proč, drží `decisions.md`, *Dvě vrstvy cyklu v kořenovém README nese pořadí, ne nadpisy*. Pravidlo o řazení v `skills/SKILLS.md` dostalo tutéž strukturu i s důvodem, proč jeden blok nestačí: kontrolní krok stojí ve víc mezerách, takže jeho místo v jedné řadě není určené.
  8. ~~**`/next` čte rámeček jako plochý seznam**~~ **Hotovo 20. 9. 2026** – `lifecycle()` vrací `{"osa": [...], "kontroly": [...]}` a `SKILL.md` z toho odvozuje chybějící krok **jen pro osu**; u kontrolních kroků „ještě nepřišel na řadu“ nic neznamená, protože stojí v mezerách a některé ve víc naráz. Zaniklá kategorie „zakládací kroky“ je ze `/next` pryč; v `decisions.md` zůstává jako historický zápis a spadá pod průchod tím souborem níž. Přibyly k tomu testy v `tests/test_next.py`, ověřené mutací – se starou plochou podobou padají.
  9. ~~**Hromadná instalace v jedenácti README**~~ **Hotovo 20. 9. 2026** – ve dvanácti README i v předloze v `skills/SKILLS.md`. Výčet teď jmenuje `/architect` a **záměrně nejmenuje `/consolidate`**: pokyn, který si vyžádá neexistující adresář, se nepovede celý, takže by chybějící krok shodil i instalaci hotových. Hlídá to test proti `cycle_missing_skills()` v obou směrech, takže se `/consolidate` sám připomene, až vznikne.
  10. **Checklisty `/skill`** – `skills/skill/SKILL.md:174, 176, 225, 274, 277` počítají s jedním rámečkem v jedné řadě a s povinnými sousedy z obou stran.
  11. **Drobnosti:** ~~`.claude/CLAUDE.md:12`~~ (hotovo 20. 9. 2026 – věta o konci cyklu přepsána dnešním slovníkem), `skills/SEVERITY.md:21` (výčet skillů hlásících nálezy, přibude `/consolidate`), `STRUCTURE.md:273–289` (kdo zapisuje do *Průchodů životním cyklem* – rozšiřovat ten výčet mlčky se nesmí, takže `/consolidate` se musí doplnit vědomě).

  **Co našel čtenář bez kontextu 20. 9. 2026 a v seznamu výš chybělo:**

  - ~~**`verify.sh` je `Stop` hook, takže červený `test` blokuje každou odpověď v `~/.claude`.**~~ **Vyřešeno 20. 9. 2026 dorovnáním testů** – byl to důvod, proč bod 1 nebyl „první v pořadí“, ale „bez něj se nedá pracovat“. Testy jsou zelené, takže tenhle tlak pominul; zůstává jako doklad, proč se u téhle vrstvy testy dorovnávají dřív než texty.
  - ~~**`/next` je jediné místo, kde se cyklus fakticky spouští**~~ – **hotovo 20. 9. 2026** jako bod 8, přednostně před číslovanou řadou. `collect.py` bere fázi jako `row.split()[0]`, takže ze zalomené osy vyrobí **neexistující fázi jménem `/breakdown`**; není to tedy jen ztráta měření, ale nesmyslná data, se kterými pak počítá porovnání vět ve skillech.
  - ~~**Kořenové `README.md` je jediný soubor, který čte člověk zvenčí**~~ – **hotovo 20. 9. 2026** spolu s bodem 7.
  - ~~**`RULES.md`, *Doc-first vývoj***~~ – **hotovo 20. 9. 2026**, věta jmenuje `/specify`, `/architect` i `/breakdown` a celý řetěz `requirements.md` → `architecture.md` → `plan.md`.
  - ~~**`skills/SKILLS.md`, *Jak se píše text uvnitř*, popisuje rámeček jako „krajní kroky a mezi nimi výpustku“**~~ – **hotovo 20. 9. 2026**: norma i předloha teď říkají, že výpustka je zástupný symbol pro zbytek řady, a rámeček vypisuje všechny kroky i s odkazy.
  - **`STRUCTURE.md` má dvě jména pro týž řádek** – *Návrh sešitý* v `todo.md` proti `- **Návrh uzavřen (<datum>)**` v `done.md`; `skills/next/collect.py` pro jistotu hledá oba. Starší dluh, ale sedí na týž soubor, který se bude přepisovat.

  **Otevřené otázky, které se musí rozhodnout dřív, než se sáhne na texty:**

  1. ~~**Kola návrhu: `/specify`, nebo `/architect`?**~~ **Rozhodl uživatel 20. 9. 2026: `/architect`.** Propsáno do `STRUCTURE.md`, `WORKTREE.md`, `/next` a `/scenarios`.
  2. **Hlásí `/consolidate` nálezy podle `skills/SEVERITY.md`, nebo vrací jen návrhy?** `LIFECYCLE.md` o něm říká, že „jako jediný vrací návrh řešení, ne nález“; bod 11 výš počítá s tím, že do výčtu skillů hlásících nálezy přibude. Jedno z toho je špatně.
  3. **Kam jde zamítnutý návrh `/consolidate`, aby se nepředkládal každý běh znovu?** `RULES.md` má výjimku pro zamítnuté nálezy prověřovacích kroků (kapitoly `## Review` a `## Consistency` v projektovém `CLAUDE.md`) a jmenuje v ní `/review`, `/attack` a `/consistency`. Nový krok, jehož výstup se z definice zamítá častěji, než přijímá, do toho systému zapojený není.
  4. **Má `/consolidate` přibýt do *Průchodů životním cyklem*** (`STRUCTURE.md`, *`done.md`*)? Ten výčet se nesmí rozšiřovat mlčky; kritérium je „má to svého čtenáře“.
  5. ~~**Dorovnat texty, nebo nejdřív napsat oba skilly?**~~ **Vyřešeno 20. 9. 2026 pořadím testy → `/architect` → texty.** Doporučení čtenáře nešlo splnit doslova, protože `/consolidate` čeká na pilotní běh a napsat se zatím nedá; místo toho se zavedl mechanismus pro krok bez skillu – `LIFECYCLE.md` ho přiznává jmenovitě, testy to porovnávají se skutečností v obou směrech, v rámečku stojí bez odkazu a do hromadné instalace nepatří. Texty o něm tedy neslibují nic, co nejde vyvolat.
  6. **Jak se pozná, ve které mezeře člověk stojí?** `LIFECYCLE.md` říká, co v které mezeře smí stát, ale ne jak poznat, kde jsi. U osy to jde z artefaktů (je `requirements.md`? je `plan.md`?), u kontrolních kroků ne – `/consolidate` se pouští „až se nasbírá dost kol“ a `/consistency` „až se nasbírá dost změn“, a ani u jednoho není práh. Zvážit rozhodovací postup přímo v `LIFECYCLE.md`.

  **Neprošlo se `decisions.md` (96 zmínek) a `done.md` (80).** Průzkumník je vynechal jako historický záznam. **V `decisions.md` ale můžou být odůvodnění, která přestavba zneplatnila** – projít je zvlášť a opravit jen ta, která tvrdí o dnešku něco nepravdivého; historické „tehdy to bylo takhle“ se nechává.

- [ ] **Dorovnat rezervační systém na rozdělení `/specify` a vznik `/architect`.** Rozdělení je hotové (20. 9. 2026), ale je to jiný repozitář a `~/.claude/RULES.md`, *Propagace změny*, tam změnu sama nepouští. **Čtyři místa v `docs/todo.md` po rozdělení lžou:** řádek 11 („vyřídí se ve `/specify`“ o dodělku z kola), řádek 39 (sešití návrhu přisuzuje `/specify`), řádek 98 – celá položka *Dorovnat kola návrhu na tvar podle `/specify`* včetně výčtu zrušených režimů `auto`, `create`, `round` a `close` – a řádek 130, který na tu položku odkazuje jménem. **Čtyři zmínky v `done.md` se nechávají**, je to historický záznam; projektový `CLAUDE.md` je v pořádku, scénáře a glosář drží `/specify` dál. Udělá to session, která nad projektem bude pracovat; žádné kolo zrovna neběží, takže to nic neblokuje.

- [ ] **Nový krok životního cyklu `/consolidate` – hledání návrhového dluhu z postupného záplatování.** Jméno i místo rozhodl uživatel 20. 9. 2026: **stojí těsně před `/breakdown`**. Vznikl z konkrétní bolesti v rezervačním systému – návrh se vedl po kolech, v každém se ukázala další kombinace a přidal se na ni sloupec nebo hodnota výčtu (`gateway_closed`, postupně přibývající `gateway_status`). Každý krok byl ve své chvíli správný, ale dohromady z nich vzniklo řešení, které by při znalosti všech případů předem šlo nahradit jedním jednodušším.

  **Proč to musí být vlastní krok a ne hledisko `/oponent`.** Ostatní kontrolní kroky se ptají „je to **špatně**?“, kdežto tenhle se ptá „bylo by to dnes navržené **jinak**?“ – a odpověď „ano“ vadu neznamená, takže propadne každým sítem. `/review` měří kód proti specifikaci, jenže dluh je v samotné specifikaci a kód by ji plnil poslušně. `/consistency` hledá rozejití, jenže tenhle dluh je **dokonale konzistentní** – čím poctivěji se záplata zanesla do všech dokumentů, tím je neviditelnější. `/oponent` je nejblíž, ale posuzuje dokument, jak stojí dnes; nemá odkud vědět, že tři sousední mechanismy vznikly ve třech týdnech ze tří podnětů, protože v textu stojí vedle sebe jako rovnocenné.

  **Čte historii, ne dnešní stav – jako jediný krok v cyklu.** Vstupem není `model.md`, ale `decisions.md`, `done.md`, git log a umlčené nálezy v `CLAUDE.md`. Shluk záplat se pozná z jejich dat narození.

  **Poznatky k designu, které už jsou rozhodnuté** (všechny 20. 9. 2026, k zapracování do zadání pro agenty):

  - **Rozsah vymezuje vstup, ne výstup.** „Nad platební bránou“ znamená, že se z ní vychází, ne že se výsledek smí týkat jen polí s tím prefixem. Nález „ten sloupec je zbytečný“ ušetří sloupec; nález „stavový prostor je vedený podle špatné osy, a proto se do něj tahle věc nevejde bez tří záplat“ ušetří celý shluk – a ten druhý je ten cenný. Omezit výstup rozsahem znamená odříznout si přesně ho.
  - **Zpětná zkouška je hlavní mechanismus, ne kontrola navíc.** Kdo dostane zadání „navrhni to elegantněji“, **vždycky něco navrhne** – a jeho varianta bude působit čistěji právě proto, že nezná zatáčky, kvůli kterým dnešní řešení vzniklo; nezná je schválně, protože jsme mu dali čerstvý pohled zvenčí. Ke každému návrhu se proto taxativně vypíšou všechny doložené případy, které dnešní řešení pokrývá, a u každého se ukáže, čím ho pokrývá varianta nová. Návrh, který tou zkouškou neprojde, se nepředkládá. Bez ní krok vyrábí regrese převlečené za úklid.
  - **Výstupem není nález, ale návrh alternativy plus doklad, že nic neztratil**, a k tomu cena přepisu. Rozhoduje uživatel.
  - **Pouští se nad sloučeným stavem**, ne nad rozdělanou větví – jinak označí za záplatu něco, co je mezitím vyřešené jinak, a chybí mu část dokladů.
  - **Ne po každé featuře.** Je drahý a jeho nález je vždycky velký přepis. Spouštěcí podmínky: povinně před `/breakdown` velkého celku, po několika kolech `/architect`, a kdykoli to člověku leží v hlavě – což je signál sám o sobě.

  **Píše se až po pilotním běhu**, který proběhne ad hoc v rezervačním systému nad platební bránou (zapsáno v jeho `docs/todo.md`). Rozhodl uživatel: ze zadání pro agenty se z hlavy netrefí, co v něm musí stát, a pilot poslouží jako srovnávací běh.

  **Pilot proběhl 20. 9. 2026** v rezervačním systému nad platební bránou; zápis je v jeho `docs/done.md` a výsledek v `docs/decisions.md` §133. **Zadání skillu níž z něj vychází – sepsat ho podle `skills/SKILLS.md` je už jen mechanická práce.**

  **Fáze, které se v pilotu osvědčily:**

  1. **Kronika.** Z `decisions.md`, `done.md` a git logu vytáhnout všechna rozhodnutí o zkoumané oblasti **v pořadí vzniku, s datem a s podnětem**. Podnět je nejcennější položka – hledá se ve formulacích „vyplavalo při“, „nález z `/review`“, „ukázalo se, že“. **Git log je samostatný zdroj**, který dokumentace nenese: `git log --reverse -S '<pole>' -- docs/` ukáže, kdy která hodnota vznikla, a zprávy commitů nesou zdůvodnění, které se do dokumentace nedostalo.
  2. **Inventura mechanismů.** Taxativně všechna pole, hodnoty výčtů, invarianty, přechody a guardy dotčené oblasti, **s doslovnými citacemi**. Zvlášť se ptát, **na kterou podmnožinu hodnot se který guard ptá** – to je detektor přetížené osy.
  3. **Zkušební sada situací.** Všechno, co dnešní řešení musí unést, s citací a se jménem mechanismu, který to řeší. Bez ní není proti čemu měřit návrh.
  4. **Návrh alternativy** nad kronikou a shluky.
  5. **Ověření s úkolem vyvrátit.** Jeden ověřovatel na jeden návrh.

  **Tři fáze sběru běžely paralelně jako `reader` agenti a vyplatilo se to** – celkem pět agentů (tři sběrači, dva ověřovatelé). Dva sběrače stačí, když je oblast malá.

  **Co musí být v zadání ověřovatele doslova:** úkol **vyvrátit**, ne posoudit; zákaz odvolávat se na dřívější zamítnutí; povinnost jmenovat **konkrétní guard, invariant nebo text pro zákazníka**, který se rozbije. V pilotu ověřovatelé zabili **dva ze dvou** velkých návrhů a jako vedlejší produkt našli dvě skutečné vady – to je ten skutečný výnos, ne návrh sám.

  **Past, kterou pilot doložil:** sběrač, který čte jen část dokumentů, vrátí neúplný seznam čtenářů hodnoty a **hlavní session z toho udělá závěr**. Stalo se to u hodnoty `UNDOCUMENTED` – sběrač našel jednoho čtenáře a uvedl to s mezí („v obou prohledaných souborech“), hlavní session tu mez zahodila a ohlásila nález, který ověřovatel vzápětí vyvrátil šesti čtenáři. **Do zadání proto patří jmenovitý výčet souborů, které má agent projít**, a do výstupu povinná sekce *Meze posudku*.

  **Výstup skillu:** u každého shluku kronika (co vzniklo kdy a z jakého podnětu), návrh alternativy, verdikt ověřovatele a cena přepisu. **„Nic velkého k přepsání“ je platný výstup** a musí na něj být šablona – jinak se z něj stane stroj na návrhy, které projdou, protože je nikdo nezkusil vyvrátit.

  **Rozsah se zadává jako výchozí bod, ne jako hranice výstupu** (`/consolidate gateway` znamená „vyjdi ze záplat kolem brány“, ne „výsledek se smí týkat jen pole s tím prefixem“).

  **Čeho se skill cíleně chytá – vyjmenoval uživatel 20. 9. 2026 po pilotním běhu.** Nejsou to příklady, ale lovné vzory; skill po nich jde adresně, ne že by na ně náhodou narazil.

  - **Přetížená osa.** Jedno pole, kterým se postupně řešily různé problémy, až odpovídá na víc otázek naráz. **Poznávací znamení je v guardech, ne v poli:** ptá-li se každý guard jen na jinou podmnožinu hodnot a k rozlišení dvou z nich je potřeba druhé pole vedle, jsou to osy dvě nebo tři. Uživatel to popsal takhle: *„postupně jsme tím jedním sloupcem řešili různé problémy, až se přetížil a pak jsme museli dodatečnýma obskurníma podmínkama řešit, co to vlastně říká.“*
  - **Táž situace řešená pokaždé jinak.** Tři podobné případy, tři různé mechanismy – jednou nová hodnota, podruhé nový sloupec, potřetí nový nález. Úkol není vybrat, který z nich je nejlepší, ale **sjednotit filozofii na jeden typ řešení** – a je-li těch případů víc za sebou, hledat, jestli z nich neplyne **jeden vzor, který je vyřeší všechny naráz** místo tří dílčích záplat.
  - **Řetěz lepení.** Oprava, která si vynutí další opravu, která si vynutí další. Skill má **dostopovat na první článek řetězu** a říct, co se mělo rozhodnout tam – ne opravovat poslední článek. Uživatel: *„hele, tohle byla volovina, měli jsme to udělat jinak tak a tak, v důsledku toho bychom nemuseli takhle lepit pět navazujících problémů.“*
  - **Slepá místa, do kterých se model dostává opakovaně.** Nerozhodné a neřešitelné stavy, ze kterých vede ven jedině další podmínka, další sloupec nebo další datum. Opakování je signál, že je špatně osa, ne ten konkrétní stav.

  **„Uživatel to zamítl“ není zeď a skill přes ni smí.** Rozhodnutí vzniklo v tehdejším rozpoložení a kontextu, a ten se vývojem projektu mohl změnit – platí to i pro rozhodnutí, která udělal uživatel sám. **Podmínka je pojmenovat, co se od té doby změnilo**; „udělal bych to jinak“ bez toho je jen jiný názor na tutéž věc. Rozhodl uživatel 20. 9. 2026.

  **Relativizují se řešení, ne zadání.** Otevřít znovu se smí rozhodnutí o tom, **jak se něco udělalo** – hodnota výčtu, sloupec, guard, mechanismus. **Nikdy rozhodnutí o tom, co se má dělat**: zadání projektu, rozsah funkcí, produktová volba, obchodní pravidlo. Ta jsou pro skill vstup, ne materiál. Bez téhle hranice by z něj byl `/oponent` s právem měnit zadání, a to je jiný krok cyklu s jiným vstupem. Vymezil uživatel 20. 9. 2026.

  **Z toho plyne tvrdý zákaz pro ověřovatele:** odvolat se na to, že něco už jednou bylo zamítnuto, **není argument**. Ověřovatel musí doložit, **co konkrétně se rozbije** – jmenovat guard, invariant, text pro zákazníka. Doloženo v pilotním běhu: ověřovatel argumentoval větou „to je přesně ta varianta, kterou §125 zamítlo“, a ten bod se udržel jen proto, že k němu vedle toho vypsal šest skutečných čtenářů. Bez toho zákazu skill jen potvrzuje, že co je rozhodnuté, je rozhodnuté.

  **Ověřuje se dvojmo a obě zkoušky jsou blokující** (formuloval uživatel 20. 9. 2026):

  1. **Pokrývá nové řešení úplně všechno**, na co se v minulosti narazilo a co dnešní řešení pokrývá – i tam, kde to dnešek zvládá nedokonale nebo neúplně?
  2. **Je to opravdu zlepšení** – jednodušší, systematičtější, přímočařejší, bez hacků –, a ne jen přepsání jednoho řešení za jiné stejně nebo obdobně dobré? Druhá zkouška je ta, na kterou se zapomíná: výměna hacku za jiný hack projde první zkouškou hladce.

  **„Nic velkého k přepsání“ je platný výsledek, ne selhání běhu.** Skill musí umět skončit větou *„shluk vypadal jako dluh, ale každá záplata má doložený důvod“* – jinak se z něj stane stroj na návrhy, které projdou proto, že je nikdo nezkusil vyvrátit. V pilotu padly dva ze dvou velkých návrhů a jako vedlejší produkt ověřování vypadly dva skutečné nálezy.

- [ ] **`/cleanup`: agent nevrací nahlas, že nevypořádaná témata jsou nula.** `SKILL.md`, *Časté chyby*, přikazuje rodiči říct nahlas i to, že nic neviselo – ale `agent.md` u agenta totéž nežádá, takže ten prostě sekci vynechá. Rodič pak nepozná, jestli agent nic nenašel, nebo se na to zapomněl podívat, a rozdíl mezi „prověřeno, nic“ a „neprověřeno“ zmizí. **Doloženo druhým ostrým během 25. 9. 2026:** agent vrátil `K rozhodnutí` s jednou položkou a o nevypořádaných tématech nenapsal nic; rodič musel dovodit, že jich je nula. **Oprava patří do `agent.md`** – vyžádat si prázdnou sekci s „žádná“, stejně jako to `out-of-scope.md` žádá u položek mimo rozsah.

- [ ] **`/cleanup`, `out-of-scope.md`: sada voleb nepokrývá položku mířící mimo projekt.** Volby jsou *Vyřešit teď / Zapsat do todo / Zapsat do backlogu / Zahodit*, přičemž todo i backlog znamenají soubory **v uklízeném projektu**. Položka, která míří do konfigurační vrstvy – nové obecné pravidlo do `~/.claude/RULES.md`, poznámka ke skillu do `~/.claude/todo.md` –, do nich ale nepatří: zapsat obecné pravidlo do `docs/todo.md` rezervačního systému by ho schovalo tam, kde ho nikdo nehledá. **Doloženo druhým ostrým během 25. 9. 2026**, kde přesně taková položka přišla a musela se obejít věcnými variantami *(kam a jak zapsat)* místo předepsané sady.

  **Co s tím:** `out-of-scope.md` má u bodu 5 rozlišit **cíl zápisu podle toho, kam položka věcně patří** – fronta projektu, nebo fronta konfigurační vrstvy –, a říct, že u položky mimo projekt se volby skládají věcně, ne z té čtveřice. Souvisí s tím, že agent do konfigurační vrstvy sáhnout nesmí, kdežto rodič ano, takže se ta položka k rodiči dostane vždycky.

- [ ] **`/cleanup`: mezi *Fází 1* a *Fází 2* se nutně čeká a skill to nikde neříká.** *Fáze 2* velí „Pusť čtenáře **hned**, ještě než se začneš ptát: běží na pozadí a jejich latence se schová za celou interaktivní část“ – jenže čtenář pozůstatků dostává diff proti pracovnímu stromu, a ten musí obsahovat **necommitnuté zápisy vytěžovacího agenta**. Ten je zapisuje až na konci svého běhu, takže čtenáře nelze pustit souběžně s ním. „Hned“ tedy znamená „hned po agentovi“, ne „hned po zadání“.

  **Proč to vadí:** skill čte jako plynulý řetěz, ve kterém se nikde nečeká, a přitom je v něm **jedno tvrdé zablokování** – po *Fázi 1* nezbývá než stát, protože *Fáze 2* na agentovi visí a *Fáze 3* visí na výstupu agenta taky. Model, který tomu textu věří, se buď pokusí pustit čtenáře předčasně (a dá jim podklad bez toho, kvůli čemu běží), nebo se zastaví a nechá řízení uživateli s větou „čekám na agenta“ – tedy přesně to, co `~/.claude/RULES.md`, *Co ohlásíš, udělej hned v téže odpovědi*, zakazuje.

  **Co s tím:** říct ve *Fázi 2* výslovně, že čtenáři jdou až nad hotovým výstupem agenta a proč; a doplnit, co se má dělat v době, kdy agent běží – buď že se legitimně čeká, nebo které kroky se dají předsunout. Doloženo druhým ostrým během 25. 9. 2026 v rezervačním systému, kde model to váhání napsal nahlas do odpovědi uživateli.

- [ ] **`/cleanup`, *Fáze 0*: první řádek transcriptu nemusí mít `timestamp`.** Skill říká „přečti si z něj čas prvního záznamu“, což svádí k `head -1`; jenže první řádek bývá meta záznam bez toho pole a vrátí `None`. Bez času pak nejde spočítat základ session, na kterém visí celá *Fáze 2*. **Správně se musí iterovat, dokud se nenajde první řádek s vyplněným `timestamp`.** Doloženo druhým ostrým během 25. 9. 2026 v rezervačním systému; nezaseklo to běh jen proto, že si toho model všiml a opravil se. **Potřetí týž den, ve třetím ostrém běhu nad jinou větví téhož projektu** – `head -1 | .timestamp` vrátil `None`, protože prvním řádkem byl záznam typu `mode`; první vyplněný čas byl až na třetím. Dva ze tří ostrých běhů do té pasti šláply, takže to není ojedinělost. **Patří to do skillu jako věta, ne do zkušenosti** – je to přesně ten druh pasti, který se při dalším běhu zopakuje.

- [ ] **`/cleanup`: zákaz pouštět testy je psaný pro projekt s kódem a v projektu bez něj vyřazuje tu jedinou kontrolu, která na jeho práci sedí.** `SKILL.md`, *Co skill nedělá*, zakazuje spustit testy, lint, typecheck i build – argument je, že to není audit projektu a že kontrola odkazů, kterou pouští agent, stačí. **V rezervačním systému ale `test` žádný kód netestuje**: `tests/test_docs.py` hlídá tvar dokumentace, tedy přesně to, co `/cleanup` vyrábí – odkazy, kotvy, dvojznačné nadpisy, odkazy `§NN` na existující kapitoly, seznam dokumentů v `README.md` a `CLAUDE.md` proti obsahu `docs/` oběma směry. `scripts/links.py` z toho umí zlomek. **Doloženo třetím ostrým během 25. 9. 2026:** vytěžovací agent sáhl na `todo.md` a `gateway.md`, obojí ta sada prověřuje, a sám to vrátil jako mez běhu s doporučením testy před commitem pustit. **Rozhodnout se má kritérium, ne výjimka pro jeden projekt** – nabízí se „pouští se ten krok kontraktu, který prověřuje soubory, do kterých skill zapsal“, což je v projektu s kódem prázdná množina a nic se nespustí.

- [ ] **`/cleanup`: vytěžovací agent nabídl otázku, u které si odpověď mohl sám ověřit v cizím repozitáři.** `agent.md` i `out-of-scope.md` říkají *Nejistotu nejdřív zkus odstranit* – jde-li odpověď dohledat, není to položka k rozhodnutí, ale práce. **Doloženo třetím ostrým během 25. 9. 2026:** agent vrátil jako nevypořádané téma otázku, kdy zapsat GoPay do veřejného seznamu zpracovatelů na `/soukromi`, a nabídl dvě varianty termínu. Ani jedna nebyla správně – **GoPay v tom seznamu už byla**, protože ji provozovatel používá na vlastním webu, takže volbou brány se veřejný dokument nemění vůbec. Cesta k souboru přitom stála v projektovém `CLAUDE.md` (`~/Dev/jantichy`, kapitola *Seznam zpracovatelů osobních údajů*) a agent má `Read`, takže si to mohl přečíst. Uživatel kvůli tomu odpovídal na otázku, která neměla vzniknout, a jeho odpověď se ukázala jako bezpředmětná. **Pravidlo má zjevně slabé místo u cizího repozitáře:** agent si ověřuje stav v projektu, ve kterém stojí, a na dohledání mimo něj nepomyslí – stojí za to to v `agent.md` říct výslovně.

- [ ] **`/cleanup`: čtenář pozůstatků dostává diff jako snímek, který mu interaktivní fáze zastarají pod rukama – a on to nepozná.** `SKILL.md`, *Fáze 2*, pouští čtenáře schválně před ptaním, aby se jejich latence schovala za interaktivní část; *Fáze 6* na to navazuje pokynem ověřit, že nález ještě platí. **To ale řeší jen jednu polovinu.** Druhá je, že se čtenáři do souboru vyrobí diff v čase T a všechno, co se zapíše ve *Fázích 3 až 5*, v něm chybí – takže posuzuje stav, který už neexistuje. **Doloženo třetím ostrým během 25. 9. 2026:** čtenář nahlásil jako nález, že „diff položku přidává, ale v pracovním stromu není“, a sám dodal, že to bez gitu rozhodnout neumí. Měl pravdu v obojím – položku jsem mezitím smazal –, ale **stálo to jeho běh i moje ověřování nálezu, který nikdy neexistoval**. Nabízejí se tři cesty: vyrábět diff až po interaktivních fázích (ztratí se schování latence), předat čtenáři i `git status` jako druhý soubor, nebo mu do zadání napsat, že diff je snímek z času T a rozpor mezi ním a stromem **není nález**. Třetí je nejlevnější a nic nerozbíjí.

### Nedodělky přestavby vývojového workflow (2. 9. 2026)

**Kontext pro novou session.** V jedné dlouhé session se přestavěl celý řetěz vývojové práce v `~/.claude`. Hotové a commitnuté je tohle: přejmenování `/spec` → `/specify` a dokumentů na `requirements.md` + `architecture.md`; `/standards` přepsaný na `/review` s panelem specialistů a adversariálním ověřováním nálezů; nový `/release`; průběžná kontrola jako `Stop` hook (`~/.claude/verify.sh`) čtoucí *Kontrakt příkazů* ze sekce `## Kontrakt příkazů` v projektovém `CLAUDE.md`; nový *Životní cyklus projektu* v `RULES.md`; sekce *Ověřování a kontroly kvality* a *Autonomie se stupňuje* v `coding/coding.md` (od 7. 9. 2026 vlastní soubor `coding/quality.md`). Zdůvodnění všech rozhodnutí včetně zamítnutých variant je v `decisions.md`, sekce `## coding` a `## Celý repozitář` k datu 2. 9. 2026.

Na konci session posoudil nezávislý agent (bez kontextu z implementační fáze) věrnost výsledku původnímu záměru z úvodní diskuze. Jeho závěr: **„Ztratilo se všechno, co stojí peníze nebo vyžaduje infrastrukturu. Přežilo přesně to, co jde napsat jako text.“** Následující položky jsou ta ztráta, seřazená podle naléhavosti. Nejsou to nápady navíc – všechny byly v úvodní diskuzi dohodnuté nebo doporučené a při implementaci vypadly.

**Aktualizace téhož dne odpoledne.** Většina té ztráty se mezitím dohnala – hotové věci se přesunuly do `done.md`, tady zbývá zbytek. Přibylo od té doby: skill **`/attack`** (krok 8 životního cyklu) a jeho první ostrý běh; celá sekce ***Model a effort podle úkolu*** v `RULES.md`; **devět meta-testů** nad skilly zapojených jako `test` v kontraktu `~/.claude`; dvě opravené díry v průběžné kontrole (otisk stavu nezahrnoval obsah souborů; chybějící nástroj se hlásil jako padající kontrola). Proběhly **čtyři průchody `/consistency`** s 37 nálezy – poučení z nich, včetně toho, že **největší riziko nese poslední oprava, ne původní stav**, je v `decisions.md`. Zbylé nápady z průzkumu cizího repozitáře jsou v sekci výš.

**Čím začít z nedodělků workflow** (platí pro položky nad tímhle odstavcem, ne pro skilly níž – ty mají vlastní pořadí). Odvozeno z tohohle seznamu, ne z priority: **vyzkoušet `/code-review ultra` a `/security-review`**. Je to jediná položka, kterou ostatní výslovně označují za svou podmínku – zbývající úpravy panelu `/review` (hloubka delegace, docs-researcher) mění právě to, co se ještě nikdy nespustilo. **Musí to udělat uživatel**: vestavěné skilly si Claude nevyvolá sám.

- [ ] **Zvážit, jestli hlídání testů nemá dělat hook místo instrukce.** Rešerše označila detekci editace testovacích souborů za **„nejlevnější a nejúčinnější opatření“** proti tomu, aby model dosáhl průběžné kontroly oslabením testů. Dnes je to pravidlo v `coding/quality.md` a v `/implement` – vynucuje ho tedy tentýž model, který má motivaci ho porušit. `verify.sh` má přitom `git status` po ruce a diff testovacích cest by uměl triviálně vyhodnotit. Je to přesně argument „instrukce je doporučení, skript je záruka“ obrácený proti vlastnímu řešení.

- [ ] **Založit typ subagenta bez nástroje na spouštění agentů.** Pravidlo *Hloubka delegace je jedna* je od 14. 9. 2026 v `~/.claude/RULES.md`, ale **mechanismus k němu chybí**: skilly typ subagenta neurčují, takže dostane výchozí s plnou sadou. Vynutit by to šlo jen typem agenta, který ten nástroj nemá – dostupné (`Explore`, `Plan`) jsou ale řezané na čtení, ne na posuzování, takže by se jimi rozbil panel specialistů `/review` a `/audit`. Potřeba je tedy **vlastní typ**: plné čtecí schopnosti, žádné spouštění agentů. Teprve pak má smysl ho do skillů doplnit.

- [ ] **Zvážit specialistu „docs researcher“ v panelu `/review`.** ⛔ **Nejdřív *Vyzkoušet `/code-review ultra`* níž** – tahle položka rozšiřuje panel, jehož základ se ještě neporovnal s hotovým nástrojem. Rešerše ji našla u týmu, který multi-agentní review provozuje, a při čtení byla vyhodnocená jako nejcennější nález: agent, který **ověřuje předpoklady o cizích API proti dokumentaci**. U workflow postaveného na Vercelu, GA4, GTM a BigQuery je to citelná díra – a v téže session se stalo přesně to, čemu ta role brání: chybné tvrzení o chování `vercel promote`, které se muselo opravovat až po ověření v dokumentaci.

- [ ] **Held-out testy a LLM judge nad diffem.** Dvě ze tří obran proti reward hackingu, které rešerše nabídla. Implementovaná je jen první (hlídání diffu testů). Zbylé dvě nejsou ani zavedené, ani zamítnuté.

- [ ] **Canary a staging v `/release`.** Původní návrh obsahoval „canary deployment s automatickým rollbackem na degradaci metrik“ a mezi tvrdými pravidly „staging první, pokud existuje“. Ani jedno v skillu není a nikde nestojí proč. Slovo „staging“ se v `~/.claude` nevyskytuje vůbec.

- [ ] **Vyzkoušet `/code-review ultra` a `/security-review`, než se dál rozvíjí vlastní panel.** V diskuzi to padlo dvakrát, podruhé jako výslovná podmínka druhého sezení („*nejdřív vyzkoušet, ať víš, co už máš, a stavíš jen ten zbytek*“). Vlastní panel se napsal a dvakrát pustil, aniž se to kdy zkusilo. `/review` je přitom na obou postavený – volá je jako dvě ze svých rolí.

- [ ] **Pustit `/release` poprvé a ověřit ho.** Skill je napsaný, dvakrát reviewovaný, ale **nikdy neběžel** – ani nanečisto. `decisions.md` to konstatuje, ale jako úkol to nikde nebylo. Do té doby platí, že první ostrý běh bude zároveň první test, a to na produkci. Nejlevnější způsob, jak to zlomit: pustit ho na `jantichy/main` u nejbližší drobné změny a projít ho celý včetně zastavení na potvrzení.

- [ ] **Přeuspořádat nedodělky podle závislostí, ne jen podle naléhavosti.** Čtenář bez kontextu upozornil, že položka *Vyzkoušet `/code-review ultra` a `/security-review`* leží až na konci seznamu, přestože položky nad ní mění právě ten panel, jehož základ se ještě nevyzkoušel.

  **Stav k 14. 9. 2026: ze čtyř blokovaných položek zbývá jedna.** Odpracovaný je model ověřovatelů, dávkování panelu i hloubka delegace; blokovaný zůstává jen *docs researcher*, který je nově označený ⛔ i s odkazem na to, co má předcházet. **Vyřešilo se to značkou, ne přeskládáním** – pořadí v souboru nese historii zápisu a přeházet 35 položek kvůli jedné závislosti je víc rizika (ztracený kus textu) než užitku. Zbývá rozhodnout, jestli tenhle tvar stačí, nebo má sekce dostat explicitní pásma.

### Oponentura workflow, testování a bezpečnostního auditu (2. 9. 2026)

**Hotovo: 70 z 75 nálezů.** Nad *Životním cyklem projektu*, `/review`, `/attack`, `coding.md`, `verify.sh` a meta-testy proběhl `/oponent` s pěti nezávislými hlediska; 84 syrových nálezů, po konsolidaci 75, deset témat. Devět témat je vypořádaných celé, desáté z většiny. Commity v `~/.claude`: `9e37297`, `062005f`, `ff0f765`, `d9670e9`, `69a843d`, `07de758`, `6c2ebf8`, `4169bda`, `f009fab`, `2d88a72`, `3e7a010`, `d1e9447`; v `~/Dev/context`: `8854da1`, `5a6b710`, `dad94d1`, `60beedd`, `c230a4e`, `f75e83f`, `5a2c533`.

**Co z toho vzešlo.** Soustava měřila **tvar, ne skutečnost** – z `/review` šlo smazat celou fázi ověřování nálezů a `test` zůstal zelený. A kontroly byly nejslabší **přesně tam, kde se pracuje**: souhlas průběžné kontroly klíčovaný cestou byl vypnutý na každé feature větvi, otisk stavu neviděl do nových adresářů. Obojí je opravené a ověřené spuštěním. Průběžná kontrola má nově 18 regresních testů a nové meta-testy během práce **třikrát samy našly pravý nález**.

Zbývá pět nálezů. Všechny jsou vědomě odložené, ne přehlédnuté – u každého je zapsané, co se navrhovalo.

- [ ] **Mechanická oprava se dělá bez ptaní a rovnou pushne** (nález 14). Kosmetický nález od standardového specialisty na nejlevnější konfiguraci panelu se neověřuje, opraví se bez dotazu a autocommit ho commitne a pushne. Je to jediná cesta ke změně kódu bez ověřovatele i bez uživatele. Navrženo: seznam mechanických nálezů předložit **jednou otázkou před opravou** (cena je jedna otázka za běh) a zavřít neomezenou kategorii „formulační a formátovací drobnosti“ tím, že kosmetický nález měnící **text nebo obsah** patří mezi sporné. Odloženo 2. 9. 2026.

- [ ] **Z fronty rozhodnutí se nedá vystoupit** (19). Průchod nálezy nemá hromadnou akci ani přerušení; čtyři volby `AskUserQuestion` jsou vyčerpané. Kdo v patnácté minutě usoudí, že zbytek chce jindy, musí odpovědět čtrnáctkrát. Navrženo: `Rozbalit` do samostatné otázky (klade se stejně jen u `batch` nálezů) a uvolněné místo dát volbě `Zbytek odložit`; k tomu rozhodovací bod ve Fázi 5 s odhadem času, ne jen počtem. Odloženo 2. 9. 2026.

- [ ] **Odložené nálezy se při dalším běhu vracejí** (44). Filtr existuje jen pro `Přeskočit`. Sedm nálezů odložených do `todo.md` se při druhém běhu znovu zaplatí panelem i ověřovateli a znovu předloží. Navrženo: Fáze 0 načte `todo.md`, Fáze 4 spojí nález s odloženou položkou podle lokace a zařadí ho jako „už odloženo, jen připomenuto“ mimo interaktivní průchod. Odloženo 2. 9. 2026.

- [ ] **Nic nehlídá projekt, na kterém se nepracuje** (51). Každá kontrola se pouští z `/review` nebo `/release`, tedy prací. Repozitář, do kterého půl roku nikdo nesáhl, se nedozví o CVE, prošlém certifikátu ani expirovaném tokenu – a tahle třída chyb nemá se změnou kódu nic společného, takže ji nemůže chytit žádná ze tří záruk. Doloženo prošlým certifikátem na jedné ze spravovaných domén. Položka „rozhodnout o CI“ výš to nepokrývá: CI běží na push, ne v kalendáři. Varianty: naplánovaný cloudový běh, GitHub Actions `schedule`. Odloženo 2. 9. 2026.

  **Třetí varianta je od 15. 9. 2026 hotová a rozsah položky se tím zúžil.** Dependabot běží v `~/.claude` a ve čtyřech dalších projektech a `/project` ho nově zakládá s CI (`skills/project/checks.md`, *Dependabot: podmínka toho, aby připínání dávalo smysl*), takže **zastarávání závislostí a akcí pokryté je** – a to i v repozitáři, do kterého nikdo nesáhne, protože Dependabot běží na straně GitHubu v kalendáři, ne na push.

  **Zbývá to, co Dependabot neumí:** expirované certifikáty a tokeny, CVE v projektu bez manifestu závislostí a repozitáře **bez remote**, kde nemá kam pull request otevřít. Na tohle pořád platí obě zbylé varianty.

- [ ] **Zjistit, čím se liší slovník, který whisperu pomůže, od toho, který ne.** Proměřeno 4. 9. 2026 sedmi běhy nad touž nahrávkou: **délka vliv nemá** (8/12/16/21 položek dalo 5/5, 4/5, 4/5, 5/5), takže původní domněnka o klesající účinnosti k pozdějším položkám padla i se stropem deseti položek. Efekt je přitom reálný a reprodukovatelný – jeden jednadvacetipoložkový seznam dá 5/5, jiný 0/5 nad tímtéž zvukem. Vyloučeno je pořadí (přesun jména z páté pozice na třetí: 1/5) i psaní velkých písmen (0/5). Zbývá složení seznamu jako celek. Další krok: měnit po jedné položce mezi seznamem, který funguje, a tím, který ne – liší se mimo jiné přítomností slova „démoni“ a tvarem „frekventant/frekventanti“. Výsledná tabulka je v `SKILL.md`, *Technické detaily*. **Syrová data (60MB WAV a sedm přepisů) ležela v `~/.claude/jobs/`, což je scratch mazaný s jobem** – kdo bude pokračovat, ať počítá s tím, že tam nejsou, a vyrobí si je znovu podle tabulky: tytéž slovníky nad toutéž nahrávkou.

  **Doplněno 6. 9. 2026 dalšími jedenácti běhy nad touž nahrávkou** – hledání příčiny se tím posunulo, ale neuzavřelo:

  - **Účinnost slovníku převrátí i parametr, který se slovníkem vůbec nesouvisí.** `-vsd 500` (delší minimální ticho ve VAD) srazil sledovanou zkratku ze 7 správných tvarů na 2, přestože na prompt nijak nesahá – posouvá jen hranice úseků, na které se zvuk před dekódováním krájí. Hypotéza k ověření: nejde o „složení seznamu“ jako vlastnost slovníku, ale o **interakci slovníku s tím, kde přesně padne hranice okna** vůči výskytu daného slova. To by vysvětlilo i to, proč dva stejně dlouhé seznamy dávají 5/5 a 0/5.
  - **Měřit je levnější, než se zdálo:** whisper je při stejném vstupu **bit-identický** (shodný otisk textu i SRT), takže každý rozdíl ve výstupu je signál a tutéž konfiguraci nemá cenu pouštět dvakrát. Neplatí to o čase – dva bitově shodné běhy trvaly 291 s a 504 s podle zahřátí stroje.
  - Syrová data z těchhle běhů **taky nezůstala** (scratchpad session). Reprodukce je ale díky determinismu spolehlivá: tytéž přepínače nad toutéž nahrávkou dají tentýž výstup.

- [ ] **Rozsekat dvojjazyčnou nahrávku automaticky.** Od 4. 9. 2026 `/transcript` dvojjazyčnou nahrávku **pozná** – `detect-lang.sh` vzorkuje na třech místech a při neshodě vrátí `mixed:cs,en` –, a `SKILL.md` nabízí dvě cesty: přepsat po částech ručně určeným zlomem, nebo přepsat celé v převažujícím jazyce a označit nespolehlivou pasáž. Co chybí, je **najít zlom sám**: dnes ho musí uživatel říct. Šlo by hledat vzorkováním po minutě a hranici klást tam, kde se jazyk mění. Krátkou vsuvku v jiném jazyce tři vzorky minou a minuly by ji i tak.

**Čím začít u skillů (platí pro následujících šest položek).** Pořadí není libovolné:

1. **Ověřit `skill-creator`** na jednom malém skillu (`/compose`). Může delegaci zúžit nebo zrušit, a tím změnit vnitřek, se kterým se migruje – dělat migraci před ním znamená riskovat patnáct běhů se špatným nástrojem.
2. **Migrovat po jednom skillu, commit na skill.** Vyškrtává se po celých skillech: jméno z `MIGRACE` zmizí, až jsou opravené **všechny** vady toho skillu, takže dávka po jednom je zároveň jediná, u které testy zůstanou zelené průběžně.
3. **`/review` (591 řádků) a `/project` (535) nechat naposled.** U obou je potřeba rozhodnout hranici řezu do vedlejších souborů, a to je samostatná úvaha, ne mechanická oprava. **Návrh k potvrzení nebo překopání:** z `/review` vytáhnout zadání jednotlivých specialistů panelu (jsou to samostatné bloky, které hlavní běh jen předává dál), z `/project` kroky 4–9 průvodce (nastavování, kterým se prochází jednou a pak nikdy). Oba kandidáti sedí na kategorie, které norma jmenuje – zadání pro agenty a dlouhé postupy –, ale **neprověřil je nikdo**, takže se s nimi nakládá jako s výchozím bodem, ne jako s rozhodnutím.
4. Zbytek (globální `/replace`, nové skilly) je na migraci nezávislý a může jít kdykoliv.

- [ ] **Globální režim `/replace`.** Dnes skill vědomě nesahá mimo projekt, jenže `~/.claude`, `~/Dev/context/` a projekty jsou jedna provázaná soustava – přejmenování doménového souboru nebo skillu se musí propsat do všech. **Úkol, který ho měl vyvolat, mezitím odpadl:** přesun `structure/` a `worktree/` proběhl 7. 9. 2026 ručně, a ne do `context/claude/`, ale ven do veřejného `~/.claude` (viz `decisions.md`, *Skill si nese své věci s sebou*). Průchod byl přes 40 souborů ve třech repozitářích a **ukázal, co takový režim potřebuje**: explicitní seznam prohledávaných kořenů, soupis dotčených repozitářů ke schválení, kontrolní průchod na starý tvar napříč všemi – a hlavně **ověření řádek po řádku, že v diffu není cizí rozdělaná práce**, protože jinak hromadný commit sebere něco, co mu nepatří.

- [ ] **Doplnit evaluace u stávajících skillů.** Rozhodnuto 4. 9. 2026, že se dohánějí i zpětně při revizi, ne jen u nových. Je to patnáct běhů agentů navíc, takže se to dělá po jednom spolu s migrací, ne dávkou. Nástroje: `skill-creator` na spolehlivost vyvolání (`improve_description.py`), `superpowers:writing-skills` na tlakové scénáře u skillů, které něco zakazují nebo vynucují – `/implement` (nesahat na testy), `/release` (potvrzení), `/attack` (nikdy proti produkci) a `/invoicing` (nikdy neodeslat mail, ani na výslovný pokyn) – **`/invoicing` má tlakové scénáře hotové od 11. 9. 2026** – podrobnosti u jeho položky níž. **Od 20. 9. 2026 do seznamu patří i `/architect` a `/specify`**: první vznikl rozdělením a nikdy neběžel, druhý se přepsal na jeden běh bez režimů. Oba nesou *Zákaz implementace*, tedy vynucující pravidlo, u kterého `/skill` žádá tlakové scénáře povinně – a ani jeden je nemá. **`/learn` má měření hotové** (10. 9. 2026): vyvolání 12/12 a **tlakové scénáře proběhly** – šest běhů proti falešné znalostní bázi, dva z nich bez skillu jako kontrola. Zákaz sahat na doklady po opravě drží (kolize s živým pokynem má nově předepsaný postup); zákaz psát před odsouhlasením plánu **neustál ani jeden běh** a rozhodnutím uživatele zůstává jako přiznané doporučení bez mechanismu, ne jako záruka – zdůvodnění i naměřené výmluvy jsou ve *Fázi 5* skillu. **Spolu s nimi pustit i `/review`**, který nad ním taky neběžel: vznikly tři soubory a zásahy do čtyř dalších ve dvou repozitářích. Tvar hlídají testy a osmnáct nálezů odhalil čtenář bez kontextu při `/cleanup`, takže nezkontrolovaná ta práce není – chybí jen panel specialistů, zejména textové standardy nad českým textem skillu.

- [ ] **Ověřit, jestli `skill-creator` opravdu stojí za zapojení – a proměřit jím `/skill` samotný.** `/skill` na něj deleguje měření, ale nikdy neběžel. **Prvním měřeným skillem má být `/skill` sám:** jeho vlastní *Fáze 6* dělá ladění `description` povinným u skillu, jehož jméno se překrývá s cizím, a tenhle se překrývá hned se dvěma (`skill-creator`, `superpowers:writing-skills`). Jedním během se tak otestuje nástroj i uzavře vlastní dluh – dnes `/skill` předepisuje ostatním měření, kterému se sám nepodrobil. Nejlevnější první běh: pustit ho na ověřovatele v `/compose`. Ukáže zároveň dvě věci – jestli jeho skripty v tomhle prostředí vůbec projdou (kontrola závislostí je ve `Fázi 0`), a jestli si při vyvolání neprosadí vlastní tvar navzdory normě. Vyjde-li druhé špatně, je to důvod delegaci zúžit nebo zrušit.

  **Změřeno 6. 9. 2026 na `/compose`, a dopadlo to špatně.** Skripty projdou, ale `run_eval.py` registruje popis jako dočasný slash command a hledá volání toho dočasného jména – u skillu, který je nainstalovaný, model zavolá ten skutečný, takže se **správné vyvolání počítá jako propadlé** (8/16, všech osm propadů byly pozitivní případy; kontrolní běh s maximálně dotěrným popisem dal 0/2). Náhradní postup, který funguje – měřit přes `claude -p --output-format stream-json` –, je zapsaný v `~/.claude/skills/skill/SKILL.md`, *Fáze 6*. Zbývá rozhodnout, jestli má na `skill-creator` delegaci vůbec smysl držet: vytěžení z konverzace a evaluace výstupu se pořád neprověřily.

- [ ] **Zbylá vylepšení skillů z revize 4. 9. 2026.** Nad rámec migrace na normu: **ověřovatel bez kontextu session pro `/compose`** (dnes si napsaný text kontroluje tentýž kontext, který ho napsal; od 6. 9. 2026 má ověření jen větev `profile` – slepý test, jehož ověřovatelem je Honza, ne model) a k tomu zpětná smyčka do `archive/` a `style.md`; **druhý běh `/report` nad novými daty** (skill sám tvrdí, že se report přegeneruje, ale fázi pro to nemá).

- [ ] **Skill `/research` – podložená rešerše s citacemi.** `~/.claude/RULES.md` má pravidlo *Neopírej rozhodnutí o neověřené tvrzení*, ale žádný proces, který by ho vynutil – a pravidlo bez nástroje se dodržuje hůř. Uplatnění: podklady pro texty, přípravu školení a argumentaci vůči klientovi.

  **Riziko, které se má rozhodnout dřív, než se skill napíše:** může to být spíš fáze uvnitř `/compose` než samostatný skill. Krok, který nikdo nezavolá, je horší než žádný. Až na to dojde, začni tou otázkou.

- [ ] **Rozhodnout, jestli `/discovery` potřebuje ověřovací vrstvu** – po prvním ostrém běhu, ne dřív.

  `~/.claude/skills/SKILLS.md`, *Ověřovací vrstva*, žádá ověřovatele u každého skillu, který pouští panel agentů hledajících problémy: *„Není to volba. Agent požádaný o hledání mezer nějakou najde vždycky, a po třetím falešném nálezu se skill přestane pouštět.“*

  **Proč se `/discovery` dnes zapsal bez něj:** jeho agenti nehledají problémy, ale fakta o cizích produktech, a jeho fáze *Ověření* kontroluje **doložení** – nález bez funkční URL nebo s nízkou jistotou u ceny se zahodí. To je jiná otázka než „je ten závěr pravdivý?“. Ověřovatel podle normy by se hodil spíš na fáze *Pozice a odlišení* a *Registr rizik*, kde z dat vzniká tvrzení – tam by měl zkusit vyvrátit, že se konkurence tou funkcí opravdu liší.

  **Přestavba z 21. 9. 2026 ten argument zčásti obrátila.** Platí dál pro konkurenční rešerši, ale **ne pro novou Fázi 2**: ta pouští panel na cesty bloku *Poptávka* – povinně na *Hlas problému*, podle volby na *Ochotu platit* a *Objem a jazyk hledání* – a ústí do **verdiktu o poptávce**, tedy do tvrzení, které se dá vyvrátit – a to je přesně vstup, pro který norma ověřovatele žádá. Rozhoduje se tedy nově o dvou věcech zvlášť, ne o jedné: ověřovatel nad verdiktem je silnější kandidát než ověřovatel nad pozicí a riziky.

  **Proč se to nerozhodlo hned:** není odzkoušené, jestli rešerše doopravdy vymýšlí. Doplnit panel naslepo znamená zaplatit ho při každém běhu; nedoplnit ho znamená riskovat, že se produkt postaví na odlišení, které konkurence dávno má. Rozhodne se po prvním běhu na skutečném projektu – tehdy bude vidět, kolik nálezů neprošlo fází *Ověření* a jestli mezi zapsanými zůstalo něco nepravdivého.

  **Zapsáno 2026-09-07**, protože mezera dnes není nikde přiznaná a `~/.claude/RULES.md`, *Zapiš i to, co vědomě nemáš*, to žádá.

- [ ] **Roztřídit `todo.md` v šesti projektech, kde nápady zůstaly promíchané s frontou.** 7. 9. 2026 dostal standard `backlog.md` (`~/.claude/STRUCTURE.md`, *`backlog.md`*) a soubor se založil v osmi projektech s `todo.md`. Vlastní položky se ale přesunuly jen ve dvou. Zbývá šest a nejvíc nálezů bude v tom největším – má přes dva tisíce řádků a jeho `CLAUDE.md` do té doby popisoval `todo.md` výslovně jako „sbírku nápadů k projednání a backlog na později“.

  **Nedělá se to hromadně, ale průchodem `/project update` v každém projektu zvlášť** – krok 14 to má jako vlastní bod a ptá se po jedné položce s kontextem toho projektu. Hromadné třídění zvenčí by rozhodovalo o obsahu cizích seznamů bez toho kontextu.

  **Kritérium je rozhodnutí, ne termín:** „až po MVP“ je plán a zůstává ve frontě; „někdy by šlo“ je nápad a jde do backlogu.

- [ ] **Revize měření nemá vlastníka procesu.** Audit je od 10. 9. 2026 pokrytý skillem `/audit` a znalostní podklady pro revizi drží `~/Dev/context/analytics/` – co chybí, je **kdo a jak z těch podkladů vyrobí výslednou dokumentaci**, tedy analytickou a implementační specifikaci, která je u revize hlavním výstupem. Rozdíl proti auditu drží `analytics/audit.md`, *Audit není revize*: výstupem auditu je dokument, výstupem revize naimplementovaný web.

  **Historie, ať se nezačíná od nuly:** původním záměrem projektu `~/Dev/gtm` bylo přesně tohle – proces, na jehož konci z podkladů vypadne ucelená dokumentace. Znalosti se z něj postupně vyzobaly do `context/analytics/`, projekt zůstal ořezaný a nedodělaný a proces zůstal nepokrytý.

  **Nejspíš to není jednorázový skill.** Revize je dlouhá zakázka s víc fázemi, ne jeden běh – blíž to má k životnímu cyklu než k `/auditu`. Než se začne psát, je potřeba rozhodnout, jestli je to skill, sada skillů, nebo návod pro člověka.

- [ ] **Ověřit kola návrhu v `/architect` prvním ostrým během.** Kola vznikla 16. 9. 2026 ve `/specify` a 20. 9. 2026 se rozdělením přestěhovala do `/architect`, kde už nemají pojmenované režimy – orientace je ze stavu projektu. Jsou ověřená jen čtením – třemi čtenáři bez kontextu, ne během. Po prvním skutečném návrhu po kolech vyhodnotit: jestli blok kola v `todo.md` stačí čisté session bez doptávání; jestli se orientace ze stavu projektu trefila ve všech případech, zejména ve větvi kola po zápisu a ve druhém běhu sešití; jestli dva běhy kola a dva běhy sešití nejsou zbytečná režie; a jestli přidělování čísla kapitoly před sloučením souběžných kol opravdu zabrání kolizi. Co se pokazí, patří do sekce *Časté chyby* ve `skills/architect/SKILL.md`.

- [ ] **Pořadí v `## Průchody životním cyklem` se rozpadá opakovaně a nic ho nehlídá.** `~/.claude/STRUCTURE.md` (*`done.md`*, *`decisions.md`*) žádá nejstarší nahoře a nové na konec. **Poprvé se sekce rozešla do 6. 9. 2026** – tehdejší `/cleanup` to našel a srovnal vzestupně, jak stojí v jeho vlastním záznamu v téhle sekci. **Podruhé se rozešla během deseti dnů:** 19. 9. 2026 stálo prvních 11 záznamů (19. 9. → 15. 9.) sestupně nad 40 vzestupnými. Znovu srovnáno.

  **Oprava bez mechanismu evidentně nevydrží.** Zapisují tam skilly samy a každý z nich se řídí tím, co v souboru zrovna vidí – takže jeden obrácený zápis stačí, aby ho další napodobily. Soubor leží v `~/Dev/context`, a **ten nemá `tests/`, CI ani *Kontrakt příkazů*** – takže tam kontrolu není kam pověsit a nejlevnější vrstva by se musela celá založit. **Jenže táž sekce je i v `~/.claude/done.md`, kde testy i CI jsou** – a právě tam se pořadí 25. 9. 2026 rozpadlo **potřetí** (tři nejnovější záznamy stály nad vzestupnou řadou; našel to čtenář bez kontextu, srovnáno). Tam tedy vrstva chybí jen proto, že ji nikdo nenapsal. **Test je navíc jednodušší, než se zdálo:** sekce má vnořené poznámky pod záznamy, takže se nesmí řadit po řádcích, ale po blocích – kontrolovat stačí, že datumy záznamů jdou vzestupně.

  **Rozhodnout je potřeba dvojí:** jestli sem kontrolní vrstvu zavést (a co všechno by ještě mohla hlídat – struktura sekcí, formát datovaných záznamů, odkazy mezi doménami), nebo jestli místo toho normu obrátit na „nejnovější nahoře“, když se v praxi prosazuje sama. Druhá varianta je levnější, ale platí i pro `decisions.md`, kde pravidlo *Nejstarší nahoře* má vlastní zdůvodnění.

- [ ] **Sjednotit podobu otázky napříč kontrolními skilly.** Zadal uživatel 20. 9. 2026 hned po předchozí položce. `FINDINGS.md` od té chvíle říká, **kdy** se ptát a že volby jsou varianty řešení – ale **jak ta otázka vypadá**, si každý skill drží po svém, a jsou to dvě slovní zásoby pro totéž:

  | Skill | Záchytné volby | Na jaký stav se mapují |
  |---|---|---|
  | `/oponent` | *Nechat být*, *Vrátit se k tomu později* | Zamítnuto, Odloženo |
  | `/review`, `/consistency`, `/attack` | *Přeskočit*, *Odložit* | won't fix, `todo.md` |
  | `/cleanup` | vlastní sada v `out-of-scope.md` | – |

  **Je to porušení *Jeden termín pro jednu věc*** (`~/.claude/RULES.md`) o úroveň výš: uživatel vidí v jednom životním cyklu dvě jména pro tutéž volbu a musí hádat, jestli znamenají totéž. `/oponent` navíc jako jediný má tabulku *Která volba znamená který stav*, bez které se odpověď nedá zpracovat – ostatní ji nemají a mapování je implicitní.

  **Co rozhodnout:** jedno pojmenování obou záchytných voleb a jejich pořadí; jestli mapování na stav patří do `FINDINGS.md` (a tedy jednou), nebo je doménové; co se stane, když variant je víc než dvě (dnes to řeší `/oponent` i `/review` každý jinou větou); a jestli `header` má napříč skilly týž tvar (`Nález N/celkem`).

  **Dotčené soubory:** `~/.claude/skills/FINDINGS.md`, `skills/review/SKILL.md`, `skills/consistency/SKILL.md`, `skills/attack/SKILL.md`, `skills/oponent/SKILL.md`, `skills/audit/SKILL.md`, `skills/cleanup/out-of-scope.md`.

  **Dnešní práce (21. 9. 2026) se téhle položky nedotkla, i když sahala na tytéž soubory.** Zúžilo se *kdy* se ptát – padla podmínka „netroufáš si“ a zákaz falešné trojice začal mířit na tvar voleb místo na jejich znění. **Jak ta otázka vypadá, zůstává nesjednocené**, a tabulka výš platí beze změny. Nové zjištění k ní: `cleanup/out-of-scope.md` má vlastní sadu voleb (*Vyřešit teď / Zapsat do todo / Zapsat do backlogu / Zahodit*), která je od dneška legitimní **jen u položky, která není vadou, ale novou prací** – při sjednocování se s tím musí počítat, protože záchytné volby se tam mapují na jiné stavy než u nálezu.

- [ ] **Doměřit u `/merge`, co zůstalo nezkontrolované.** Skill vznikl 21. 9. 2026 a jeho zakládací běh **nedokončil ověřovací fázi `/skill`**: změřil se jen tvar (testy prošly), ale neproběhlo **ladění `description`** ani **tlakové scénáře**. Obojí je u něj na místě – „merge“ je běžné slovo, takže hrozí, že se skill chytne i na `git pull` nebo na merge uprostřed rozdělané práce; a skill něco **zakazuje** (nemergovat bez pokynu, neobcházet selhání `--force`, nemazat větev souběžně s mergem), což je přesně ta třída, u které `/skill` tlakové scénáře označuje za povinné, protože tvarem se omezení ověřit nedá.

  **Srovnávací běh se pustil, ale je neplatný** – agent si mezitím `/merge` načetl a jel podle něj, takže neměřil, jak se selhává bez skillu. Doložil jen to, že se skill dodrží, když je k dispozici; pro sekci *Časté chyby* tedy nepřinesl nic a ta dnes stojí na doložených selháních z provozu, ne na měření.

  **První ostrý běh proběhl týž den** na dvou větvích téhle konfigurace a prošel – včetně větve, kde se `main` mezitím posunul a musel se natáhnout s konfliktem. Vedl se **ručně podle `SKILL.md`**, protože registr skillů se načítá při startu session a nově založený skill v ní zavolat nejde. Co se pokazí v dalších bězích, patří do sekce *Časté chyby* v `skills/merge/SKILL.md`.

- [ ] **Umět převzít existující projekt, který v téhle soustavě nevznikl.** Zadal uživatel 21. 9. 2026. **Vstupní bod existuje, chybí až to, co má vyrobit.** `/project` má režim `adopt` právě pro adresář, ve kterém už něco je a otisk `/project` v něm chybí, a většina jeho kroků má pro ten režim vlastní větev – tohle zadání ho tedy **nemá zakládat znovu**. Co `adopt` neumí, je **zpětná rekonstrukce obsahu z kódu**: nikdo z cizího repozitáře nevyrobí `requirements.md`, `architecture.md`, glosář ani *Kontrakt příkazů*. A přitom je to vstupní bod pro každého, kdo si soustavu chce vyzkoušet – na zelené louce nezakládá projekt nikdo. **Týká se to i vlastních projektů:** osm z dvanácti nemá `CLAUDE.md` vůbec (viz položka o dorovnání kontraktů), takže pro ně dnes platí totéž.

  **Co rozhodnout:** jestli rekonstrukce patří **do stávajícího režimu `adopt`**, nebo je to samostatný krok – hranice je v tom, že `/project` se ptá uživatele, kdežto tady se odpovědi **čtou z kódu** a uživatel je jen potvrzuje; co se z repozitáře odvodit dá (kontrakt ze skriptů v manifestu, glosář z jmen entit, architektura z adresářové struktury a závislostí) a co se odvozovat **nesmí**, protože by vznikla dokumentace, která vypadá doloženě a není – `requirements.md` z kódu odvodit nejde, protože kód nenese *proč*; a jak se výsledek označí za rekonstrukci, aby ho příští běh nebral za rozhodnutí, která se doopravdy udělala.

- [ ] **Rozhodnout, co ze soustavy platí pro druhého člověka a pro tým.** Zadal uživatel 2026-09-21. `skills/LIFECYCLE.md`, *Jeden člověk, jedna interaktivní session*, dnes popisuje omezení jako vědomé: skoro celý cyklus stojí na `AskUserQuestion` a na souhlasu průběžné kontroly vydaném lokálně pro jeden `$HOME`, takže v CI a u spolupracovníka platí **jen deterministická vrstva**. Je to poctivě zapsané, ale jen jako konstatování – **není rozhodnuté, jestli to tak má zůstat**, a přitom z toho plyne, co se stane, až konfiguraci vezme někdo další.

  **Co rozhodnout:** jestli je jednouživatelskost trvalá vlastnost (pak to patří do `README.md` do prvního odstavce, ne do poznámky, aby si to nikdo nenainstaloval a nenarazil), nebo mez k odstranění; co konkrétně by musela soustava umět, aby ji použil druhý člověk – souhlas průběžné kontroly vázaný na repozitář místo na `$HOME`, průchod nálezy bez toho, kdo odpovídá, sdílený `done.md` bez kolizí; a co z toho je vůbec žádoucí, protože panel specialistů bez člověka u obrazovky je jen dražší CI.

- [ ] **Rozhodnout jazyk konfigurační vrstvy dřív než cokoliv dalšího.** Zadal uživatel 2026-09-21. Celá vrstva je česky – pravidla, skilly, README i `description` hlaviček –, a zatím to nikdo nerozhodl, jen to tak vzniklo. Pro české publikum je to výhoda, pro kohokoliv dalšího blokující: skill se nevyvolá popisem, kterému uživatel nerozumí, a README skillu je psané pro člověka zvenčí, tedy přesně pro toho, koho čeština vyřadí.

  **Proč to má přijít brzy:** rozhodnutí předchází všemu ostatnímu, co by se dělalo **pro publikum venku** – jménu, tvaru README i tomu, jestli má smysl řešit předchozí položku o druhém člověku. **Nepřebíjí to rozdělanou práci** na dorovnání cyklu ani pořadí v ostatních sekcích tohohle souboru; je to přednost uvnitř jedné větve práce, ne fronty jako celku. **Varianty jsou tři a každá něco stojí:** zůstat česky (nejlevnější, ale vymezuje publikum), přeložit vrstvu pro cizí čtenáře celou (drahé a dvě jazykové verze se rozejdou – přesně to, čemu brání *Single source of truth*), nebo hybrid, kde je česky práce a anglicky jen to, co čte cizí člověk, tedy README skillů a `description`. U hybridu je potřeba zvlášť rozhodnout `description`, protože podle ní se skill vyvolává a míchání jazyků v ní ovlivňuje spolehlivost vyvolání – to by se mělo změřit, ne odhadnout.

- [ ] **Doměřit u `/discovery`, co po přestavbě zůstalo nezkontrolované.** Přestavba z 21. 9. 2026 (`demand.md`, nová Fáze 2, nový blok cest) proběhla **ruční editací, ne přes `/skill update`**, takže se změřil jen tvar: testy, lint, typecheck a kontrola odkazů prošly. **Neproběhlo ladění `description`** ani **tlakové scénáře**, přestože hlavička se dnes přepisovala celá a skill nově něco **zakazuje** – „mlčky přes nedoloženou poptávku neprocházej“ a „nerozhoduj, jestli se to postaví“. To je přesně ta třída, u které jsou tlakové scénáře povinné, protože tvarem se omezení ověřit nedá.

  **Zvlášť stojí za změření vyvolání:** popis nově slibuje odpověď na „proč se to má stavět“ a překrývá se s `/specify` víc než dřív, takže hrozí, že se skill chytne na dotaz, který patří do zadání. A **sekce *Časté chyby* v skillu od 21. 9. 2026 není** – celá se smazala, protože ani jeden z jejích deseti řádků nebyl z provozu, jen z úvahy, a skill na skutečném projektu nikdy neběžel. Norma u té sekce žádá doložená selhání. **Založí se znovu po prvním ostrém běhu** a napíše se do ní to, co se doopravdy pokazilo.

- [ ] **Rozhodnout, jestli *Kontrakt příkazů* dostane klíč pro tichou variantu příkazu** (zadáno 23. 9. 2026). `~/Dev/context/coding/quality.md`, *Příkaz vol podle velikosti jeho výstupu*, žádá pouštět kontroly tiše, dokud procházejí – jenže kontrakt dnes zná jeden příkaz na klíč, takže tichou podobu si musí složit ten, kdo ho pouští, a u každého projektu znovu. Nabízel se klíč `- test:quiet: …`.

  **Zamítnuto zatím, ne natrvalo** (`~/Dev/context/decisions.md`, sekce `coding`): formát kontraktu parsuje `~/.claude/verify.sh` i jeho testy, takže by to byl zásah do vynucovací vrstvy kvůli pravidlu, které se zatím nikde neosvědčilo. **Rozhodne se, až bude doložené, že si tichou variantu skládá víc projektů stejně** – do té doby je ruční volba levnější než nový klíč, který musí umět skript, testy, `/project` i každý existující kontrakt.

- [ ] **Opravit ve `/cleanup` recept na základ session – porovnává časy v různých pásmech.** *Fáze 0* dává tenhle příkaz:

  ```sh
  git log --format='%H %cI' | awk -v start='<čas prvního záznamu transcriptu>' '$2 > start {h=$1} END {print h}'
  ```

  **`%cI` nese lokální posun (`+02:00`), kdežto transcript má časy v UTC (`Z`).** Awk je porovnává jako řetězce, takže se srovnává `2026-09-25T15:43:34+02:00` proti `2026-09-24T19:31:17` – dvě různá pásma bez převodu. **Při prvním ostrém běhu 25. 9. 2026 to vrátilo `987e39b`, tedy commit ze *včerejší* session**, místo správného `b24d8c1`. Chyba je tichá: vrátí platný hash, jen ne ten správný, takže diff pro čtenáře by obsahoval cizí práci.

  **Oprava je na epoch, kde pásmo nehraje roli:**

  ```sh
  START=$(python3 -c "import datetime;print(int(datetime.datetime.fromisoformat('<čas prvního záznamu>'.replace('Z','+00:00')).timestamp()))")
  git log --format='%H %ct' | awk -v s="$START" '$2+0 > s+0 {h=$1} END {print h}'
  ```

  **Sekce *Časté chyby* už varuje, že `HEAD` není základ** – tohle je druhá, samostatná vada v tomtéž kroku a varování před ní nechrání, protože vypadá, že recept funguje.

- [ ] **Ověřit, jestli je id session ve scratchpadu spolehlivé – cesty se rozcházejí.** `/cleanup` i `SESSION.md` říkají brát id session z cesty ke scratchpadu. Při prvním ostrém běhu 25. 9. 2026 to sedělo, ale **vedle toho existovala druhá cesta s jiným id**:

  - scratchpad: `/private/tmp/claude-501/-Users-honza-Dev-veneti/53e03c78-…/scratchpad` – správné id téhle session,
  - výstupy subagentů: `/private/tmp/claude-501/-Users-honza-Dev-veneti/3168ebc9-…/tasks/…` – **id předchozí session** z téhož projektu.

  Kdo by id vzal z druhé cesty, sáhne po cizím transcriptu a nic mu to neřekne – oba soubory existují a oba se parsují. **Zjistit, čím ta druhá cesta vzniká** (zůstává po dřívější session? je to jiná komponenta harnessu?) a jestli se na první dá spolehnout vždycky. Než se to ověří, stojí za to v `SESSION.md` napsat, že platí **scratchpad**, ne libovolná cesta pod `/private/tmp` s podobným tvarem.

- [ ] **Změřit první ostrý běh `/cleanup` v subagentovi a doložit, nebo vyvrátit slíbenou úsporu.** Ohlášených **75 až 85 %** proti dnešním 10,2M je **extrapolace z poměru fází**, ne měření: pokusný běh 25. 9. 2026 pokryl jen vytěžení a konfrontaci (45 volání, 0,87M) a odhad celého úklidu v agentovi zněl 1,5–2,5M. Změř to týmž způsobem, jakým se měřilo 249 běhů – ze session logů –, a porovnej s pásmem podle velikosti transcriptu, ne s jedním číslem. **Změřit je potřeba obě části zvlášť**, agenta i rodiče: kdyby úspora nevyšla, rozhoduje, jestli ji sežral agent, nebo režie rodiče. Výsledek patří do `decisions.md` k záznamům o ekonomice úklidu; nevyjde-li, je to argument pro zúžení řezu, ne pro návrat.

  **Agentská část je od 25. 9. 2026 změřená, rodičovská ne.** První ostrý běh nad transcriptem 1,7 MB: vytěžovací agent **33 volání a 197k tokenů**, čtenář navazitelnosti 32 volání a 272k, čtenář pozůstatků 29 volání a 189k (`done.md`). Chybí právě to, co rozhoduje – **kolik stál rodič**: jeho volání a kontext se z notifikací o agentech nedozvíš a je k tomu potřeba týž postup ze session logů jako u těch 249 běhů. Bez toho se úspora tvrdit nedá, protože rodič nesl celou interaktivní část a pět rozhodnutí nad nálezy čtenářů.

- [ ] **Zredukovat počet volání uvnitř `/cleanup`** (zadáno 23. 9. 2026). Úklid dělá **zhruba 150 volání bez ohledu na to, co uklízí** – medián 142, průměr 196, maximum 1872, změřeno na 249 bězích ze session logů. Má proto **podlahu kolem 2,3M nákladových jednotek** i nad malinkou session, kde není co zapisovat.

  **Do 25. 9. 2026 byla ta podlaha schovaná pod cenou kontextu** – úklid nad velkou session stál 10,2M, takže se v tom vlastní režie skillu ztratila. Přesunem do subagenta se z ní stane se z ní hlavní zbývající položka a bude vidět.

  **Dělat až po změření přesunu do subagenta, ne současně.** Řezání volání může ublížit kvalitě výstupu, kdežto přesun ne – a kdyby se udělalo obojí naráz, nepozná se, co za co může. **Kandidáti k posouzení:** dávkovat zápisy místo souboru po souboru, sloučit body 3 a 4 zadání agenta (obě čtou tytéž soubory, jen z opačné strany; do 25. 9. 2026 to byly Fáze 3 a 4 skillu), nepouštět kontrolu odkazů opakovaně po jednotlivých souborech.

- [ ] **Přepsat `/review` a `/consistency` na běh v subagentovi** (rozhodnuto 25. 9. 2026; `/cleanup` hotový týž den). Tvar, o který se to opře, drží `~/.claude/decisions.md`, *Tvar `/cleanup` po přesunu do subagenta*, a hotová podoba `skills/cleanup/` – **neopisuj je sem**. Doložení cesty a naměřená čísla jsou v *Jak se chová `/clear`, `/compact` a transcript, a co stojí `/cleanup`* a v *Skill `/cleanup` poběží v subagentovi, ne v čisté session po `/clear`u*.

  **Co se dá vzít beze změny:** řez (agent vytěží, posoudí a zapíše jednoznačné, nahoru vrací otázky s hotovým textem zápisu), commit jmenovaných cest v rodiči, agent bez práva ptát se a pushovat, a rozdělení dokumentace na postup rodiče a zadání agenta ve vlastním souboru.

  **Co je u nich jinak:** **session-id nepotřebují** – jejich vstup je git diff a soubory, ne transcript; potřebují jen vědět, ve které větvi stojí. A **panel specialistů už dnes delegují**, takže nové je jen delegovat načtení podkladu, konsolidaci a jednoznačné opravy. Pozor na *Hloubka delegace je jedna*: poběží-li skill v agentovi, panel musí pouštět rodič, stejně jako `/cleanup` pouští čtenáře bez kontextu.

  **Ověřovací vrstva k tomu chybí a je to vědomá mezera.** Že agent necommituje a že rodič commituje jen jmenované cesty, drží dnes jen věta v zadání – tedy text pro model, ne mechanismus (`~/.claude/RULES.md`, *Přednost pravidel*). **Týká se to i dvou dalších hranic, které v `/cleanup` vznikly 25. 9. 2026:** že agent nesahá mimo projekt, ve kterém stojí, a že se běh zastaví, vede-li pracovní adresář uklízené **cizí** session jinam. Obojí je jen věta, a přitom je to právě ta třída selhání, kterou oponentura 23. 9. označila za drahou – zápis, commit a push do cizího repozitáře vypadá jako povedený běh. Testy v tomhle repozitáři měří tvar Markdownu, ne chování běhu, takže tohle by musel být tlakový scénář přes `superpowers:writing-skills`. Zapsáno jako mezera, ne jako úkol navíc: rozhodne se u něj spolu s položkou o evaluacích výš.

  **Rozsah je nejspíš širší – je to vlastnost celé vrstvy kontrolních kroků** (Honza, 23. 9. 2026). Kroky osy běží interaktivně v hlavní session, kontrolní kroky neinteraktivně v subagentovi a vracejí nahoru jen pár konkrétních věcí k rozhodnutí.

  **Není to nové pravidlo, ale důsledek toho, které už platí.** `~/.claude/RULES.md`, *Velké průzkumné úkoly deleguj*, říká, že delegace se vyplatí při splnění aspoň jednoho ze tří kritérií – vynucený tvar výstupu, izolace kontextu, práce která se neamortizuje. **Kontrolní kroky splňují všechna tři naráz:** vracejí nález s doložením a závažností, jejich smysl je posuzovat něco, do čeho nemají sáhnout, a je to jeden vstup a jeden výstup. Kroky osy nesplňují ani jedno: výstupem je dokument, kontext je jejich vstupem a práce se amortizuje iteracemi.

  **Vzorec ale sedí na pět ze sedmi, ne na všechny.** `/oponent`, `/review`, `/consistency`, `/attack` a `/cleanup` ano. **`/merge` ne:** je nevratný, mění hlavní větev a maže worktree, a jeho vlastní pravidlo velí mergovat jen na výslovný pokyn – agent na pozadí je přesně to, co má zakázané. **`/consolidate` ne** z jiného důvodu, který `LIFECYCLE.md` sám pojmenovává: jako jediný z kontrol vrací **návrh řešení, ne nález**, a návrh je rozhodnutí, ne měření.

  **Přesné kritérium proto zní:** do agenta patří práce, jejímž výstupem je **nález nebo jednoznačná oprava**; v hlavní session zůstává to, co potřebuje **uživatelovo rozhodnutí**, a to, co je **nevratné**.

  **Do `~/.claude/skills/LIFECYCLE.md` to zatím nepatří** – ten popisuje, jak to je, a platí to dnes u jednoho skillu ze pěti. Až se přepíše druhý a ověří se, že vzorec drží i tam, zapíše se to k rozdělení na osu a kontrolní vrstvu; do té doby je to tady.

  **Vědomě zamítnuté – nenavrhuj znovu bez nového argumentu:**

  - **Evidence uklizených session** (rejstřík a čára po vzoru `/depot` `_state/`): skill je záměrně opakovatelný a evidence by šla proti té vlastnosti. Opakovaný běh je legitimní použití, ne chyba. Měkčí varianta – **id uklizené session v řádku `done.md`** – přijata 25. 9. 2026 a zapsaná ve skillu i v `~/.claude/STRUCTURE.md`; od téhož dne z toho řádku bere druhý běh i hash, odkud vytěžovat.
  - **Nástroje třetích stran na úsporu tokenů** (caveman, rtk, headroom, ponytail): rozbor drží `~/.claude/decisions.md`, *Úspora nákladů míří na kontext, ne na délku odpovědí*.

- [ ] **Posoudit, jestli panel a ověřování v `/review` a `/oponent` převést na nástroj `Workflow`** (zadáno 25. 9. 2026). Je to samostatné téma na vlastní session – souvisí s přepisem `/cleanup` do subagenta (`done.md`) a s položkou o témž přepisu `/review` a `/consistency` výš – obojí je o delegaci kontrolních kroků, ale řeší jinou osu: ta se ptá **kde** skill běží, tahle **čím se orchestruje jeho panel**.

  **Co `Workflow` je** (ověřeno 25. 9. 2026, ne převzato): **vestavěný nástroj Claude Code**, ne plugin ani nic vlastního – v binárce verze 2.1.274 je zadrátovaný včetně `ultracode`, `workflow-authoring` a `.claude/workflows`; v `~/.claude/plugins/` nic takového není. Přibyl s aktualizací z 2.1.267 na 2.1.274. Spouští JavaScriptový skript, který deterministicky orchestruje víc subagentů – `agent()`, `parallel()`, `pipeline()`, `phase()` –, běží na pozadí, průběh je vidět přes `/workflows` a výstup agenta jde vynutit schématem. **Volat ho smí Claude jen na výslovný pokyn uživatele**, protože umí roztočit desítky agentů; tahle instalace má navíc strop „medium“, tedy pod deset agentů. Dokumentaci k psaní skriptů drží skill `workflow-authoring` – **načíst až ve chvíli, kdy se to bude psát**, ne kvůli rozhodnutí, jestli do toho jít.

  **Proč zrovna `/review`:** ukázkový příklad v dokumentaci toho nástroje je doslova „review changed files across dimensions, verify each finding“, což je přesně jeho Fáze 2 a 3. Převést by šly **jen ty dvě**: Fáze 0 je úsudek a výpis vybraných specialistů uživateli, Fáze 1 jsou příkazy za nulu tokenů, Fáze 4 a 5 je syntéza a Fáze 6 až 8 interakce. Totéž platí pro `/oponent` – panel hledisek a ověřovatel na každý nález.

  **Tři věci by se tím získaly a všechny tři dnes drží jen text v pravidlech:**

  - **Ověřování by běželo průběžně, ne až po celém panelu.** Dnes jsou to dvě bariéry za sebou; `pipeline()` pustí ověření nálezů prvního specialisty ve chvíli, kdy doběhne, zatímco ostatní ještě čtou.
  - **Tvar nálezu by vynutilo schéma**, ne věta v zadání. `severity` a `basis` podle `~/.claude/skills/SEVERITY.md` dnes drží text, který si agent vykládá sám – schéma to zamkne strojově. Je to *Ověřitelná kontrola místo dojmu* posunutá o vrstvu níž.
  - **Stropy by přestaly být prosbou.** „Nad sedm specialistů nechoď“ a „nejvýš 12 ověřovatelů“ vykonává tentýž model, který ta pravidla čte.

  **Dvě věci se musí ověřit dřív, než se do toho půjde** – na obojím to může padnout:

  - **Umí `agent()` ve skriptu nastavit model pro jednotlivého agenta?** `/review` rozlišuje pečlivě: standardoví specialisté na výchozím modelu s `medium`–`high`, Bezpečnost a Data a stavy na nejsilnějším s `xhigh`. Jeden model pro všechny by byl krok zpátky, ne dopředu.
  - **Nahradí `resumeFromRunId` soubor `.claude/run/review.json`?** Dokumentace nástroje říká, že obnovení běhu je **jen v rámci téže session**, kdežto ten soubor existuje právě proto, aby rozpracovaná fronta nálezů přežila kompaktaci **i novou session**. Pak by to byla náhrada jen z poloviny a soubor musí zůstat.

  **Třetí nejistota:** Fáze 2 volá `/code-review` a `/security-review` jako vestavěné skilly Claude Code. Agent v workflow nástroj `Skill` nejspíš má (běžný subagent ho má – ověřeno), ale nevyzkoušelo se to.

- [ ] **Dvě věci, na které narazil čtenář bez kontextu 25. 9. 2026 a nevyřešily se** (zapsáno při úklidu, aby nezmizely se session).

  - **`decisions.md` není chronologický, ačkoliv to sám vyžaduje.** Mezi zápisy z 18. 9. sedí *2026-09-08 – `deny` v `settings.json`…*, *2026-09-08 – Dvě podmínky pro `PostToolUse` hook* a *2026-09-10 – Skill `/audit`…*. Buď jsou to vědomé tematické shluky, a pak to musí být napsané, nebo se mají přesunout. **Tvar nadpisů se 25. 9. sjednotil** (všech 75 nese `### YYYY-MM-DD – Název`), pořadí ne. **Pozor při opravě:** zápisy mají vnořené odrážky, takže se nesmí řadit po řádcích, ale po blocích – týž řez tentýž den rozbil `done.md` a musel se vracet z gitu.
  - **Není jasné, jestli se práh z *Dlouhá session je dražší než dvě krátké* vztahuje i na běh skillu.** `RULES.md` velí ohlásit se při ~150 voláních; změřeno je, že `/cleanup` dělá zhruba 150 volání bez ohledu na to, co uklízí. Ohlášení uprostřed vlastního běhu by nemělo komu pomoct, ale pravidlo tu výjimku nezná. Přepis skillu do subagenta to nerozhodl, jen zúžil: 25. 9. 2026 dělal rodič ve celém běhu řádově desítky volání a agent 33, takže se práh nepřekročil ani u jednoho a otázka zůstala nevyzkoušená.
