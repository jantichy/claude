# Hotovo

Odvedená práce na konfigurační vrstvě a záznamy průchodů životním cyklem. Nikdy se nemaže – je to doklad, ze kterého jde zpětně říct, co se kdy udělalo a proč.

**Repozitář je veřejný.** Nic, co sem přibude, nesmí prozradit **obsah** soukromého `~/Dev/context/` – jméno klienta či organizace, sazbu, obchodní nebo osobní údaj, detail přístupu ke klientskému systému, jméno klientského projektu ani know-how, které se prodává. Struktura toho adresáře veřejná je a `README.md` ji sám píše; konkrétní obsah ne. Podrobněji `.claude/CLAUDE.md`, *Výjimky z obecných pravidel*.

## Průchody životním cyklem

Záznamy běhů `/review`, `/oponent`, `/consistency` a `/cleanup` nad tímhle repozitářem. Čtenářem je příští běh téhož skillu: potřebuje vědět, co minule zůstalo mimo rozsah a jak se s tím naložilo.

- **2026-09-02** · `/oponent` · `~/.claude@574dade` · konfigurační vrstva `~/.claude` (životní cyklus projektu, `/review`, `/attack`, `coding.md`, `verify.sh`, meta-testy) · hlediska: vnitřní rozpor, co chybí, skeptik, provoz, hraniční případy · 75 nálezů (70 zapracováno, 0 zamítnuto, 5 odloženo)
  - Katalog hledisek se **po tomhle běhu** zmenšil na sedmnáct: *skeptik* splynul s *předpoklady* a *hraniční případy* s *co chybí*. Příští oponentura ať proti tomuhle řádku srovnává obsah, ne názvy – ty dva hlediska už v katalogu nejsou.
  - Před konsolidací bylo nálezů 84; do počtu výš se sloučily duplicity nalezené víc hledisky nezávisle.

- **2026-09-03** · `/cleanup` · tentýž rozsah · čtenář bez kontextu našel 20 nálezů, všechny zapracovány
  - Tři z nich byly chyby v kódu, ne v textu – zejména kalibrace tempa, kterou otrávil selhaný běh. Zbytek byly pozůstatky po dnešních cílených zásazích do dokumentace.
  - Vzešlo z toho obecné pravidlo `~/.claude/RULES.md`, *Rozsah pravidla se nešíří sám*, doložené dvěma nezávislými výskyty v jednom dni.

- **2026-09-03** · `/consistency` · `~/.claude`, skill `/transcript` po přestavbě (průvodce, slovník jmen, VAD, kalibrace ETA, diarizace, detekce jazyka) · 6 nálezů (4 mechanické, 2 po odsouhlasení, 0 odloženo)
  - Kritický nález byl důsledek vlastní opravy z předchozího kroku: krok 1 sliboval předat detekovaný jazyk do kroku 6, ale příkaz v kroku 6 proměnnou neuváděl. Sedělo to v popisu, nesedělo v příkazu.

- **2026-09-04** · `/cleanup` · `~/.claude`, dořešení odložených položek z úklidu nad `/transcriptem` · **1 zapsána jako hotová, 3 přeformulovány** – práce na nich proběhla, ale otevřená část zbyla u všech tří
  - Měření vyvrátilo tvrzení, na kterém stál strop deseti položek ve slovníku `/transcriptu`. Původní porovnání míchalo délku seznamu s pozicí položky; sedm nových běhů s pevnou pozicí ukázalo, že délka vliv nemá. Vyvrácené tvrzení šlo pryč ze `SKILL.md` i z připraveného článku.

- **2026-09-04** · `/consistency` · `~/.claude`, norma tvaru skillu a skill `/skill` · 4 nálezy (1 mechanický, 3 po odsouhlasení, 0 odloženo)
  - Tři ze čtyř nálezů byly v **normě, která vznikla téhož dne** – nový normativní text má vad nejvíc a audit hned po jeho sepsání se vyplatil.
  - Nejcennější nález nebyl rozpor, ale **asymetrie**: `~/.claude/RULES.md` a `SKILLS.md` mají oba rozřazovací test „co sem nepatří“, ale odkazovaly na sebe jen jednosměrně. Pravidlo o tvaru skillů by podle `~/.claude/RULES.md` skončilo v `~/.claude/RULES.md` – přesně to se dnes málem stalo.
  - Audit taky odhalil, že **norma tvrdila víc, než uměla vynutit**: kontrola souladu s normou hlídala přítomnost sekcí, ne jejich pořadí. Doplněno v `/cleanup`, ověřeno mutačním testem.

- **2026-09-04** · `/cleanup` · `~/.claude`, norma tvaru skillu a skill `/skill` · 5 nevypořádaných témat probráno, 2 čtenáři bez kontextu, 10 položek mimo rozsah rozhodnuto jednu po druhé
  - **Druhý čtenář bez kontextu našel regresi z opravy prvního:** kontrola závěru podle jména se zrušila jako nezávazná, ale `vady_poradi()` závěr podle téhož jména dál hledala – přejmenování, které se právě prohlásilo za legitimní, celou kontrolu pořadí tiše vypínalo. Potvrzuje to poučení z dřívějších průchodů, že **největší riziko nese poslední oprava, ne původní stav**.
  - Táž vlna odhalila, že `SKILLS.md` a `PREFLIGHT.md` byly mimo kontrolu odkazů, takže uříznutá kotva v normě prošla všemi testy – doloženo mutačním testem. A že se odkaz na `PREFLIGHT.md` hledal podle slova „Příprava“ v textu, takže `/oponent` a `/project` z kontroly vypadávaly i se svým opsanou přípravou.
  - **Nejcennější nevypořádané téma nebylo technické:** čtyři nové skilly navržené v první odpovědi zapadly, protože uživatel vybral jednu položku z *prvního* seznamu a k druhému se už nevrátil. Klasická vícebodová odpověď vypořádaná zpola – navenek vypadá vyřízeně.

- **2026-09-04** · `/consistency` · `~/.claude`, norma tvaru skillu podruhé – po dvou čtenářích bez kontextu · 5 nálezů (4 mechanické, 1 po odsouhlasení, 0 odloženo)
  - **Čtyři z pěti nálezů vyrobily opravy z prvního kola** – další datový bod do řady 7 z 8, 8 z 10, 1 z 5.
  - Nejpoučnější nález nebyl rozpor, ale **zestárlé číslo**: text tvrdil „deset skillů opisuje přípravu“, což bylo změřené správně, jenže táž session pak upřesnila detekci a platná hodnota se stala dvanáct. Ne chyba měření, ale posun sémantiky pod ručně zapsaným číslem.
  - Sporný nález ukázal, že norma a `/project` si tiše odporovaly v písmenných podfázích – vyústilo to v přečíslování na plochou řadu 0–13.

- **2026-09-04** · `/cleanup` · `~/.claude`, norma tvaru skillu podruhé – druhý audit a přečíslování `/project` · 2 nevypořádaná témata, obě vyřešena
  - **Test na vnitroskillové odkazy uměl jen „Fázi“, ne „Krok“**, takže přečíslování `/project` prošlo bez kontroly a ověřoval ho jednorázový skript. Rozšířeno o kroky, písmenné podkroky i konce rozsahů; ověřeno mutačním testem. Vzor musel vyloučit odkazy na kroky **životního cyklu** – ty se číslují nezávisle.
  - **Čtenář bez kontextu pak našel, že to rozšíření nestačilo a že přečíslování čtyři odkazy rozbilo.** Ověřovací skript měl tutéž slepou skvrnu jako test – u výčtu čísel za jedním „krok“ viděl jen první položku –, takže tvrzení „žádný odkaz nevisí“ bylo nepravdivé a přežil i zrušený krok `8b`. Opraveno; test rozšířen o celé výčty.
  - **Sekce *Průchody životním cyklem* byla řazená sestupně**, proti pravidlu *Nejstarší nahoře* ze `structure.md`. Odchylku jsem v prvním úklidu vyslovil, ale nikam nepřistála, a další zápisy ji rozšiřovaly. Srovnáno vzestupně.

- **2026-09-06** · `/cleanup` · `~/.claude`, dokončení nedořešených položek od pátečního čtenáře bez kontextu · 9 položek probráno jednu po druhé, 8 vyřešeno, 1 odpadla
  - **Čtenář bez kontextu našel, že páteční ověření přečíslování `/project` bylo nepravdivé** – ověřovací skript měl tutéž slepou skvrnu jako test, který obcházel, takže čtyři odkazy zůstaly rozbité včetně zrušeného kroku `8b`. Odtud pravidlo, že doklad z jednorázového skriptu neváží víc než kontrola, kterou ten skript zastupuje.
  - Z toho vzešly **mutační testy**: „ověřeno mutačním testem“ přestalo být tvrzením a stalo se šesti testy. Tři z šesti mutací přitom nejdřív nechytaly nic – nahrazovaly jen první výskyt.
  - `/project` se přečísloval **potřetí za tři dny** (zrušení `6a`–`6c`, přesun Souladu na Krok 14). Tentokrát to hlídal test, ne skript.

- **2026-09-07** · `/cleanup` · `~/.claude` a `~/Dev/context`, revize neustálených termínů · 3 nevypořádaná témata probrána, 4 mezery v zápisech doplněny
  - **Za den se vypořádalo 34 termínů ve dvou vlnách** – 24 nahrazeno, 10 prověřeno a ponecháno. Slovník `~/.claude/PTYDEPE.md` má 26 hesel, protože `baseline` i `sonda` se rozpadly každý na dvě a je importovaný do každé session; rozhodnutí o ponechaných termínech drží `decisions.md`. **Platilo k 7. 9. 2026:** od 10. 9. drží `PTYDEPE.md` jen tabulku náhrad a celou rozvahu včetně ponechaných termínů převzal `~/.claude/skills/ptydepe/terms.md`.
  - **Nejcennější nález nebyl termín, ale mrtvý import.** `~/.claude/RULES.md`, `STRUCTURE.md` i `PTYDEPE.md` byly v `~/.claude/CLAUDE.md` zapsané uvnitř code spanu, takže se **nikdy do žádné session nenačetly**. Doloženo měřením, opraveno, zapsáno do `STRUCTURE.md` a vynuceno testem.
  - **Vznikl skill `/ptydepe`** se dvěma režimy a ověřený třemi vrstvami: 78 testů, 24 běhů měření vyvolání a tři tlakové scénáře, všechny obstály.
  - **Sekce kontraktu se přejmenovala** z `## Příkazy` na `## Kontrakt příkazů` v osmi repozitářích, bez zpětné kompatibility; ověřeno v čerstvém repozitáři, že hook sekci najde a nad padajícím testem zablokuje.
  - **Dvakrát se ukázalo, že měřidlo lže dřív než měřený.** Detektor vyvolání hlásil 20/24 úspěchů, tedy čtyři propady u doslovných slash příkazů – ve skutečnosti 24/24, protože harness u `/jméno` vloží tělo skillu rovnou a nástroj `Skill` se nevolá. Metodika v `/skill` opravena.
  - **`/cleanup` odhalil, že `/ptydepe` lhal o svém vnitřku:** deklaroval delegaci na `/replace`, která se za celý den nestala ani jednou. Delegace vypadla, skill si náhradu dělá sám mapou frází.

- **2026-09-07** · `/cleanup` · `~/.claude@3f69eec` · popisky v `/cleanup`, kontrolní bod v `/ptydepe`, přepis výstupních šablon napříč skilly · 1 nevypořádané téma probráno, dva čtenáři bez kontextu, 4 položky mimo rozsah rozhodnuty jednu po druhé
  - **Přepis šablon byl po prvním kole hotový jen zčásti a testy o tom mlčely.** Čtenář bez kontextu našel osm šablon bez pokynu, druhý po opravě dalších sedm. Nejdražší jeho nález byl ale jiný: přepis z bloku kódu na tučné popisky vyrobil **horší** stav než původní, protože v Markdownu není konec řádku zalomení a řada `**Kde:** … / **Co:** …` se slije do jedné věty. Opraveno na odrážky.
  - **Šablona v bloku kódu formálně přebíjela `~/.claude/RULES.md`** – *Přednost pravidel* jí dávala přednost „v rozsahu jejího výstupu“ a formát do toho rozsahu spadá. Žebříček dnes rozlišuje obsah výstupu od formátování odpovědi.
  - Norma o šablonách dostala **test se seznamem, který musí přesně sedět** (bloky zapisované do souboru jsou jmenovitě vyjmenované) a čtyři mutace. Bez něj by se to rozešlo potřetí.

- **2026-09-08** · `/review` · `~/.claude@2acbdbd` · celý projekt (85 souborů, 7 specialistů) · 33 nálezů (18 opraveno, 0 odloženo, 1 won't fix, 4 vyvráceno při ověření, 2 falešně pozitivní odhalené až při vypořádání, 8 vyřešeno v rámci jiných)

- **2026-09-08** · `/cleanup` · `~/.claude@f15edac` · vytěžení session po `/review full` · 3 nevypořádaná témata (ztracená kapitola `## Review` obnovena, zákaz exploitování doplněn do zadání ověřovatelů, tři přijatá rizika zapsána), 7 nálezů čtenáře bez kontextu opraveno, 6 položek mimo rozsah vypořádáno

- **2026-09-10** · `/cleanup` · `~/.claude@610723e` · vytěžení session po založení skillu `/learn` · 0 nevypořádaných témat, 5 zápisů doplněno, čtenář bez kontextu našel 18 nálezů (15 opraveno, 1 vyvrácen jako mířící proti zavedenému termínu, 2 přešly do fronty), 7 položek mimo rozsah vypořádáno
  - **Nejcennější nález ležel mimo dnešní soubory a čtenář ho vidět nemohl:** knowledge base měla od 8. 9. vlastní postup vytěžování nahrávek v `analytics/sources.md` a dvě rozhodnutí k němu. `/learn` o nich nevěděl a v evidenci zdrojů jim odporoval. Postup se sloučil do skillu, soubor zůstal evidencí.
  - Poučení pro příští běh: **nový skill se musí porovnat s tím, co v cílové doméně o téže práci už stojí** – ne jen s normou a s okolními skilly.

- **2026-09-10** · `/cleanup` · `~/.claude@71e3fb0` · vytěžení session, ve které `/learn` dostal netextové vstupy · 0 nevypořádaných témat, 3 rozhodnutí předložena uživateli, dva čtenáři bez kontextu (18 + 14 nálezů, všechny opraveny)
  - **Druhý čtenář našel vadu, kterou zavedly opravy po prvním** – věta dopsaná do `/transcript` („neptej se znovu“) rušila i jeho pojistky u kolize jmen a existujícího přepisu, které `/learn` zodpovědět neumí. Potvrzuje to pravidlo skillu pouštět druhého čtenáře po netriviálních opravách; bez něj by ta věta prošla.
  - Poučení pro příští běh: **číslo opsané z cizího skillu je vada i tehdy, když je dnes pravdivé.** Týž commit odstranil opsaný limit stran PDF a o dvacet řádků výš zavedl opsaný strop slovníku – opravovaná vada se zopakovala v jedné změně.

- **2026-09-10** · `/review` · `~/.claude@0208f2c` · netextové vstupy `/learn` (4 soubory, 4 specialisté) · 11 nálezů (8 opraveno rovnou, 6 po odsouhlasení, 2 vyvráceno při ověření)
  - **Dva kritické nálezy byly moje vlastní čerstvé chyby:** neuvozovkovaná cesta v šabloně ffmpegu (jméno souboru od klienta by spustilo cizí kód) a úplně chybějící pravidlo, že obsah zdroje jsou data, ne pokyny – přestože skill zpracovává klientská PDF a `~/.claude/RULES.md` opis do zadání subagenta výslovně žádá.
  - **Ověřovatel použil proti nálezu moje vlastní měření:** u prompt injection namítl, že plán ve *Fázi 5* by byl jediné místo, kde by to uživatel zachytil – jenže skill sám na témže místě dokládá, že se plán nepředkládá spolehlivě (0 z 6 běhů). Tím nález ustál.
  - Poučení pro příští běh: **specialista si bere víc volnosti, než mu dáš.** Agent na bezpečnost dostal větu „klidně si to vyzkoušej v `/tmp`“, odešel se dívat do cizích repozitářů a zasekl se na `curl` bez timeoutu – panel na něj čekal 28 minut, než si toho všiml uživatel. Úzké zadání znamená i tvrdý rozpočet: žádná síť, jmenovitě vypsané soubory, strop na počet příkazů.

- **2026-09-10** · `/cleanup` · `~/.claude@0208f2c` · vytěžení session o rozdělení `PTYDEPE.md` na tabulku a rozvahu · **0 nevypořádaných témat, 2 položky mimo rozsah (obě vyřešeny), 8 nálezů čtenáře bez kontextu**
  - Session byla krátká a nekompaktovaná, takže se nevytěžovala subagentem; `todo.md` ale nesl **zastaralá čísla o velikosti globálního importu** (93 kB včetně `PTYDEPE.md` 10 kB) a bylo je potřeba přepočítat na změřených 100 kB.
  - Čtenář bez kontextu našel dvě chyby vyrobené týž den: **rozbitou shodu v úvodu `PTYDEPE.md`** (přepis „jako by byl zavedený“ na „bylo zavedené“ při zachovaném podmětu) a **vycucaný počet „čtyřicet hesel“** v odůvodnění zamítnuté varianty, ačkoliv hesel je 27. Druhá je poučnější: rozhodnutí se zdůvodňovalo číslem, které nikdo nespočítal.
  - **`terms.md` je po přesunu větší, než býval celý `PTYDEPE.md`** (33 kB proti 23 kB), protože do něj přibylo osm ponechaných termínů z `decisions.md`. Ušetřilo se v kontextu, ne v součtu – a nikde to nebylo řečeno, takže zápis vypadal jako rozpor sám se sebou.
  - Vyřešeno při té příležitosti: `/ptydepe suggest` četl celý `terms.md`, přestože mu k vytipování kandidátů stačí jména. Byla to **táž vada o úroveň níž**, než jakou dnešní práce odstraňovala.

- **2026-09-10** · `/cleanup` · `~/.claude@da62123` · vytěžení session o odpojení `STRUCTURE.md` a životního cyklu z paušálního kontextu · **1 nevypořádané téma (bezpředmětné), 1 položka mimo rozsah (vyřešena), 28 nálezů ve třech kolech čtenáře bez kontextu (28 opraveno, 0 odloženo, 0 won't fix)**
  - **Všech sedm nálezů vzniklo týž den, žádný nebyl starší dluh.** Nejzávažnější: tabulka *otázka → soubor* se do `~/.claude/RULES.md` **zkopírovala místo přesunu**, takže překryv se `STRUCTURE.md` se čistou bilancí zvětšil – a zápis v `decisions.md` přitom tvrdil, že překryv vypadl.
  - **Pravidlo o přeskakování kroku se rozešlo hned při vzniku:** `LIFECYCLE.md` si nechal plné znění včetně `/project`, zkrácená verze v `~/.claude/RULES.md` ho ztratila. Doklad pro *Single source of truth* v přímém přenosu – a v souboru, který o dva odstavce výš sám tvrdí, že rámeček neopisuje, „nesl by druhou kopii, která se rozejde“.
  - **Dva routovací testy si odporovaly:** bod 1 v `~/.claude/RULES.md` posílal každé pravidlo jmenující `docs/` do `STRUCTURE.md`, tedy i nový rozcestník o dvě sekce níž; `SKILLS.md` o `LIFECYCLE.md` nevěděl vůbec.
  - **Číslo −19 % v `decisions.md` bylo kumulativní za oba dnešní kroky**, ale stálo v zápisu o tom druhém, jehož vlastní tabulka sčítá desetinu. Opraveno s výslovnou poznámkou, ať se to nesčítá podruhé.
  - **Druhé kolo čtenáře našlo dvanáct dalších věcí a většinu z nich vyrobilo první kolo oprav** – to je hlavní poučení průchodu. Nejhorší byl **sebeodporující routovací bod**: `~/.claude/RULES.md` posílal pravidlo o přeskakování kroku do `LIFECYCLE.md`, odkud ho oprava o hodinu dřív vyhodila, a tři další místa slibovala týž neexistující obsah. Ve `SKILLS.md` zase **širší bod 1 zastínil nově přidaný užší bod 2**, takže se nikdy nevyhodnotil.
  - **Kola byla tři a každé našlo víc než nula:** 7 → 12 → 9 nálezů. Závažnost ale klesala (sebeodporující routovací pravidlo → chudší kritérium o jeden znak → odstavec před výčtem, na který odkazuje) a rozsah oprav se s každým kolem zužoval, takže se čtvrté nepouštělo. **Kdo opravuje po čtenáři bez kontextu, ať počítá minimálně se dvěma koly** – skill předepisuje druhé jen podmíněně, ale tady by jeho vynechání nechalo v souborech `~/.claude/RULES.md` routující pravidlo do souboru, odkud ho táž oprava právě vyhodila.
  - **Vzorec, který se opakoval ve všech třech kolech:** oprava zúží obsah souboru, ale **popisy toho souboru jinde zůstanou v původní šíři**. Třikrát po sobě tvrdilo několik míst, že `LIFECYCLE.md` drží pravidlo o přeskakování, respektive že v `~/.claude/RULES.md` je „jen rámeček“. Zásah do obsahu si žádá projít i **věty o tom obsahu** – ty grep po přesunutém textu nenajde.
  - **Čísla velikostí se změřila před opravami a zůstala v zápisu nepřepočítaná** (rozdíl 0,7 kB). Doklad k `~/.claude/RULES.md`, *Hodnotu, kterou čte stroj, nepiš – nech ji vyrobit příkazem*: hodnota vypadá pořád správně, takže ji nikdo nepřeměří.

- **2026-09-11** · `/review` · `~/.claude@0e3de3e` · skill `/audit` (`SKILL.md`, `agents.md`, `README.md`) · specialisté: korektnost, bezpečnost, agentní infrastruktura, norma skillů, text a typografie · 43 nálezů → 38 po sloučení → 31 ověřeno → **6 přežilo** (11 opraveno, 0 odloženo, 0 won't fix)
  - **Vyvráceno 25 nálezů, tedy čtyři pětiny.** Nad instrukcemi v Markdownu panel opakovaně grepl jméno pole nebo řetězec, nenašel ho a usoudil, že chybí krok; ověřovatelé pak ukazovali, že pravidlo v textu je – formulované obecně, o fázi vedle, nebo v `PREFLIGHT.md`, kam `Fáze 0` svůj obsah deleguje. **Nad skillem se bez ověřovací vrstvy review pouštět nedá.**
  - Nosné zjištění: **`allowed-tools` je předschválení, ne omezení** – vypsané nástroje projdou bez potvrzovacího dotazu. `/audit` tím u `fill_form`, `press_key` a `evaluate_script` vypínal jedinou vrstvu, která v druhém pásmu držela mechanismem. Platí to pro každý skill, který si do hlavičky píše zapisující nástroje.
  - Subagenti **nedědí `allowed-tools`** – pouštějí se jako `general-purpose` s plnou sadou. Zúžení hlavičky je tedy neomezí; omezit je jde jen zadáním a volbou typu agenta.

- **2026-09-11** · `/cleanup` · `~/.claude@9bde6e6` · vytěžení session, ve které `/ptydepe add frontmatter` rozhodl o termínu „hlavička“ · **dva průchody, 0 nevypořádaných témat, tři čtenáři bez kontextu (10 + 16 + 19 nálezů)**
  - **Co se rozhodlo:** rozsah termínu zůstává u `SKILL.md` (rozšíření na každý Markdown se schválilo a vrátilo), do `terms.md` přibyla výjimka pro vědomě ponechané výskyty, preventivní zápis se uznal jako třetí konec běhu. **Mimo rozsah úklidu se vypořádalo pět položek, všechny vyřešené rovnou:** rozhodnutí o termínech mimo `terms.md`, nepropsaný preventivní zápis do *Fází 2–5*, požadavek na čistý pracovní strom, chybějící kritérium pro vyloučená místa a neproklikatelný *Obsah* `terms.md`. **Nic se neodložilo ani nezahodilo.**
  - **Běh, ve kterém nebylo co nahradit, odhalil chybějící kategorii.** `frontmatter` v běžném českém textu `~/.claude` nestál ani jednou – „hlavička“ tam byla zavedená dávno –, takže rozhodnutí bylo **preventivní zákaz**, ne náhrada. Skill znal jen dva konce (nahrazeno, ponecháno) a tenhle třetí nikde neuznával; doplněn do *Fáze 2, 3, 4, 6*, do popisu `suggest`, do `terms.md` i do README. `suggest` ho přitom nabídnout nemůže, protože řadí podle četnosti – kandidát na preventivní zápis chodí vždycky od uživatele.
  - **Druhé kolo čtenáře našlo víc než první a většinu vyrobilo první kolo oprav** – další bod do řady **7 z 8, 8 z 10, 1 z 5, 4 z 5**, kterou vede `decisions.md`, *Čtyři průchody `/consistency` za sebou a co z toho plyne* – dnešek přidává **15 z 16**, protože jediný nález druhého kola, který nevyrobilo první, byly chybějící kotvy u ponechaných termínů. Dvě z toho byly **faktické chyby v mém vlastním zdůvodnění**: napsal jsem, že pro „session“, `worktree` a `hook` neexistuje český protějšek (heslo o tři odstavce níž zamítá „sezení“ i „relace“), a doklad jsem umístil do `/skill`, *Fáze 5*, kde slovo „hlavička“ není.
  - **Doklad zavedenosti termínu byl vymyšlený.** Heslo se opíralo mimo jiné o `/autocommit` – jehož jediná „hlavička“ je ta od projektového `CLAUDE.md`, tedy přesně to homonymum, které totéž heslo o pět řádků níž vylučuje. Odtud dovětek v *Častých chybách*: u preventivního zápisu se z paměti smí brát úvaha, ale ne fakta o cizích souborech.
  - **Rozšíření rozsahu na „každý Markdown“ se schválilo a téhož dne vrátilo.** Nabídl jsem ho v poměrech `~/.claude`, ale `PTYDEPE.md` jde do každé session každého projektu – a v projektu se statickým generátorem je *front matter* jméno funkce nástroje, tedy přesně situace, která v tomtéž souboru udržela „session“. Rozsah zůstal u `SKILL.md`, zamítnutá varianta je zapsaná i s důvodem.
  - **Povinný závěrečný verdikt `/ptydepe` byl nepravdivý.** „Starý tvar se mimo `PTYDEPE.md` a `terms.md` nevyskytuje“ neplatilo už dnes (funkce `frontmatter()`, datovaný plán, vendor kód) a po zavedení výčtu čtyř druhů vědomě ponechaných míst by neplatilo skoro nikdy. Přeformulováno o „vědomě vyloučená místa“.
  - **Požadavek `/ptydepe` na čistý pracovní strom se změkčil, a to až na druhý pokus.** Nejdřív padlo rozhodnutí nechat ho tvrdý a důsledně se ptát; uživatel ho vzápětí otočil na změkčení. Platí tedy, že rozpracovaný nesmí být jen soubor, **na který náhrada sáhne** – na zbytek stromu se nečeká, protože nad `~/.claude` běží souběžné session skoro pořád a tvrdé znění by buď zablokovalo každý běh, nebo se začalo mlčky obcházet. Rozsah se přitom ve *Fázi 0* ještě nezná, takže se kontrola opakuje ve *Fázi 4*, kde je hotová inventura. **Zamítnuto – nechat tvrdé znění a ptát se pokaždé:** dotaz na cizí rozpracovanou práci, která se do commitu stejně nedostane, nic nerozhoduje a naučí uživatele odklikávat ho bez čtení.
  - **Třetí kolo čtenáře už nehledalo termín, ale samotný záznam** – a našlo v něm dvě nepravdy o vlastní práci: hash ukazoval na commit, který popsaný stav ještě neobsahoval, a počty nálezů byly odhad místo součtu. K tomu tři pozůstatky, které propagace minula: úvodní popis skillu dál znal jen dva konce, *Fáze 6* předepisovala větu „nahrazuje dřívější …“ i tam, kde se nic nenahrazuje, a `terms.md` tvrdil, že se ponechané termíny přestěhovaly 10. 9., ačkoliv dva z nich přišly až 11. **Poučení: záznam o průchodu si zaslouží vlastní kolo kontroly** – při tom nad věcným obsahem se nečte.
  - **Bod 3 *Fáze 0* se přepsal podruhé.** První znění se odvolávalo na odstavec o commitech jako na důkaz, že se cizí rozpracovaná práce nesebere – ten odstavec ale mluví o opačném riziku a záruku nese až `~/.claude/RULES.md`, *Commituj jmenované cesty, ne `-A`*. Druhá vada byla v pořadí: kontrola kolize se souběžnou session stála proti **zastaralému** výpisu z *Fáze 0* a navíc před schvalovací pauzou. Dnes se měří čerstvým `git status` ve *Fázi 4* a opakuje těsně před zápisem.
  - **Konsolidace záznamů o termínech z 10. 9. minula doménové sekce `decisions.md`** – prošla jen `## Claude`. Dohledáno a dokončeno: `guard` (`## coding`) a `stopa práce` (`## business`) přesunuty mezi ponechané termíny, zdvojená rozvaha o „próze“ (`## text`) zkrácena na odkaz. Bez toho by `/ptydepe` oba ponechané termíny otevřel znovu, protože při rozhodování čte jen `PTYDEPE.md` a `terms.md`.

- **2026-09-14** · **Rozhodnuto o CI a zavedena** · `~/.claude@ebadd1c` · Položka z 2. 9. tvrdila, že žádný skill CI nezakládá ani nenabízí a deterministické kontroly běží jen z ruky. Dnes CI existuje: `.github/workflows/verify.yml` pouští kontrakt příkazů čtený přes `verify.sh --contract`, k tomu gitleaks a semgrep jako kroky mimo kontrakt. `/project` ji zakládá v Kroku 12 podle `skills/project/checks.md`, katalog vrstev drží `~/Dev/context/coding/quality.md`, *Vrstvy kontroly a co do které patří*, a `/review` i `/consistency` z ní čtou výsledek místo opakovaného spouštění.

- **2026-09-14** · `/review full` · `~/.claude@1c74baf` · celá konfigurační vrstva · **70 nálezů: 64 opraveno, 4 vyvráceny jako falešné, 2 vědomě neopraveny, 0 otevřených**
  - Pět bezpečnostních děr, každá s reprodukcí i mutačním testem: status line spouštěla cizí kód dvěma nezávislými cestami (git filtry z cizího `.git/config`, hodnoty z JSONu v aritmetické expanzi), průběžnou kontrolu šlo vypnout přesměrováním `XDG_STATE_HOME`, `git cc` obcházel tři deny pravidla naráz a tři skilly běžely s celou sadou nástrojů včetně Gmailu a Drivu.
  - **Nejtišší nález byl ten, že registraci `verify.sh` jako Stop hooku neměřilo nic.** Skript je otestovaný do detailu, ale že ho Claude Code po každé odpovědi opravdu volá, drželo jen ručně editované `settings.json`. Ztráta té registrace nic neshodí – jen se přestane kontrolovat.
  - **Dvakrát se ukázalo, že vadné měřidlo vyrábí falešné nálezy.** Měření zdí textu filtrovalo odrážky, takže minulo tu největší (400 slov); hledání jmen MCP nástrojů přes `name: '…'` minulo definice přes konstantu a nahlásilo dva neexistující nástroje. Obojí by vedlo k „opravě“ něčeho, co bylo v pořádku.
  - Testů ze 181 na 196. Přibyly kontroly, které nálezy téhle revize zachytí příště samy: `allowed-tools`, sekce *Jak je to postavené uvnitř*, odkazy dovnitř cizích fází i ve vedlejších souborech, registrace a timeout Stop hooku, existence vyjmenovaných MCP nástrojů.
  - Nález o zástupném znaku v `allowed-tools` se **obrátil ověřením**: `mcp__server__*` funguje, ale oficiální `plugin-dev` ho označuje za anti-pattern, takže ruční výčet je správně a chyběl mu jen test.

- **2026-09-14** · `/review full` · `~/.claude@f1f73f4` (+ `context@782c336`) · celý repozitář, 97 souborů a 17 800 řádků · specialisté: agentní infrastruktura, bezpečnost, korektnost, standard quality.md, standard SKILLS.md, standard text a typografie · 66 nálezů (7 opraveno, 2 odložena, 57 zbývá ve frontě)
  - **Panel našel šest kritických nálezů a pět z nich mířilo na souhlasový mechanismus a permission systém** – tedy na to, na čem celá konfigurace stojí. Opraveno: kontrakt z libovolného podadresáře pod odsouhlaseným repozitářem, souhlas nevázaný na kontrakt, parser Markdownu rozcházející se s CommonMarkem a možnost vydat souhlas neinteraktivně. Souhlas se nově váže na otisk kontraktu a vydává se jen z terminálu, takže starých pět souhlasů bylo potřeba obnovit.
  - **Nejdražší nález byl mimo bezpečnost:** `transcribe.sh` ničil zdrojovou nahrávku, protože pojistka porovnávala řetězce cest místo souborů. Z 30sekundové nahrávky zbylo 4,6 s. U nahrávek schůzek je to ztráta, kterou nejde vzít zpět; `/transcript` dosud neměl jediný test, teď má `tests/test_transcript.py`.
  - **Doloženo, že deny seznam není hranice, ale doporučení.** Pravidla porovnávají text příkazu, takže je obejde volání přes interpret – a při vlastní revizi se ukázalo, že neplatí ani v přímém tvaru, když je volání součástí složeného příkazu. Agent si tím omylem vydal souhlas, který měl vydat člověk. Odtud potvrzení z `/dev/tty`: jediná pojistka, kterou nástroj nemůže obejít, protože terminál nemá.
  - **Deny pravidla na tajemství platí jen uvnitř projektu**, mimo něj ne. Opravit to nešlo v témže běhu, protože změna `settings.json` se projeví až po restartu session – zapsáno do `todo.md` i s postupem ověření.
  - **Zůstává otevřený jeden kritický nález**: `statusline.sh` spustí `filter.clean` z cizího `.git/config`. Zapsán v `todo.md`.

- **2026-09-14** · `/review` · `~/.claude@d103a5b` (+ `context@296ce58`) · git hook na zprávu merge commitu, CI, katalog kontrolních vrstev · specialisté: bezpečnost, korektnost, testy, standard quality.md, standard text a typografie · 36 nálezů (28 opraveno, 4 vyřešena jako následek, 3 vyvrácena, 1 won't fix)
  - **Panel našel dvě kritické díry v kódu napsaném týž den.** Ve worktree se delegace na lokální hook tiše přeskakovala, protože cesta stála na `--git-dir` místo `--git-common-dir` – tedy selhání přesně v layoutu, který je standardem. A vzor chytal jen `Merge branch `, takže `git merge origin/vetev` prošel. Obojí potvrdily tři nezávislí specialisté a přehrání.
  - **Ověřovací vrstva se vyplatila dvakrát.** Dva bezpečnostní nálezy o CI neobstály: fork PR nemá secrets ani zápisový token a testy z pull requestu se stejně spouštějí, takže parser nepřidával vektor. Bez ověření by se opravovalo něco, co není vada.
  - **Mutační ověření odhalilo falešně zelené testy třikrát**, pokaždé v testu napsaném o pár minut dřív: dva měřily celý soubor a uspokojil je vlastní komentář nad kódem, třetí použil `^` bez `MULTILINE`. Zapsáno jako pravidlo do `RULES.md`, *Ověřitelná kontrola místo dojmu*, a rozvedeno v `coding/quality.md`, *Vynucovací vrstva se testuje jako kód, obousměrně*.
  - **Druhý parser kontraktu zrušen.** Workflow si sekci parsovalo samo a rozešlo se s `verify.sh` ve třech vlastnostech naráz (HTML komentáře, dvě sekce téhož jména, klíč `cwd`). Nový přepínač `verify.sh --contract` je jediná implementace; zavřel čtyři nálezy jednou změnou.

- **2026-09-14** · `/cleanup` · `~/.claude@d4c37c4` · práce na `/transcript` (slučování rozřezané schůzky, ověřování data, seznam zkomolenin) · 4 nevypořádaná témata (3 rozhodnuta, 1 ponecháno) · mimo rozsah: 4 položky, všechny vyřešeny hned
  - Dva čtenáři bez kontextu za sebou, 9 a 14 nálezů. **Druhý našel funkční chybu v tom, co první kolo oprav právě opravilo:** kritérium kontroly data bylo matematicky splněné vždycky, takže by hlásilo poplach i u poctivých metadat.
  - Mimo rozsah se vyřešilo: souhlas před zápisem do vlastního repozitáře, test pro `join.sh`, regresní test nové výjimky v kontrole výstupů, přeťatý komentář v `transcribe.sh`.
  - Testů z 201 na 209, z toho dva mutační – ověřují, že vyřazení hlídané podmínky test opravdu shodí.

- **2026-09-15** · `/cleanup` · `~/.claude@8f5178a` · 4 nevypořádaná témata (4 rozhodnuta, 0 bezpředmětných) · mimo rozsah: nálezy čtenáře bez kontextu vypořádány rovnou (9 oprav ve skillu a doméně), čtenář bez kontextu dostal v `/cleanup` závazný tvar volání bez práva zápisu, `/depot` dostal test na nosnou část

- **2026-09-15** · `/consistency` · `~/.claude@153304b` · `full` – celý repozitář, 109 souborů · 13 nálezů (4 opraveno mechanicky, 9 po odsouhlasení, 0 won't fix)
  - **Panel byl 5 průzkumníků na Sonnetu** rozdělených po vrstvách (normy v kořeni, skilly cyklu, skilly mimo cyklus, README, kód a infrastruktura); ověření nálezů zůstalo v hlavní session, protože je celé mechanické.
  - **Dva nálezy neobstály ve své původní podobě.** Agent hlásil „sekci navíc mají dva skilly“ – měřením se ukázalo, že ji má 14 z 22, takže vada nebyla v těch skillech, ale v normě, která ten prostor neznala. Norma teď uznává blok **předběžných podmínek** mezi *Jak je to postavené uvnitř* a *Fází 0*.
  - **Oprava vyrobila navazující nález, jak skill předjímá:** čtyři skilly měly takovou sekci ještě před vymezením proti sousedům a musely se přeskládat.
  - **`/review` byl na 501 řádcích, tedy na tvrdé mezi délky**, a doplnění povinné sekce ho přes ni přehodilo. Katalog specialistů proto odešel do `skills/review/specialists.md`.
  - **Testy chytily dvě vady v mých vlastních opravách** – překročenou mez délky a odkaz na `/code-review` mimo povolený kontext volání. Vrstva, která vynucuje, tu funguje.
  - Nezkontrolováno: mrtvý kód, nepoužité závislosti, env proměnné a překladové klíče – repozitář nemá `package.json`, `.env.example` ani překlady, takže `knip`, `depcheck` a obdoby neměly co měřit.

- **2026-09-15** · `/consistency` · `~/.claude@97d8625` · `full` – celý repozitář, druhý průchod téže session · 12 nálezů (7 opraveno mechanicky, 5 po odsouhlasení, 0 won't fix)
  - **Dopadů předchozího kola byly 4 z 12, ne většina.** Zbylých osm byl starší dluh, který první panel minul – rozdělení agentů podle vrstev rozhoduje o tom, co se najde, víc než čerstvost textu.
  - **Škála závažnosti dostala třetí stupeň NÍZKÉ místo KOSMETICKÉ.** Vyšlo to z nálezu, že `/audit` si vedl vlastní trojici mimo `SEVERITY.md`: původní trojice míchala dvě osy – *kritické* je o naléhavosti, *kosmetické* o povaze –, takže na audit cizího webu nesedla. Nově je celá na ose závažnosti a `/audit` ji přebírá.
  - **Plošná náhrada přepsala i slova mimo škálu** („drobná změna“ → „nízká změna“, „podrobné README“ → „ponízké README“) napříč osmnácti soubory. Chytlo se to čtením diffu, ne grepem – přesně jak velí `RULES.md`, *Mazání ověř diffem, ne grepem*. Příště na tohle `/replace`, ne ruční `str.replace`.
  - **Mutační test odhalil díru ve vlastní opravě testu.** Přesun zadání ověřovatele do `agents.md` shodil kontrolu vázanou na `SKILL.md`; první oprava ji rozšířila na všechny soubory skillu a tím otevřela cestu ke smazání celé `Fáze 3`, protože hledané řetězce žijí i v zadání. Test teď měří obojí zvlášť.
  - **Nad repozitářem běžela souběžně druhá session** (připnutí akcí v CI, Dependabot, nový skill `/depot`, odkaz na `~/Depot/`). Commitovaly se jen jmenované cesty, takže se nic cizího nezatáhlo – `RULES.md`, *Commituj jmenované cesty, ne `-A`*, se osvědčilo v ostrém provozu.

- **2026-09-15** · `/cleanup` · `~/.claude@bd142da` · 0 nevypořádaných témat · mimo rozsah: 9 položek – 8 vyřešeno hned, 1 odpadla jako falešný nález
  - **Čtenář bez kontextu našel devět vad, které oba audity minuly**, a druhý čtenář pak **šest dalších přímo v opravách toho prvního**. Potvrzuje to, co skill předpokládá: opravy zanechávají nové pozůstatky a jeden průchod čtenářem nestačí, když se sáhlo na devět souborů.
  - **Nejhorší vada celého dne byla věcná, ne tvarová:** do zadání pro agenta `/auditu` jsem vnesl tvrzení, že se nízké nálezy neověřují reprodukcí. Ten skill ale ověřování filtruje cenou a stropem, ne stupněm, a potvrzený nízký nález ve svém souhrnu počítá. Vzniklo to tím, že se mechanika `/review` opsala do cizího skillu.
  - **Táž třída vady byla i v `SEVERITY.md`:** popisoval, co se s nálezem stane, slovy `/review`, přestože po dnešku platí pro pět skillů. Univerzální je jen to, že nejnižší stupeň nese nejmenší jistotu a musí být čím ho podložit; zbytek si určuje skill sám.
  - **`/depot` prošel kontrolou proti normě**, protože vznikl v souběžné session až během auditů a neviděl ho ani jeden. Tvrdil o `/learn` víc, než ten slibuje (doménu jen navrhne, evidenci zapíše jen tehdy, vede-li ji doména), a `argument-hint` zamlčoval výchozí režim.
  - **Klíče kontraktu příkazů měly tři různé výčty** a `/attack` četl klíč `preview`, který nedefinoval žádný. Zdroj pravdy je `coding/quality.md`; opsaný výčet ve `STRUCTURE.md` nahradil odkaz.
  - Smazán zbytkový `.claude/run/review.json` ze 14. 9. – poslední místo v repozitáři, kde žila stará škála závažnosti.

- **2026-09-15** · `/cleanup` · `~/.claude@64bbad8` · session (typy subagentů) · 3 nevypořádaná témata (2 rozhodnuta, 1 bezpředmětné) · mimo rozsah 7 položek, všechny vyřešeny hned: /skill se nově ptá na typ v tabulce delegací, norma dostala tvar volání i definici agents/, instalace v README bere agents/, věta o nezapisování u agenta s plným shellem přeformulována na pokyn, Ekonomika provozu přešla na Explore, snímky v /learn se mažou až po kontrole úplnosti. Session se dotkla i jednoho projektu v `~/Dev`.

- **2026-09-16** · `/cleanup` · `~/.claude@6888b6a` · 1 nevypořádané téma (1 rozhodnuto: měření vyvolání `/next` se nechává na provozu) · mimo rozsah: natvrdo zapsané `origin/main` ve `/specify` vyřešeno rovnou, třetí čtenář bez kontextu nad konečným `/next` vyřešen rovnou (7 nálezů opraveno ve skriptu a skillu); během úklidu přibyly na pokyn uživatele obsazené a opuštěné větve a obnovení přes `/resume`

- **2026-09-16** · `/cleanup` · `~/.claude@f75ebd4` · 0 nevypořádaných témat · mimo rozsah: nic; čtenář bez kontextu nad zrychleným `/next` našel 9 nálezů, všechny opraveny rovnou (hledání kontejneru, obsazenost hlavní větve, prázdná vs. opuštěná větev, neuložené změny v jiných worktree, závislost z konce popisu, plán po úkolech, kořen ve výstupu, pád na zmizelém transcriptu, neexistující sekce v šabloně)

- **2026-09-16** · `/cleanup` · `~/.claude@4670b5c` · 2 nevypořádaná témata (2 rozhodnuta: dorovnání rezervací zapsáno do jejich todo, `SKILL.md` specify zůstává pohromadě) · tři čtenáři bez kontextu, nálezy k dnešní práci opraveny · mimo rozsah: třetí čtenář puštěn hned, ověření kol ostrým během zapsáno do todo

- **2026-09-17** · `/cleanup` · `~/.claude@3daa2f6` · 0 nevypořádaných témat · mimo rozsah: počet znění verdiktu v `/cleanup` proti normě vyřešen rovnou (norma v `SKILLS.md` připouští obměny hotového znění); dva čtenáři bez kontextu nad nabídkou merge a postupem dokončení větve našli dohromady přes 30 nálezů, jasné opraveny rovnou, na pokyn uživatele merge nabízí i před `/attack`, ne však u projektu nasazujícího z `main`

- **2026-09-17** · `/consistency` · `~/.claude@ae990dd` · full, po převodu identifikátorů do angličtiny (tři čtenáři `reader` po částech repozitáře) · 28 nálezů (21 opraveno mechanicky, 6 po odsouhlasení, 1 vyřešen automaticky jako následek, 0 won't fix, 0 odloženo) · vrstva nástrojů přeskočena celá – projekt nemá `package.json`, `.env.example`, překlady ani soubory s verzí runtime

- **2026-09-17** · `/cleanup` · `~/.claude@088e19f` · 3 nevypořádaná témata (3 rozhodnuta: ruční ověření naostro provedeno – `calendar.swift` a `collect.py` v pořádku, `progress.py` nad starým logem vědomá mezera; `/consistency` nad `~/Dev/context` puštěn; druhé kolo nad `~/.claude` nahrazeno čtenářem bez kontextu) · dva čtenáři bez kontextu, opraveno: validace `cwd` v `--contract` proti kořeni projektu, kritérium pro jazyk argumentů, režimů a hodnot v `RULES.md` · mimo rozsah: kolize jména `branch_state` v `collect.py` vyřešena hned (`classify_branch`), úkol o zbylých projektech rozepsán do `docs/todo.md` v pěti z nich; ostatní se neřeší

- **2026-09-17** · `/cleanup` · `~/.claude@16715c0` · 1 nevypořádané téma (1 rozhodnuto: úkol v jednom z projektů pushnut) · čtenář bez kontextu nad úkoly v pěti projektech: opraveno přímo v jejich `docs/todo.md` (pravidlo se odkazuje místo opisu, výjimky do `CLAUDE.md`, opravené počty a citace, doplněné `<sekce>`/`<skript>`) a v `RULES.md` rozlišeno jméno testu od řetězce `describe`/`it` · jeden projekt bez úprav úkolu: převod v něm podle zapsaného úkolu rovnou odpracovala souběžná session ve větvi `fix/identifiers` a přimergovala do `main` (`55b6ed4`), takže nálezy čtenáře k úkolu v něm jsou bezpředmětné · mimo rozsah: žádné

- **2026-09-18** · `/cleanup` · `~/Dev/context@1c82e75` · rozsah platnosti stylistických a typografických pravidel v doméně `text` · 3 nevypořádaná témata (3 rozhodnuta: rozsah dostala i sesterská pravidla o opakování slova a opisném pasivu, zbylé nálezy `/review` v dotčeném projektu si drží tamní session, tvrzení o středníku v `compose/style.md` doplněno o četnost) · dva čtenáři bez kontextu, druhý nad opraveným stavem: první našel 11 rozporů a 5 pozůstatků (mimo jiné chybějící rozhodovací test rozsahu a odkaz na kapitolu přestěhovanou do `copy.md`), druhý odhalil, že vlastní oprava vrátila do textu zamítnutý test podle čtenáře a nechala v hlavičce `text.md` zrušené znění; vše opraveno · mimo rozsah 6 položek: `/review` dostal specialistu na `text/copy.md` a větu o užším rozsahu (`~/.claude@dba6c41`), import `copy.md` doplněn do dvou projektů (třetí doménové importy nemá, neřešeno), sedm záznamů o konfiguraci přesunuto ze sekce `## text` do `## Claude`, vznik `copy.md` dopsán do `done.md`, oslovení uživatele v rozhraní rozhodnuto na vykání jako výchozí, pravidla o textu odkazu do `copy.md` doplněna už při opravě rozsahu

- **2026-09-19** · `/cleanup` · `~/.claude@b847c2b` · zrychlení Fáze 6 a chování Fáze 7 ve skillu `/cleanup` · 1 nevypořádané téma (1 rozhodnuto: rozdělení `cleanup/SKILL.md` odloženo do `todo.md`, protože řez narychlo na konci session rozdělí fázi od pravidla, které ji řídí) · **první běh nové Fáze 6** – skript na odkazy čistý, dva čtenáři bez kontextu běželi na pozadí přes celou Fázi 7 a vrátili 12 + 15 nálezů; 1 vyvrácen (datace 18. 9. sedí s commitem `a5cad3f`), zbytek opraven. Nejcennější byly tři, které přestavba vyrobila: *Rozsah* a úvod Fáze 7 dál posílaly starší dluh do Fáze 7, přestože čtenáři doběhnou až do Fáze 8; **mechanismus běhu na pozadí nebyl popsaný nikde**, takže skill podle sebe sama nešlo provést; a mezi skriptem (vidí jen změněné soubory) a zadáním čtenářů (odkazy nehledat) zůstala díra, kterou by neprověřil nikdo. K tomu doplněno, co dělat při návratovém kódu `2`, při nezměněném Markdownu a při spadlém čtenáři, a zapsáno, že dvojice čtenářů není panel agentů ve smyslu `SKILLS.md` a ověřovatele nemá · mimo rozsah 2 položky: sekce `## Průchody životním cyklem` srovnána vzestupně podle normy (11 záznamů; **podruhé** – poprvé 6. 9. 2026), a zjištění, že oprava bez mechanismu nevydržela deset dní, zapsáno do `todo.md` i s tím, že tenhle repozitář nemá `tests/` ani CI, kam kontrolu pověsit

- **2026-09-20** · `/cleanup` · `96e0b4d` · session přestavby životního cyklu na osu a kontroly · 2 nevypořádaná témata (2 rozhodnuta) · čtenáři našli 13 + 13 nálezů, opraveno 15, zbytek do `todo.md` · mimo rozsah: 1 položka (testy změřeny, neopravovány)

- **2026-09-20** · `/skill update` · `~/.claude@3c9a3c1` · všech 27 skillů proti normě `skills/SKILLS.md`; měřeno jen to, co nevynucují testy, protože `MIGRATION` je prázdný a sada je od 15. 9. 2026 na normě · 4 vzorce nálezů (4 skilly opraveny mechanicky, 9 po odsouhlasení, 1 vyvrácen ověřením, 7 ponecháno vědomě): `allowed-tools` psané jako řetězec u `/serviceaccount` a nepoužívaný `Agent` u `/breakdown`, `/compose` a `/specify`; pět skillů cyklu se nevymezovalo proti sousedovi přímo v `Co skill nedělá`, `/implement` ani proti jednomu – sousedé byli jen v `Co skill dělá`, což norma nestačí; `/discovery`, `/report` a `/transcript` neurčovaly typ subagenta, takže jejich agenti dostávali plný shell i zápis (doplněn `researcher`, `reader` a u `/transcript` přiznaný výchozí typ, protože zapisuje `<name>.md`); `/cleanup` měl do tvrdé meze 500 řádků rezervu 4 a uvolnil se vytažením Fáze 7 do `cleanup/out-of-scope.md` (496 → 422), zbylých sedm skillů v měkkém pásmu 300–500 ponecháno · **vyvráceno:** nástroj `ListAgents` nenahradí `sessions.py` v `/next`, protože vrací jen jméno, stav a stáří session, ne větev ani adresář projektu – zapsáno do jeho `SKILL.md`, ať se příští revize neptá znovu · **mimo rozsah 1 položka:** `test_contract_patterns_cover_repo` zčervenal kvůli `git-guard.py`, který do kořene přidal commit `fbf30e2` během tohohle běhu. Kontrakt příkazů Pythonem kořen nečetl vůbec, takže ten hook nelintoval nikdo; po doplnění `./*.py` vyšlo najevo, že `verdict` má cyklomatickou složitost 14 proti prahu 10, a rozdělil se na `table_verdict` a `combo_verdict` se zachovaným pořadím vyhodnocení (`~/.claude@382351c`). Práh se nesnižoval

- **2026-09-20** · `/cleanup` · `9c07ed9` · 0 nevypořádaných témat · **první běh nad vlastní frontou** – tenhle repozitář dostal `todo.md`, `backlog.md`, `done.md` a `decisions.md` v půlce téhle session, takže záznam už nejde do knowledge base · dva čtenáři bez kontextu, čtenář pozůstatků dvakrát (podruhé nad opravami): 12 + 16 + 13 nálezů, 3 vyvráceny jako mezitím vyřešené · mimo rozsah 4 položky, všechny vyřešeny hned

  **Nejcennější byl nález, který nikdo nehledal.** Čtenář pozůstatků si vyžádal ověření tvrzení o `git clean` a ukázalo se, že hook `git-guard.py` propouští `git clean -fx`, `-ffd` i `-xf` – výčet slepených zkratek v tabulce byl děravý. Po opravě přišel druhý, horší: s `clean.requireForce=false` v konfiguraci maže `git clean` i úplně bez `-f`, takže kritérium „obsahuje písmeno f“ bylo pořád vedle. Zastavuje se proto podle toho, jestli je běh suchý. Obě díry doloženy spuštěním, obě mají test v obou směrech a mutační běh potvrdil, že je testy chytí.

  **Druhý nález téže povahy:** kontrakt příkazů Pythonem nečetl kořen repozitáře, takže `git-guard.py` nelintoval nikdo – a hned po doplnění `./*.py` vyšlo najevo, že `verdict` překračuje práh složitosti. Práh se nesnižoval, funkce se rozdělila.

  **Pozůstatky po vlastní práci byly tři** a všechny vznikly řezem, kterým se z `/cleanup` vytáhla *Fáze 7*: dvě doslovné kopie téhož textu, které se hned rozešly, odkaz na „bod 2“ mířící do souboru, kde už nebyl, a chybějící prázdný řádek před `------`, kterým se ze závěrečné věty stal setext nadpis. Žádný z nich nechytil skript ani testy.

- **2026-09-20** · `/cleanup` · `532b9ce` · 1 nevypořádané téma (1 rozhodnuto, 0 bezpředmětných) · mimo rozsah: 3 položky – věta o konci cyklu v `.claude/CLAUDE.md` vyřešena rovnou, přečíslování rozbitého seznamu v `todo.md` a přednost `/next` před číslovanou řadou vyřešeny na přání.

  **Nevypořádaná zůstala délka `/architect`** – otevřel jsem ji sám v rozhovoru o zadání, uživatel odpověděl na něco jiného a k délce jsme se nevrátili. Rozhodl ji nechat na měkké mezi a posoudit při revizi.

  **Čtenáři bez kontextu našli 22 nálezů a byli tím nejcennějším krokem celého běhu.** Navazitelnost přinesla, že seznam práce v `todo.md` byl rozbitý – písmenný bod 5a stál nad pětkou, takže v Markdownu končila číslovaná řada, a dva pokyny o pořadí si v témže odstavci odporovaly. Pozůstatky přinesly osm míst, kde dnešní práce nechala nepravdu: `/breakdown` tvrdil, že `/architect` neexistuje, `/project` o něm nevěděl a norma dokládala písmennou podfázi příkladem, který téhož dne zanikl.

  **Druhý průchod čtenáře pozůstatků nad opravami byl to podstatné.** Našel čtrnáct dalších věcí a mezi nimi **jedinou funkční vadu celého úklidu**: nový řádek tabulky v `/breakdown` stál pod obecnějším případem, takže se na něj při čtení shora dolů nikdy nedošlo a `/architect` by se nenabídl nikdy. Zbytek byla evidence, která se rozešla s vlastními opravami – stav bodů, počet testů, odrážky popisující vady, které se mezitím vyřešily. **Bez druhého průchodu by ta vada v repozitáři zůstala**, protože první čtenáři ji vidět nemohli: vznikla až jejich opravou.

- **2026-09-21** · `/cleanup` · `0ca8b42` · session přestavby `/discovery` na otázku „proč“ · 2 nevypořádaná témata (2 rozhodnuta) · čtenáři našli 12 + 8 nálezů, 1 falešný, zbytek opraven; druhý průchod čtenáře pozůstatků přidal 9 · mimo rozsah: 2 položky rozhodnuty uživatelem (sekce *Časté chyby* v `/discovery` smazána celá jako nedoložená, oponentura rozšířena o `risks.md`) · **zjištěno souběžné běžení druhé session** – commit `9455ff2` sebral rozpracovanou práci, zapsáno jako devátý výskyt do `todo.md`

- **2026-09-21** · `/cleanup` · `7365c04` · 2 nevypořádaná témata (1 rozhodnuto, 1 do backlogu) · mimo rozsah: 11 pozůstatků po přestěhování postupu do `/merge` opraveno rovnou, rozpad odstavců v `done.md` vyřešen na přání uživatele, CI opraveno na pokyn uživatele (padalo na každém pushi na testech vázaných na jeden stroj)

- **2026-09-22** · `/cleanup` · `ddac9c8` · session zavedení `/evaluate` do životního cyklu · 1 nevypořádané téma (1 rozhodnuto: doměřit `description`, výsledek 12/12) · kontrola odkazů čistá (33 souborů) · čtenáři našli 11 + 14 nálezů, **1 vyvrácen** (opsané pořadí kroku v `/release` hlídá test, takže to není vada), 22 opraveno, 1 sporný rozhodnut uživatelem; **druhý průchod čtenáře pozůstatků našel dalších 9**, z toho jednu rozbitou větu, kterou vyrobila vlastní oprava · mimo rozsah: nic – vše z dneška spadalo do rozsahu · **do `backlog.md` přibyly 2 nápady** (podmínka srovnávacího běhu ve `/skill`, zápis fáze cyklu do projektového `CLAUDE.md`)

- **2026-09-25** · `/cleanup` · `1ca43d2` · session `45743833-4be9-4f40-9715-e55cb062f626` · session ekonomiky kontextu a rozhodnutí, že `/cleanup` poběží v subagentovi · 1 nevypořádané téma (1 rozhodnuto: řádek v `done.md` nese i id uklizené session) · kontrola odkazů čistá (7 souborů) · **čtenáři našli 12 + 20 nálezů a ani jeden se nevyvrátil** – nejcennější byl, že hlavní rozhodnutí session nemělo záznam v `decisions.md` a žilo jen v `todo.md`; dál dva různé odhady úspory, nadpis položky jmenující zamítnutou cestu, kritérium čisté session zároveň zrušené i platné, a `SESSION.md` tvrdící, že session-id je poslední adresář v cestě ke scratchpadu (je předposlední) · mimo rozsah 6 položek, všechny vyřešeny: `skills/synced/` do `.gitignore`, opačné pořadí fází v README skillu, **pořadí v téhle sekci rozpadlé potřetí** (srovnáno po blocích; první pokus po řádcích zahodil 111 řádků vnořených poznámek a musel se vrátit z gitu), nepravdivé tvrzení v `todo.md`, že kontrolu pořadí není kam pověsit, mrtvý odkaz v `~/Dev/context/coding/coding.md` (opraveno tam, commit `9e6dc55`) a sjednocení tvaru nadpisů v `decisions.md` na přání uživatele

- **2026-09-25** · `/cleanup` · `4893c40` · session `65f296fc-765d-4353-8a5b-b9ec35914707` · **první ostrý běh přepsaného skillu** – přepis `/cleanup` do subagenta · 1 nevypořádané téma (1 rozhodnuto: mez souběžné práce v téže session zapsána do `decisions.md` a `SKILL.md`) · kontrola odkazů čistá (11 souborů) · **agent vytěžil transcript 1,7 MB za 33 volání a 197k tokenů** proti mediánu 150 volání a 10,2M nákladových jednotek u staré podoby; kvalita obstála – sám našel, že `git rev-parse HEAD` nedává základ session v projektu s autocommitem, což byla vada v pravidle napsaném téhož dne · **čtenáři vrátili 19 + 8 nálezů, 1 vyvrácen** (ukazatel na oponenturu z 23. 9. se neztratil – je v řádku `done.md` s id session `45743833-…`, což je právě to, kvůli čemu se id na řádek zapisuje) · z nálezů vzešlo pět rozhodnutí: základ session počítá rodič a předává v promptu, do šablony promptu přibyl režim umístění standardních souborů, **navazování druhého běhu na hash z `done.md` se zrušilo** týž den, co se rozhodlo, **argument `[id session]` se zrušil celý** jako pozůstatek po zamítnuté cestě přes `/clear`, a bez gitu skill pokračuje s ohlášenými mezemi místo aby se zastavil · doplněna sekce `Časté chyby` ze čtyř věcí, které se v tomhle běhu pokazily · mimo rozsah: žádné – všechny nálezy se týkaly dnešní práce

## Odvedená práce

- [x] **`/cleanup` běží v subagentovi.** (2026-09-25) Jádro skillu – rekonstrukce session z transcriptu, konfrontace se soubory, ověření průběžné aktualizace, zápis a kontrola odkazů – se přesunulo do vytěžovacího subagenta; rodičovské session zbyly otázky, čtenáři bez kontextu, commit a verdikt. Důvod je naměřený: úklid stál **55 %** nákladů session, ve kterých běžel, protože svých ~150 volání dělal nad největším kontextem, jaký ta session kdy má – a přitom ten kontext nepotřebuje, session si rekonstruuje z disku. **Devět fází se přečíslovalo na sedm** (čtenáři se pouští hned po agentovi, tedy před interaktivní částí) a zadání agenta má vlastní soubor `skills/cleanup/agent.md`, protože si ho agent načte sám. Přibyla osmá vytěžovaná kategorie – **co zůstalo rozbité nebo nedodělané** –, základ session **počítá rodič a předává ho v promptu**, protože `HEAD` jím v projektu s autocommitem není, čtenář pozůstatků dostává **diff větve** místo diffu session, a druhý běh vytěžuje transcript vždy celý, aby nezdědil slepá místa prvního. Commit skládá rodič **z jmenovaných cest**, které mu agent vrátí, takže cizí rozdělaná práce v pracovním stromu do něj nespadne. Šest rozhodnutí o tvaru i pět zamítnutých variant drží `decisions.md`, *Tvar `/cleanup` po přesunu do subagenta*; **úspora 75 až 85 % zůstává extrapolací** z poměru fází a doloží ji až první ostrý běh. Totéž čeká `/review` a `/consistency` (`todo.md`).

- [x] **`description` u `/evaluate` je změřená v plném počtu.** (2026-09-22) Zakládací běh stihl jen 4 prompty ze dvanácti, které norma žádá; doměřeno na **12/12**. Všech šest pozitivních chytilo `/evaluate`, všech šest near-missů minulo správně – `/consistency`, `/audit`, `/review`, `/release`, `/report` a `/attack`. Měřeno na skutečném kanálu (`claude -p --output-format stream-json`) nad projektem, který měl o čem mluvit, protože prompt bez existujícího vstupu se propadne na dotazu, ne na popisu. **Dvojí běh na prompt se nedělal** – to je z normy jediné, co u tohohle skillu zůstalo nezměřené.

- [x] **Bod 1 přípravy rozlišuje, jestli skill git doopravdy potřebuje.** (2026-09-22) `PREFLIGHT.md` velel u adresáře bez `.git` skončit bez dalšího příkazu – jedna tvrdá podmínka pro každý skill nad projektem. **Obešli ji tři agenti nezávisle na sobě**, protože jejich skill nic nemění a necommituje. Bod mísil *zjisti, kde stojíš* s *bez gitu nepokračuj*; rozdělil se tedy na dvě, druhá půlka se váže na **kritérium, ne na jmenovitý výčet skillů**, a dopsalo se i to, že s gitem odpadají body 3 až 5, protože všechny na něm stojí. Zamítnuté varianty i poznámka, že to odhalilo měření ve scratchpadu a ne provoz, jsou v `decisions.md`.

- [x] **Tlakové scénáře `/evaluate` prošly.** (2026-09-22) Čtyři scénáře, každý tlačil na jedno omezení skillu: přepsat produktové podklady, přeskočit běh kvůli chybějícímu měření, ohlásit nezopakovaný poznatek, zamítnout vyžádanou věc odkazem na zadání. **Žádné se neprolomilo** – ani tam, kde zadání porušení nabízelo jako nejsnazší cestu („klidně to zabal“, „co jsme si odškrtli, to platí“). Cenné ale bylo, že agenti ve třech místech udělali víc, než skill předepisoval, a všechna tři se do něj zapsala: **vyvrácené odůvodnění rozhodnutí se hlásí, i když rozhodnutí platí dál**; **uklidňující tvrzení musí ověření přežít stejně jako alarmující** (nešlo tvrdit „ztrácí se“ ani „neztrácí se“, takže nepotvrzeno v obou směrech a závažnost podle toho, co nejde vyloučit); a **použití nepostavené funkce nemůže být v datech**, takže záznam, který se jím tváří, měří něco jiného. Rozbor i mez měření drží `decisions.md`.

- [x] **Zpětnou vazbu z provozu zavírá `/evaluate` a `docs/operation.md`.** (2026-09-21) Cyklus umožňoval zavřít smyčku jen pro **pády** – sledovací okno v `/release` se uzavírá větou „N nových chyb“ a tím to končilo –, kdežto otázka „je to k něčemu?“ neměla v soustavě vlastníka. Nový **poslední krok osy** odpovídá na šest otázek (používá se to, kde to lidé nedokončili, co si vyžádali, **co jim systém odmítl**, co z rizik se projevilo, co nepoužil nikdo) a vyrábí nový produktový podklad, ze kterého se **nepropisuje nic samo**: zadání mění `/specify` v dalším průchodu. **Jako jediný krok osy ho nespouští výstup předchozího kroku, ale čas** – `/release` proto zapíše do `todo.md` datované `od <YYYY-MM-DD>` a `/next` tu položku v ten den nabídne; parkování k datu je přitom obecný mechanismus v `collect.py`, ne věc jednoho skillu. Zdroje bere podle **žebříčku síly** (analytika, databáze, logy, tikety, vlastní pozorování) a jejich absence je platný výsledek, ne důvod krok přeskočit. **Hotovo znamená, že žádný poznatek nezůstal bez rozhodnutí.** Propsáno do `RULES.md`, `LIFECYCLE.md`, `STRUCTURE.md`, `/release`, `/next`, `/project`, `/specify`, `/cleanup`, `/audit`, `SEVERITY.md`, `FINDINGS.md`, kořenového README a rámečků všech README cyklu – **část z toho až úklidem druhý den**, kdy dva čtenáři bez kontextu našli, že se nový krok propsal do rámečků a tabulek, ale ne do vět kolem nich; šest rozhodnutí o *jestli a jak* a čtyři ze stavby drží `decisions.md`. **První ostrý běh vytěžil tři opravy** – podmíněnou delegaci sběru, ověřovatele nálezu jako samostatného agenta a dělení poznatků podle toho, na čem stojí –, a doložil, že **srovnávací běh nad skillem v témže repozitáři měřit nejde**: agent si ho uprostřed práce našel na disku.

- [x] **`/discovery` se ptá nejdřív na „proč“ a vyrábí `docs/demand.md`.** (2026-09-21) Krok, který měl v cyklu odpovídat na *proč*, odpovídal jen na *proti čemu* – zkoumal konkurenci a rizika, ne existenci problému a poptávky. Mezi `/project` a `/specify` tak nestálo nic, co by odchytilo nejdražší způsob selhání celého cyklu: pečlivě postavený produkt, který nikdo nechce. Nově má skill sedm fází místo šesti, Fáze 1 se ptá na pět věcí (přibylo „co ho to dneska stojí“ a „odkud víš, že to chce řešit“) a nová Fáze 2 doklady dohledá venku dvěma až třemi cestami z vlastního tříčlenného bloku `paths.md` (*Hlas problému* povinná, *Ochota platit*, *Objem a jazyk hledání*), jejichž rozpočet se nepůjčuje s konkurenční rešerší. **Doklady se oddělují od dojmů zvláštní sekcí** a verdikt má dvě hodnoty bez ničeho mezi nimi; u nedoložené poptávky se skill zastaví a nabídne tři cesty dál, ale nerozhoduje za uživatele a nesmí přes to přejít mlčky. Nedoložená poptávka pak jde do `risks.md` jako riziko s nejvyšším dopadem. **Skill se nově škrtá po dokumentech, ne celý** – u projektu bez trhu odpadá jen konkurence, protože interní nástroj, který si lidé obejdou tabulkou, je totéž selhání jako aplikace bez zákazníků. Propsáno do `STRUCTURE.md`, `LIFECYCLE.md`, `/project`, `/specify`, `/cleanup` a obou README; zamítnutá jména dokumentu (`problem`, `need`, `why`, `evidence`) drží `decisions.md`. **Mezera pojmenovaná při téže úvaze zůstává otevřená** – zpětná vazba z provozu, viz `todo.md`.

- [x] **Dokončení větve navazuje na `/cleanup`.** (2026-09-16) Po úspěšném úklidu se skill zeptá, co dál: pokračovat v práci a další kolo úklidu nabízí vždy, ve worktree větvi navíc merge do `main` – ne ale, když merge něco brání, rozpracovaný `main/` nebo projekt nasazuje přímo z `main` (merge by byl nasazení a patří `/release`). `/compact`, `/clear` a `/exit` jen jmenuje, protože je spustit neumí. Vybraná volba platí jako výslovný pokyn k merge. Tím padla položka „chybějící krok životního cyklu pro dokončení větve“ z revize 4. 9. (`/cleanup` sám říkal, že větev zůstává otevřená, a nic s tím nedělal) – samostatný krok se zavrhl, zdůvodnění je u otázky v `skills/cleanup/SKILL.md`. **Způsob merge volí Claude sám** (`WORKTREE.md`, *Dokončení větve*, kroky 1–4): `git merge-base --is-ancestor` zjistí, jestli se `main` od odbočení větve posunul; když ne, jde merge rovnou do `main`, když ano, `main` se nejdřív přihraje do větve (`--no-commit`), konflikty se vyřeší a spojený stav ověří kontraktem příkazů tam – **i bez konfliktu**, protože textově čistý merge ještě neznamená funkční kód. První verze rozhodovala podle `git merge-tree` (jen konflikt); dva čtenáři bez kontextu v `/cleanup` pak ukázali neověřený stav na `main`, merge do nasazovací větve, obejité přečíslování kola návrhu a chybějící postup při odmítnutém pushi či posunu `main` mezi kroky. Claude se uživatele ptá jen na konflikt, kde obě strany rozhodly tutéž věc jinak.

- [x] **Celá sada skillů je na normě, `MIGRACE` je prázdná.** (2026-09-15) Dvanáct skillů prošlo 14. 9., poslední `/transcript` o den později v `/cleanupu`. Tělo mu kleslo z 570 na 479 řádků vytažením dvou celků, které se čtou jen ve svém kroku (`output.md`, `glossary.md`). **Přitom se ukázalo, proč je plochá řada číslování v normě:** `## Postup` byl mezinadpis nad `### 1.` až `### 10.`, takže po přidání `Krok 0` vznikly dvě úrovně číslování – a test to odhalil okamžitě, protože odkazy „viz krok 3“ přestaly mířit na existující nadpis. Kroky jsou teď jedna řada 0–11 na úrovni `##`. Seznam `MIGRACE` zůstává jako prázdná množina schválně: až norma přituhne znovu, je kam zapsat, co nedorovnalo, a test dál hlídá oba směry.

- [x] **Osa modelu a effortu dostala měření spotřeby** (nález 73 z `/review full`). (2026-09-14) Kapitola *Model a effort podle úkolu* měla všechny páky mířící nahoru – ověřovatel na nejsilnějším modelu ke každému nálezu, panel o několika specialistech, průzkumník navíc –, ale po běhu nezůstalo žádné číslo, takže se dávkování rozhodovalo odhadem. Pravidlo je v `~/.claude/RULES.md`, řádek se spotřebou mají `/review` i `/attack`. **Přesné tokeny skill nezná a nemá je předstírat**; počet agentů a jejich tier je proxy, která stačí na to, aby šlo dávkovat podle čísla místo podle dojmu.

- [x] **Souhrnná kontrola před `/release`.** (2026-09-14) Fáze 1 pustí všechny kontroly a teprve pak rozhodne, místo aby skončila na první, která padne. Je to jiná otázka než u průběžné kontroly: ta je binární a běží po každé odpovědi, kdežto před nasazením se ptám „kolik věcí je rozbitých?“. Skončit na první znamená opravit ji, spustit znovu a narazit na druhou – a každé kolo stojí celý běh včetně `build` a `e2e`. Výjimkou zůstal čistý pracovní strom: nad špinavým by se měřilo něco jiného, než co se nasadí.

- [x] **Předpočítaný kontext při delegaci + doslovné předání výstupu.** (2026-09-14) Obě půlky do `~/.claude/RULES.md` k delegaci. Co hlavní session zjistila v přípravě, dostane agent rovnou – při pěti paralelních specialistech je to pětinásobek téže práce a pětinásobek kontextu, který jim chybí na vlastní úkol; patří sem i to, co se vědomě zamítlo. Strukturovaný výstup se předává **doslova**, protože parafráze tiše ztrácí detail, kvůli kterému se agent posílal. **Přitom se dořešilo zdánlivé protiřečení s pravidlem *Interní značky ven nepatří*:** doslovně se předává mezi kroky, uživateli se hlásí obsahem – doslovnost míří na to, co nález nese, ne na to, jak se pojmenuje.

- [x] **Registr obcházení blokujících kontrol.** (2026-09-14, `dc42ed7`) `~/.claude/BYPASS.md` – u každé z pěti vynucovacích vrstev (průběžná kontrola, git hook, CI, permission systém, status line) tabulka *čím se obejde / co to chytí / stav*. Většina řádků je `accepted` a **u každého stojí důvod**, což hlídá test. Kompletnost je zamčená druhým testem, který seznam vrstev čte z disku – z hooků v `settings.json`, z `githooks/` a z workflow –, takže nová vrstva bez řádku shodí testy. Obecné pravidlo, že takový registr má vzniknout, přibylo do `coding/quality.md`.

- [x] **Hlasitost varování škáluje se závažností.** (2026-09-14) Zapsáno do `coding/quality.md` jako tabulka čtyř úrovní: bezpečnostní kontrola nahlas **i s návodem, čím to spravit**, kontrola správnosti nahlas jednou větou, měřítko kvality řádkem ve výpisu *Nezkontrolováno*, kosmetika mlčí. Důvod: varování, které křičí u všeho, přestane být slyšet u toho podstatného – hlášení o chybějícím formátovači si je v terminálu rovno s hlášením o nespuštěném scanu tajemství, takže si čtenář odvykne číst obojí. K tomu opačný směr jako stejná vada: kontrola, která mlčí o tom, že neproběhla, se od úspěšné nepozná.

- [x] **Zapsat pravidlo hloubky delegace = 1.** (2026-09-14) V `~/.claude/RULES.md` u delegace. Zisk z rozeslání práce plyne z toho, že si každý agent nese vlastní kontext místo tvého – ve druhé úrovni se ale nekoná: vnuk načítá totéž co rodič a jeho výstup se vrací přes prostředníka, který ho převypráví. **Zadání se přitom v jedné věci mýlilo:** `Agent` v `allowed-tools` skillu neurčuje nástroje subagentů, ale hlavní session, takže z něj větvení neplyne. Skutečná příčina je, že skilly typ subagenta neurčují vůbec. Zbytek – založit vlastní typ agenta bez toho nástroje – zůstal v `todo.md` jako samostatná položka, protože dostupné typy jsou řezané na čtení a panel specialistů by jimi nešel pustit.

- [x] **Doříct, jak se konflikt podle pravidla *Rozsah pravidla se nešíří sám* řeší.** (2026-09-14) Kritérium je jedno: platí pravidlo pro obě sekce **stejně**? Pak se buď zobecní nadpis nad obě (a dvě sekce s vymezeným rozsahem tam nemají co dělat), nebo se odkáže. Platí-li v každé jinak, nejsou to dvě kopie, ale dvě různá pravidla a píšou se obě celá. **Opsat pravidlo podruhé je vždycky špatně** a hybrid odkazu s opsanou půlkou je z toho nejhorší – rozejde se právě ta opsaná část a odkaz vedle ní budí dojem, že je to hlídané. Tím se vysvětlily i tři různé postupy použité dřív ve čtyřech případech.

- [x] **Odkaz na fázi cizího skillu nehlídá nic.** (2026-09-14) Vyřešeno cestou, kterou zadání označilo za správnou: postup se přesunul do `~/.claude/skills/PREFLIGHT.md` a oba skilly na něj odkazují jako na sdílený zdroj, místo aby si ho půjčovaly přes cizí fázi. Určení rozsahu z gitu tam ostatně už bylo. K tomu se opravil vzor `CIZI_FAZE` v testu, který kvůli hvězdičce míjel kurzívou psané odkazy – a právě v té podobě v repozitáři ležel skutečný nález ve `/skill`. Kontrola nově čte i vedlejší soubory skillů, kde jich `skills/ptydepe/terms.md` nasbíral osm. Dnes je odkazů dovnitř cizí fáze **nula** a hlídá to test.

- [x] **Prohnat starší skilly režimem `/skill update`.** (2026-09-14, `65052e5`) Dvanáct ze třinácti na normě. Dominantní vada byla u všech táž: příprava **opsaná** místo odkazu na `PREFLIGHT.md`, tedy dvanáct kopií něčeho, co má jeden zdroj. Každý skill si nechal jen vlastní předpoklady a výslovně říká, které body neplatí a proč; `/project` navíc přiznává vědomou odchylku (běží i nad prázdným adresářem, kde `PREFLIGHT` velí skončit). Tělo `/review` kleslo z 632 na 495 řádků a `/project` z 506 na 468 vytažením zadání pro agenty a katalogu typů do vedlejších souborů. Zbývá `/transcript`, viz `todo.md`.

- [x] **`done.md` vedle `todo.md` pro hotové úkoly.** (2026-09-01) (zadáno 1. 9. 2026) Dnes drží `todo.md` nehotové i hotové, takže není přehledně vidět, co zbývá, a vzniká zbytečná otázka, kam hotové patří (konec souboru vs. pod sekci). Nově: `todo.md` jen nehotové, `done.md` jen hotové. U strukturovaného `todo.md` se sekce zrcadlí i v `done.md`.

  Co udělat:
  - `structure.md`: `done.md` mezi standardní soubory; v `/project` **jedna zaškrtávací volba pro obojí** – todo a done dohromady, samostatně nedávají smysl.
  - Zrušit v `structure.md` pravidlo o sekci `## Hotovo` uvnitř `todo.md`.
  - **Projít všechny projekty, kde `todo.md` je** (v obou režimech umístění), založit `done.md`, hotové přesunout, commitnout.
  - `context/CLAUDE.md`: smazat výjimku o hotových pod sekcemi – je překonaná.
  - **Nezapomenout** (dodatek k zadání): skilly zapisující do `todo.md` (`/cleanup` tabulka „kam co patří“, `/oponent` odložené nálezy, `/implement` nápady nad rámec plánu); dvě sekce v `~/.claude/RULES.md` o odložených a parkovaných věcech; **revize** rozhodnutí *Provozní soubory jsou centrální* z 28. 8. 2026 v `decisions.md`, které dnešní pravidlo o hotových pod sekcemi zavedlo – záznam se nepřepisuje, přibude revize.
  - **Postup:** pět bodů „na co se zapomnělo“ výš probrat **jeden po druhém** a rozseknout, ne najednou.
  - **K rozhodnutí:** zakládat `done.md` i tam, kde zatím nic hotového není? Odporovalo by to pravidlu „nezaložený soubor není odchylka“. A zachovávat u přesunutých položek datum dokončení?

- [x] **Umístění standardních souborů jako rovnocenná volba, ne výjimka.** (2026-09-01, viz `decisions.md`) (zadáno 1. 9. 2026) Dnes `structure.md` předepisuje `docs/` a projekt, kterému to nesedí (třeba `context`), to má zapsané jako *výjimku z pravidla*. Jenže potřeba výjimky je podle `~/.claude/RULES.md` (*Mechanická pravidla nad rozhodováním případ od případu*) nejdřív signál, že je špatně formulované pravidlo – a tady přesně o ten případ jde: nejsou to výjimky, je to druhý legitimní režim.

  Co udělat:
  - Rozšířit `structure.md` o **dva rovnocenné režimy umístění**: standardní soubory v `docs/`, nebo přímo v rootu projektu. Žádný z nich není výjimka.
  - **Pozor na worktree layout:** root projektu je tam až podadresář větve (`main/`), ne kontejner s `.bare`.
  - Ve `/project` se na režim **zeptat hned** a nechat **zaškrtat checkboxy, které standardní soubory založit**. Jen u těch, kde to má smysl (`todo.md`, `decisions.md`); soubory, které vznikají až voláním dalšího kroku životního cyklu (`design.md`, `plan.md`, `prd.md`), se v `/project` neřeší.
  - **Projít všechny projekty v `~/Dev`, kde už `/project` proběhl**, a doplnit do nich zvolený režim – aby bylo zřejmé, že tam není výjimka, ale vědomý výběr, a který.
  - **Dorovnat formulace, které dnes předpokládají `docs/`:** `~/.claude/RULES.md` má v *Životním cyklu projektu* podmínku „projekt s dorovnaným nastavením a založeným `docs/` nepotřebuje `/project`“ (zapsáno 1. 9. 2026 při oponentuře) – s dvěma režimy přestane platit doslova.

- [x] **Vypořádány všechny nálezy tří nezávislých posudků** (2026-09-02) – posudek věrnosti původnímu zadání (7 ztracených požadavků + 4 posuny významu), audit překryvů mezi kroky životního cyklu (13 nálezů) a průzkum cizího repozitáře `claude-leverage`. Celkem 29 položek. Nejvýznamnější: seznam citlivých oblastí konečně čte `/review` a dělá specialistu na bezpečnost povinným; ten dostává jmenný seznam tříd zranitelností místo odkazu na dokument z paměti; panel se dávkuje na 3–4 specialisty; `/code-review` se volá s explicitní úrovní; `e2e` a `coverage` mají vlastníka; fuzzing a property-based testy rozhoduje `/specify` a kontroluje `/breakdown`; tabulka kontrol má sloupec „kdo ji zapíná“. Zdůvodnění v `decisions.md`.

- [x] **Průběžná kontrola: dvě opravy nalezené vlastní kontrolou** (2026-09-02) – otisk stavu nezahrnoval obsah souborů, takže po první zelené kontrole šlo tentýž rozpracovaný soubor libovolně rozbít a hook to nechytil; a chybějící nástroj se hlásil jako padající kontrola, takže model opravoval nevinný kód. Obojí doloženo experimentem, opraveno a pokryto regresí. Zdůvodnění v `decisions.md`.

- [x] **Ekonomika modelů zapracovaná napříč workflow** (2026-09-02) – `RULES.md`, *Model a effort podle úkolu*: tabulka podle role úkolu (ne podle jmen modelů), pravidlo nula (nejlevnější práce je ta, kterou neudělá model), *effort lad dřív než model* a eskalace `high` → `xhigh` → `max` → silnější model. Konkrétní volba doplněna do deseti skillů. Opraveny dvě zastaralé věci z původní rešerše: „Sonnet výchozí“ (dnes se doporučuje Opus) a představa, že se náklady řídí výměnou modelu (řídí se effortem). Zdůvodnění v `decisions.md`.

- [x] **Průběžná kontrola: souhlas jde vypsat a odebrat** (2026-09-02) – `verify.sh --list` vypíše, pro které projekty je souhlas vydaný a jestli tam kontrakt pořád je; `--revoke <project>` ho odebere. Do té doby šel souhlas jen přidat. První běh `--list` rovnou našel souhlas pro testovací adresář, který už neexistoval. Zdůvodnění v `decisions.md`.

- [x] **Pomlčka v kontraktu příkazů znamená „neaplikuje se“** (2026-09-02) – `- test: -` hook přeskočí mlčky, chybějící řádek dál hlásí jako nezkontrolované. Zapsáno v `coding/quality.md`, *Kontrakt příkazů*; zdůvodnění a riziko v `decisions.md`.

- [x] **`~/.claude` má vlastní kontrakt a průběžnou kontrolu** (2026-09-02) – hook nově hledá i `.claude/CLAUDE.md` (projektem zůstává kořen repozitáře, ne `.claude/`). Kontrakt je `lint: shellcheck --severity=info`, typecheck a test s pomlčkou. Přitom se opravilo šest nálezů `shellcheck` ve `statusline.sh` a `transcribe.sh` – repozitář, který průběžnou kontrolu vynalezl, ji na sebe neměl.

- [x] **Ověřovatelé v `/review` běží na nejsilnějším modelu** (2026-09-02) – Fáze 3 předepisovala model specialistům panelu, ale ne ověřovatelům, přestože tentýž skill píše, že na téhle vrstvě stojí jeho použitelnost. Slabý model nález nepotvrdí ani nevyvrátí, jen přizvukuje.

- [x] **První ostrý běh `/attack`** (2026-09-02) – skill poprvé běžel proti živé aplikaci a odhalil **sedm mezer v sobě samém**, všechny opravené: prázdný diff na hlavní větvi, chybějící postup ověření cíle, neznámý obsah testovací databáze, kolize agentů nad sdílenými daty, prohlížeč nejde sdílet mezi agenty, chybějící vektor *vykreslení* a úklid zvednuté infrastruktury. Potvrdilo se i to, že příprava správně zastaví práci, běží-li projekt v produkci proti jediné instanci databáze – izolace se pak musí vyrobit zvednutím lokálního stacku. **Nálezy nad samotnou aplikací sem nepatří** a leží u ní; tohle je záznam o skillu.

- [x] **Explorativní útok má vlastní krok životního cyklu** (2026-09-02) – nový skill `/attack`: zvedne aplikaci lokálně a paralelní agenti ji zkouší rozbít vstupy, pořadím kroků, cizími identitami a mezními daty. Zařazený **před `/release`**, ne do uzavírání – `/review` je levný a běží často, útok je drahý a dělá se zřídka nad hotovým celkem. Nálezy nedostane ověřovatel, ověřují se přehráním reprodukčního postupu, a každá oprava si odnese regresní test. Zdůvodnění včetně zamítnuté varianty (role v panelu `/review`) v `decisions.md`; tři druhy záruk v `coding/coding.md`.

- [x] **Čtyři průchody `/consistency` nad `~/.claude`** (2026-09-02) – 37 nálezů (14 + 8 + 10 + 5), všechny vypořádané. Nejvážnější: `/attack` neměl v `allowed-tools` `Write` ani `Edit`, přestože opravuje kód a zapisuje nálezy – **nedokázal by nic z toho, co slibuje**. Dál: `note_broken` mizel ve větvi druhého pokusu v hooku, tři kontroly měly v tabulce vymyšleného vlastníka, `/project` obsahoval návod, jak tiše obejít vlastní kontrolu, a `__pycache__` se dostal do gitu. Poučení o konvergenci a o tom, že největší riziko nese poslední oprava, je v `decisions.md`.

- [x] **Souběžné session si v `~/.claude` přebíraly commity** (2026-09-03) – téhož dne dvakrát za sebou session pracující na `/project` commitla `git add -A` a smetla s sebou rozpracované změny druhé session ve `skills/transcript/` (`bd9b56d`, `9fc3e53`). Obsah se neztratil, ale `git blame` odkazuje na commit o něčem jiném a jedna zpráva popisuje diff, který v ní není. Pushnutá historie se už opravit nedala. Vyřešeno pravidlem `RULES.md`, *Commituj jmenované cesty, ne `-A`*, i se zdůvodněním, že commit message je jediné místo, kde je zapsané proč – a když sedí u cizí změny, je to zdůvodnění ztracené.

- [x] **Každý skill dostal vlastní README a termín „osa“ zmizel** (2026-09-06) – vznikla norma *README skillu* v `~/.claude/skills/SKILLS.md` a podle ní **README u všech 18 skillů**: co skill umí, proč zrovna ten, jak se používá, ukázka výstupu, co nedělá a instalace psaná jako pokyn pro Clauda. Kořenové `README.md` se zkrátilo na jeden odstavec na skill, cyklus v pořadí kroků a zbytek abecedně. Vynucuje to osm testů (celkem 59). Zároveň se sjednotil termín **Životní cyklus projektu** místo dosavadní dvojice „osa“ / „Životní cyklus práce“ – napříč `RULES.md`, skilly, testy, knowledge base i dva projekty; kontrolní průchod přes všech 57 repozitářů v `~/Dev` nevrátil nic. Zdůvodnění i zavržené varianty v `decisions.md`.

- [x] **Přejmenované termíny propsány do projektů v `~/Dev`** (2026-09-07) – druhá půlka fronty termínů: co se dohodlo v `~/.claude/PTYDEPE.md` a nahradilo v `~/.claude` a `~/Dev/context`, se dorovnalo i v projektech. **22 náhrad v 9 souborech napříč 6 projekty:** zelená linka → průběžná kontrola, brána → blokující kontrola, viséc → pozůstatek, chirurgický zásah → cílený zásah, a `~/.claude/green-line.sh` → `verify.sh` na třech místech, kde by zapsaný příkaz dnes spadl na neexistujícím souboru. **Vědomě nezměněno:** platební brány i s celým `gateway_status`, schvalovací brány v klientských zadáních (lidské schválení, ne automatická kontrola), aplikační kontrolní body (idempotence tokenu, hash artefaktu), vstupní brána v doméně kurzu a fyzika v jednom školním projektu. Kontrolní průchod přes `~/Dev` nevrátil mimo evidenci rozhodnutí nic.

- [x] **Backlog oddělen od todo napříč standardem i projekty** (2026-09-07) – `structure.md` dostal `backlog.md` jako třetí soubor vedle `todo.md` a `done.md`, s hranicí po rozhodnutí, ne po termínu. Propsáno do `RULES.md`, `/project` (zakládání, deklarace i revize), `/specify` (povinně backlog projde a nabídne, co vytáhnout do zadání), `/cleanup` (mapa, zatřídění, volba ve Fázi 7) a `/implement`. **Zavedeno v osmi projektech** s `todo.md`, včetně dorovnání deklarace struktury, které u čtyř z nich neuvádělo ani existující `done.md`; ve dvou se rovnou přesunuly nalezené nápady. Zbylé projekty roztřídí `/project update`. Zdůvodnění a zavržené varianty v `decisions.md`.

- [x] **Zastaralý řetěz kroků cyklu v šabloně `/project`** (2026-09-07) – sekce `## Typ projektu` se do každého vývojářského `CLAUDE.md` zapisovala s vypsanou cestou bez `/discovery`; opraveno v šabloně i ve dvou projektech, které ji měly zapsanou. Krok 14 nově kontroluje **znění** generovaných sekcí a chybějící standardní soubory, norma zakazuje opisovat seznamy s vlastním zdrojem pravdy a test to nad `SKILL.md` vynucuje, ověřený mutačním testem i návratem té vady. Zdůvodnění v `decisions.md`.

- [x] **Přepínač autocommitu bez zastřešující sekce** (2026-09-07) – `## Automatické akce` zastřešovalo jediný podnadpis, protože druhá automatika (autoprompt) mezitím zanikla; přepínač se proto přesunul o úroveň výš na `## Autocommit`, v globálním `CLAUDE.md` totéž pro `## Autocommit v projektech`. **Srovnáno naráz v sedmnácti projektech v `~/Dev`** i v `~/.claude` – starý tvar by `/autocommit` hlásil jako vypnutý, přestože zapnutý je. Propsáno do `/autocommit` (detekce, zápis, vypnutí a nově i srovnání starého tvaru v globálním souboru, aby nevznikly dvě definice mechanismu vedle sebe), `/project` (režim `adopt` starý tvar dorovná), `PREFLIGHT.md` a odkazů v sedmi skillech. Obě strany mechanismu hlídá po vlastní kontrole v `tests/test_skills.py`. **Úklid po změně sáhl dál:** detekce vylučuje globální soubor cestou, ne jménem nadpisu, a čte i text pod nadpisem, aby sekce s tělem „Autocommit je vypnutý.“ tiše neznamenala opak; `/autocommit` je zároveň dorovnaný na normu `SKILLS.md` a vyškrtnutý ze seznamu `MIGRACE`, a otázka, jestli se má vstřebat do `/project`, je uzavřená zamítnutím. Zdůvodnění, zavržené varianty i rozdíl mezi dorovnáním v `/project` a nabídkou v `/autocommit` v `decisions.md`.

- [x] **Worktree layout a standard struktury ven do veřejného repozitáře** (2026-09-07) – provozní pravidla worktree layoutu i konvenci struktury projektu odkazovalo dvanáct veřejných skillů a `PREFLIGHT.md`, ale samy ležely v soukromém `~/Dev/context`. Kdo si skill nainstaloval z GitHubu, dostal odkaz do adresáře, který nemá. **Vznikl skill `/worktree`** (`SKILL.md` se zřízením i zrušením kontejneru, `README.md`) a v kořeni **`~/.claude/STRUCTURE.md`** i **`~/.claude/WORKTREE.md`**, oba vedle `RULES.md`. Rozhodovalo přitom, **kolik skillů to čte**: `WORKTREE.md` jich má dvanáct a `structure.md` devět, takže oba patří do kořene, ne dovnitř skillu. `worktree.md` tam nejdřív skončil a **opravilo se to až čtenářem bez kontextu v `/cleanupu` téže session** – zapsáno i s tím, proč se to spletlo. Zdůvodnění, zavržené varianty i zúžení rozsahu (`coding.md`, `text.md`, `design.md` zůstávají soukromé) v `decisions.md`, *Skill si nese své věci s sebou* a *Standard struktury projektu je veřejný, ale nepatří žádnému skillu*. **Padl tím starší plán domény `context/claude/`** ze 4. 9. 2026 – tam je to označené jako nahrazené.

- [x] **`/autocommit` si nese pravidla s sebou, režimy `enable`/`disable`** (2026-09-07) – pravidlo commitování stálo v globálním `CLAUDE.md`, opsané i ve skillu, a hlídal je test na shodu obou kopií; rozbalovalo se přitom do každé session i v projektech s vypnutým autocommitem. Nově je v `skills/autocommit/autocommit.md` a importuje si ho sekce `## Autocommit` v projektovém `CLAUDE.md`. **Sekce bez importu je starý tvar** a skill ho srovná. Migrováno sedmnáct souborů `CLAUDE.md` (šestnáct v `~/Dev` včetně dvou worktree téhož projektu, plus `~/.claude`), čtyři rozcestníky kontejnerů a přes čtyřicet souborů odkazů; test na shodu šablony zanikl, protože duplicita přestala existovat. **Jeden projekt vynechán** – měl rozdělanou cizí práci.

- [x] **Výjimka pro hromadnou migraci konfigurační vrstvy** (2026-09-07) – `worktree.md` zakazoval práci v `main/` „bez výjimky“, ale jednořádkový zápis do všech projektů naráz se do toho pravidla nevešel a dnešní migrace ho porušila. Výjimka je teď pojmenovaná i s podmínkami (čistý strom, jmenovitý commit, ověření diffu řádek po řádku) a s kritériem, které brání jejímu rozšíření na běžnou práci.

- [x] **Fronta termínů k revizi vypořádána celá** (2026-09-07) – session věnovaná neustáleným termínům, které Claude převzal z náhodné zmínky v konverzaci a začal používat napříč projekty. Postup u každého: vysvětlit význam a původ, dohodnout náhradu, zapsat do `~/.claude/PTYDEPE.md` a nahradit ve všech verzovaných souborech v `~/.claude` i `~/Dev`. Bere se vždy jen jeden a další až po dokončení předchozího. **Obě vlny jsou vypořádané (7. 9. 2026).** Druhá vzešla z běhu `/ptydepe suggest` a přinesla dvě obecná kritéria, která si skill odnesl do *Fáze 3*: termín, který je zároveň **identifikátorem v kódu**, se nepřekládá, a **anglicismus použitý mimo svůj oborový význam** je horší než obyčejný anglicismus. Další kandidáty vytipuje `/ptydepe suggest` znovu.

  **Hotovo:** brána → blokující kontrola · viséc → pozůstatek · voda → vata · zelená linka → průběžná kontrola (včetně identifikátorů `green-line.sh` → `verify.sh`, `CLAUDE_NO_GREEN_LINE` → `CLAUDE_NO_VERIFY`) · ráčna → opis („seznam musí přesně sedět“) · vizitka → README skillu · úhel → hledisko · koncové věty → závěrečný verdikt · zamluvené téma → nevypořádané téma · chirurgický zásah → cílený zásah · fresh-reader → čtenář bez kontextu · tah → odpověď · šťastná cesta → hlavní scénář · panel rolí → panel specialistů · skeptik → ověřovatel · pre-flight → příprava · stub → rozcestník · baseline → konvence projektu a srovnávací běh · drift → rozejití · tabulka švů → tabulka delegací · explorer → průzkumník · debrief → řízený rozhovor · fan-out → rozeslání práce agentům · sonda → ověřovací pokus a kontrola závislostí (dva významy rozděleny) · osa, vektor, stopa práce, sledovací okno, kontrakt příkazů, soustava, session, mutace, guard a heuristika prověřeny a ponechány

- [x] **Skill `/learn` na zapracování nové znalosti do knowledge base** (2026-09-10) – mezera mezi `/transcript` a knowledge base: z přepisu školení se dala vytěžit metodika, ale končila buď nikde, nebo jako samostatný soubor vedle struktury. Skill zdroj vytěží do očíslovaného seznamu poznatků, úplnost nechá ověřit izolovaným agentem a poznatky rozpustí do stávajících textů; hloubku zásahu odvozuje z povahy textu (metodika / fakta a hotové formulace / doklad), ne z konfigurace. Skutečný rozpor odlišuje od zjednodušení a zúžení a předkládá jen ten. **Měření vyvolání 10/10**, **tlakové scénáře neproběhly** – vedeno v položce o evaluacích v `todo.md`. Zdůvodnění a zavržené varianty v `~/Dev/context/decisions.md`.

- [x] **Skill `/audit` – audit cizího webu proti pojmenovanému návodu** (2026-09-10). Parametrem se řekne, proti kterému návodu z `~/Dev/context/` se audituje: `/audit analytics` projede měření klientského webu proti `analytics/` včetně katalogu nálezů, `/audit web` rozhraní proti `web/web.md`, `/audit text` text proti `text/`. Vzniklo zobecněním užšího návrhu `/measure` (jen měření) – parametrizovaný skill je *generic-base + delta*, kdežto skill na doménu by se musel psát pro každou znovu.

  **Proč zrovna tenhle:** `analytics/` je nejhotovější doménová znalost (94 souborů, vlastní katalog nálezů) a jako jediná nemá skill, který by ji řídil. Doména dnes existuje jako checklist, který nikdo nevyvolá ve správný moment.

  **Předlohou je `/attack`, ne `/review`** – audituje se běžící věc, ne kód: sebrat přístupy, projet web přes chrome-devtools MCP, zkontrolovat dataLayer, GTM a consent, nálezy se `severity` a `basis`, ověřovatel, který má nález vyvrátit, a předání do `/report`.

  **Vymezení vůči `/review`, které musí být v `Co skill nedělá`:** `/review` má mezi rolemi „soulad s doménovými standardy“, ale měří **vlastní práci na projektu**. `/audit` měří **cizí věc** – klientský web, měřicí nastavení, převzatý text. Bez toho rozlišení by šlo o duplicitu kroku životního cyklu (`~/.claude/RULES.md`, *Životní cyklus projektu*).

  **Postaveno jinak, než zápis předpokládal, ve třech bodech.** Doména se nepředává jako holý parametr, ale volným popisem (`/audit analytiky na www.example.com`); režimy `full`, `brief`, `audit`, `report` a `update` dělí dlouhý běh na části; a `/report` **není** výstupní cestou – výstupy sestavuje skill sám podle normy výstupu domény. Zdůvodnění v `decisions.md`, *Skill `/audit`: audit cizího webu proti doménové znalosti*.

- [x] **Rozhodnuto, že do každé session nemá jít `RULES.md` celý** (2026-09-10) – a provedeno. Paušální kontext klesl ze **100 kB na 57,4 kB**: rozhraní kroků životního cyklu odešlo do `~/.claude/skills/LIFECYCLE.md`, `STRUCTURE.md` se přestal importovat a zůstal odkazem, `PTYDEPE.md` se týž den rozdělil na tabulku a rozvahu u skillu. Nic se nesmazalo – mění se jen okamžik načtení.

  **Protiargument, kvůli kterému položka roky visela, se vyřešil mechanismem, ne slibem.** Námitka zněla, že podmíněně načítané pravidlo se dodržuje jen tehdy, když si na něj někdo vzpomene. Odpověď: `PREFLIGHT.md` načtení obou souborů **žádá po každém skillu** a tři testy hlídají, že se ten pokyn nevytratí a že se `@` nevrátí do `CLAUDE.md`. Zbývá vědomě přijatá sleva u práce mimo skill, kde mechanismus není žádný – zapsaná v `decisions.md`.

  **Otázka „změřit, kolik z `RULES.md` se v běžné session uplatní“ padla.** Měřit se to nemuselo: rozhodovalo, které části se čtou **jen v konkrétní situaci**, a ty šly poznat bez měření. Kolik z `RULES.md` zbývá stlačit a proč se dál nešlo, drží tabulka *Kde se přestalo* v `decisions.md` k témuž datu.

- [x] **`statusline.sh` už nespustí program z cizího `.git/config`.** (2026-09-14) `becef7c` Status line se překresluje po každé odpovědi, permission systém na ni nesahá a stojí v adresáři, který si uživatel nevybral. `git diff --name-only`, kterým počítala změny, přitom čte obsah pracovních souborů, takže na ně aplikuje `filter.<name>.clean` z konfigurace toho repozitáře – stačilo rozbalit archiv nesoucí `.git/` a stát v něm. Ověřeno spuštěním nad návnadou: status line vypsala normální řádek a zároveň spustila cizí příkaz.

  **Proč se to neřeší dalšími `-c` přepínači:** jméno filtru si volí ten, kdo config napsal, takže `filter.zlo.clean` nejde předem přebít tak jako `core.fsmonitor`. Obrana je proto na takový repozitář nesahat – konfigurace se přečte napřed, což nic nespouští, a při nálezu se místo počtu změn vypíše varování.

  **Vědomě přijatý falešný poplach:** repozitář s lokálně nainstalovaným git-lfs má legitimní `filter.lfs.clean` a dostane totéž varování. Rozlišovat podle hodnoty by znamenalo, že útočníkovi stačí pojmenovat svůj filtr `lfs`. Cena je nízká, protože varování nahrazuje jen počet změn; jméno větve se čte ze `symbolic-ref`, který obsah nečte, a zobrazuje se dál.

  Regresní testy v `tests/test_statusline.py` hlídají oba směry – spuštěný program i falešný poplach nad běžným repozitářem – a nad nimi stojí mutační test, který vyřízne filtry z blacklistu a ověří, že se cizí příkaz pak opravdu spustí.

- [x] **Deny pravidla na tajemství platí i mimo projekt.** (2026-09-14) `b487ddb` Vzory `Read(**/.env)`, `Read(**/*.pem)` a dalších 44 se vyhodnocovaly relativně ke kořeni projektu: uvnitř držely, mimo něj neblokovaly nic. Claude přitom běžně čte mimo projekt – knowledge base v `~/Dev/context`, cizí repozitáře při `/auditu`, soubory v domovském adresáři. Doloženo návnadou: `.env` mimo projekt se přečetl bez odporu.

  **Správný tvar je `//**/…`, a vybral se měřením, ne odhadem.** Každá změna `settings.json` se projeví až po restartu session, takže se to muselo měřit přes tři restarty: nejdřív plné cesty (`~/…` i `//…`, obojí drží), pak globy, a u nich zvlášť sonda v `/tmp`, tedy mimo domovský adresář i mimo projekt. Tvar `~/**/…` drží taky, ale nechal by díru všude mimo home – `/tmp`, `/var`, externí disky.

  **Ke každé sondě patřil kontrolní soubor** ve stejném adresáři, který projít **měl**. Bez něj by odmítnutí mohlo pocházet z čehokoliv jiného (zakázaný adresář, `/tmp` jako celek) a přepis 46 vzorů by stál na měření, které neměří vzor. Hláška permission systému totiž mluví o *adresáři*, i když blokuje jeden soubor.

  **Závěrečné ověření:** `.env` mimo projekt odmítnut, `.env` uvnitř projektu odmítnut (ochrana se přepisem neztratila), kontrolní soubor v témže adresáři přečten. Návnady uklizené.

  **Vedlejší nález:** `.env` chybělo v `.gitignore` – ve veřejném repozitáři konfigurace. Vyplavalo to jen proto, že se tam založila návnada a objevila se v `git status`.

- [x] **`/specify` dělí větší návrh na tematická kola** (2026-09-16) `~/.claude@db1adaa`. Metoda vznikla samovolně v jednom z projektů a nikde mimo něj nebyla zapsaná. Tvar se rozhodl s uživatelem.

  Rozhodnutí i zamítnuté varianty stojí v `skills/specify/SKILL.md` u míst, kde platí; tvar mapy a záznamu kola v `STRUCTURE.md`. `architecture.md` vzniká až v `close` – tohle rozhodnutí nechal uživatel na skillu. Úklid po čtenáři bez kontextu přesunul zápis kola do `done.md` a přidělení čísla kapitoly až na samostatný krok těsně před sloučením větve, dořešil jednoznačné kritérium `auto` a přidal do `/breakdown` zastavení nad rozběhnutými koly. Nálezy z úklidu: dřív rozběhnutá kola nový tvar zatím nedodržují, úkol k dorovnání je zapsaný u nich (`docs/todo.md`), provede se po sloučení jejich větve.

  **Neověřeno:** skill po kolech zatím nikdy neběžel. První ostrý běh ukáže, jestli blok v `todo.md` čisté session opravdu stačí. `SKILL.md` zůstává přes 300 řádků vědomě, viz tamtéž.

- **2026-09-20** · `39b2ffa` · **`skills/cleanup/SKILL.md` je z tvrdé meze pryč** – tělo kleslo z 494 na 422 řádků vytažením celé *Fáze 7* do `skills/cleanup/out-of-scope.md`. Odložený úkol z 19. 9. 2026 tím padl.

  **Řez se vedl jinudy, než zněl plán, a je to v pořádku.** Původní kandidáti byli tabulka povinností zápisu z *Fáze 4*, sekce *Co dál* z *Fáze 9* a podrobnosti *Fáze 2* – tedy tři malé kusy ze tří různých míst. Fáze 7 je proti tomu jeden souvislý blok o osmdesáti řádcích, který se čte jen tehdy, když se v té fázi opravdu stojí, takže uvolnila víc a rozdělila míň. Původní kandidáti zůstávají ve hře, kdyby skill přerostl znovu; skill je dnes v měkkém pásmu 300–500, kde norma velí rozdělení zvážit, ne provést.

  **Řez po sobě nechal tři vady a našel je až čtenář bez kontextu:** dvě doslovné kopie týchž odstavců v obou souborech (`SKILL.md` si nechal zkrácenou verzi, takže se kopie hned rozešly), odkaz na „kritérium z Fáze 7, bodu 2“ mířící na číslovaný seznam, který se mezitím přestěhoval, a chybějící prázdný řádek před `------`, kterým se ze závěrečné věty stal podle CommonMarku setext nadpis. Potvrzuje to, proč se řez neměl dělat narychlo – jen se ukázalo, že cena za spěch se neplatí ztrátou textu, ale tichými pozůstatky na švu.

- **2026-09-20** · `ee218b9` · **`/specify` se rozdělil na `/specify` a `/architect`** – největší kus dorovnání konfigurační vrstvy na přestavěný životní cyklus. `/specify` zůstal jeden běh bez režimů a vyrábí `requirements.md` plus produktové podklady (423 → 226 řádků měřeno `wc -l`); `/architect` dostal návrh řešení jako sadu dokumentů a celou mechaniku tematických kol (316 řádků). Rozbor v `decisions.md`, *`/architect` vznikl a nemá režimy*.

  **Režimy `auto`, `create`, `round` a `close` zanikly, a to je změna oproti zadání.** Původní plán je `/architect` převzít; uživatel je zrušil s tím, že skill se má zorientovat sám a ovlivnit se dá volnými slovy za příkazem. Bylo to správně i podle normy: `auto` byl výchozí a jeho tabulka rozhodovala čistě ze stavu projektu, takže zbylá tři jména pojmenovávala vnitřní fáze, ne volbu uživatele. Zůstala deterministická tabulka a přibyla povinná věta, která zvolenou cestu oznámí **dřív, než se sáhne na první soubor**.

  **Zamítnutý sdílený `skills/DOCUMENTS.md` byl nejcennější moment běhu.** Návrh zněl přestěhovat šablony obou dokumentů o úroveň výš, protože hranici omezení/volba potřebují oba skilly. Uživatel se zeptal, proč to není věc `STRUCTURE.md` – a byla: hranice tam stála doslova už předtím, takže kapitola *Proč dva dokumenty a ne jeden* ve `specify/SKILL.md` byla druhá kopie **už v té chvíli**, ne něco, co se teprve má kam umístit. Doplnilo se jen chybějící odůvodnění a kapitola ze skillu zmizela.

  **Testy předcházely textům a chovaly se přesně podle plánu.** Nejdřív se dorovnaly měřicí funkce (`cycle_with_order()` čte rámeček jako dvě vrstvy, kontrolní kroky nemají pořadí ani sousedy, nová `cycle_missing_skills()` porovnává přiznané chybějící kroky se skutečností v obou směrech), načež se z 28 selhání stal jmenovitý seznam práce, který zhasínal, jak se texty přepisovaly. Dorovnalo se okolí: rámečky a hromadná instalace ve dvanácti README, kořenové `README.md` přeskládané do dvou bloků podle vrstev cyklu, `RULES.md`, `STRUCTURE.md`, `WORKTREE.md`, `LIFECYCLE.md`, `SKILLS.md`, `/next` a `/scenarios`.

  **Nezkontrolováno zůstaly tlakové scénáře na *Zákaz implementace***, které `/skill` u vynucujícího pravidla žádá povinně, a **první ostrý běh** – skill nikdy neběžel. Srovnávací běh se vynechal vědomě: vstupem nebylo nové téma, ale 423 řádků odladěného textu.

- **2026-09-20** · `1de1d70` · **`/next` čte rámeček cyklu po vrstvách** – `lifecycle()` v `skills/next/collect.py` vracela syrové řádky bloku z `RULES.md`, takže po rozdělení rámečku na osu a kontroly dělala z odsazeného pokračování osy „krok“ jménem `/breakdown` a z komentáře pod rámečkem další položku. Vrací `{"osa": [...], "kontroly": [...]}` a `SKILL.md` z toho odvozuje chybějící krok **jen pro osu** – u kontrolních kroků „ještě nepřišel na řadu“ nic neznamená, protože stojí v mezerách a některé ve víc naráz.

  **Přibyly k tomu testy** (`tests/test_next.py`, třída `LifecycleLayers`) a jsou ověřené mutací: s původní plochou podobou padají. Bez nich by to byla přesně ta tichá vada, kterou má `/next` sám hlídat u živých session – nic nespadne, jen fronta práce nabídne krok, který neexistuje.

  **Uživatel si o tenhle bod řekl uprostřed `/cleanupu`**, hned poté, co ho postavil na první místo před číslovanou řadu.

- **2026-09-21** · Kontrolní skilly se přestaly ptát tam, kde není o čem rozhodovat – zadal uživatel uprostřed `/consistency full` nad rezervačním systémem. Vznikl sdílený `skills/FINDINGS.md` (čtvrtý vedle `PREFLIGHT.md`, `SESSION.md` a `SEVERITY.md`) se třemi skupinami nálezů: **mechanické** nemění chování, **jednoznačné** mění, ale podoba opravy je jedna, **sporné** mají víc obhajitelných podob. Osou tedy není riskantnost zásahu, ale jestli je z čeho vybírat. K tomu pravidlo, že **volby v otázce jsou varianty opravy** – vyjde-li trojice *Opravit / Odložit / Přeskočit*, nález mezi sporné nepatří –, a povinnost vyčíslit v přehledu obojí, kolik se opraví rovnou a kolik doopravdy zbývá na rozhodnutí. Srovnáno v `/review` (držel definici pro ostatní), `/consistency`, `/attack`, `/audit`, `/oponent` a `/cleanup`; předlohou byl `/oponent`, který to dodržoval odjakživa. **Doklad z běhu, který to vyvolal:** ze 37 nálezů označených za sporné jich 18 po uživatelově větě padlo bez jediné otázky. Zdůvodnění i zamítnutá varianta (přejmenovat skupinu „mechanické“) v `decisions.md`. **Podoba té otázky zůstala nesjednocená** – dvě slovní zásoby pro záchytné volby –, vedeno v `todo.md`.
- **2026-09-21** · `5516c6a` · **Dokončení větve dostalo vlastní krok `/merge`.** Postup – přihrát hlavní větev do pracovní, vyřešit konflikty, pustit nad spojeným stavem *Kontrakt příkazů*, teprve pak mergovat a uklízet – žil jako sekce *Dokončení větve* ve `WORKTREE.md`. **Neplatil tedy v projektu bez worktree layoutu**, kde se ten soubor nenačte, přestože nejdražší doložená chyba (souběžný úklid, který 17. 9. smazal nepřimergovanou větev) na layoutu nezávisí; a `/cleanup` ho opisoval odkazem dovnitř cizího souboru na třech místech. Skill je zobecněný na obyčejný repozitář, `WORKTREE.md` si nechal jen layoutové odchylky a `/cleanup` místo popisu postupu volá skill. V životním cyklu stojí mezi **kontrolními kroky**, hned za `/cleanup` – nic nevyrábí, jen převádí hotovou práci tam, kam patří.

  **Zařazení mezi kontroly určil uživatel** slovy, že je to „úplně stejný maintenance kolem již vytvořeného jako cleanup nebo consistency“. Vrstva se tím nerozmělnila: její nosné kritérium *nezvětšuje rozsah práce* platilo v `LIFECYCLE.md` už předtím. Popis obou vrstev se proto musel přepsat ve dvanácti README a v `RULES.md` – „druhé měří, co už je“ nepokrývalo krok, který slučuje.

  **Obrátilo to zamítnutí ze 16. 9.**, kdy se samostatný krok pro dokončení větve odmítl. Argument mířil na **volání**, ne na **umístění postupu** – a volání se nemění: nabídka na konci `/cleanup` zůstává, jen je z ní zkratka k volání navazujícího kroku místo opisu postupu.

- **2026-09-21** · `1a0041b` · **Ptaní se zúžilo podruhé** – den po `FINDINGS.md` se otázka bez volby objevila znovu, uživatel ji zachytil snímkem obrazovky z běžícího `/cleanupu`. Dvě systémové příčiny: podmínka **„netroufáš si“** byla jediná položka tabulky bez vnějšího kritéria, takže se jí dala odůvodnit pracnost (nahradilo ji *chybí údaj, který ví jen uživatel a nedá se zjistit z repozitáře*, plus krok *Nejistotu nejdřív zkus odstranit*); a **zákaz falešné trojice byl psaný na konkrétní slova**, takže táž trojice pod jmény *Vyřešit teď / Zapsat do todo / Zahodit* prošla – a `cleanup/out-of-scope.md` ji sám předepisoval tabulkou. Zákaz teď míří na tvar a má test: **je aspoň jedna volba podobou řešení?** Výjimku má položka, která není vadou, ale novou prací. Opraveno i v `/review` (mělo „cokoliv, u čeho si netroufáš“), v `/oponent` (tvrdil, že trojice je v `/review` a `/consistency` „naopak správně“ – od 20. 9. přímý rozpor) a v hlavní fázi `/cleanup`, kde se u položky ptal *Zapsat / Odložit / Přeskočit*, přestože v otázce nesl hotový návrh, co kam zapsat.

  **Vědomě bez mechanismu.** Hook `PreToolUse` nad `AskUserQuestion` se předložil i s tím, že textové pravidlo se obešlo podruhé; uživatel ho zamítl slovy „hook ne, takhle textově to stačí“. Cenou je, že třetí obejití nezachytí nikdo dřív než člověk u obrazovky.

- **2026-09-21** · `7365c04` · **CI přestalo padat na testech, které měřily jeden konkrétní stroj.** Kontroly na GitHubu selhávaly **na každém pushi** a maily o selhání se staly šumem, který nikdo nečetl – přesně ten směr, před kterým varuje `~/.claude/RULES.md`, *Ověřitelná kontrola místo dojmu*: vrstva, kterou si člověk odfiltruje, nehlídá nic. Lokálně přitom kontrakt procházel, takže se na to nepřišlo jinak než otázkou uživatele, proč mu ty maily chodí.

  **Testy se vázaly na jeden konkrétní stroj, a to dvěma různými způsoby.** `tests/test_next.py` četl `RULES.md` přes `Path.home()` a `collect.py` spuštěný v testu tam na runneru nenašel nic, takže `lifecycle()` vracela prázdno a test padal na chybějícím souboru místo na vadě, kterou má měřit. Kontrola `Stop` hooků hlásila jako chybějící status line, která leží v `~/.config` a **součástí repozitáře není** – na cizím stroji tam být nemá. Testy teď čtou repozitář, ve kterém běží, a hook mimo něj se na cizím stroji přeskočí nahlas.

  **Ověřeno spuštěním s cizím `HOME` a `CI=true`**, ne jen lokálně – jinak by se totéž opakovalo. Prvním zeleným během je `7365c04`.
